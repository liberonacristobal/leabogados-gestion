2026-09-01: Desktop/movil Fase 3 — FACTURACION. BillingView con useIsDesktop(): en la lista 'Por cliente' (y 'Todas'), la funcion fila() pinta cada factura como FILA DE TABLA ALINEADA en desktop (display grid, columnas 62px fecha / 20px icono estado / 1fr N°+concepto+RS / 180px monto+dias a la derecha; padding 9x14). En movil sigue igual (display flex, sin cambios). La foto 'Etapas del cobro' + tiles ya aprovechan el ancho desde Fase 1. Cambio minimo y contenido (solo el contenedor de la fila), reusa toda la logica/filtros/acordeon de BillingView (1.982 lineas, no se reescribio). Verificado a 1280px (facturas alineadas como tabla, agrupadas por cliente con totales alineados) y a 375px (cards por cliente sin cambios). Build verde. Una tabla desktop mas profunda (columnas explicitas vence/estado, orden por columna) queda para cuando se extraiga el estado a useXModel.
