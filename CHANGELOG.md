2026-09-01: Desktop/móvil Fase 0 + Fase 1 (shell de escritorio). (0) Hook useIsDesktop() (matchMedia, SSR-safe, breakpoint UNICO 1024px) — infraestructura para vistas duales. (1) SideNav: barra lateral fija (212px, navy) que reusa TABS_ADMIN/LIMITED + moduloOn + iconos; para admin agrega la seccion Finanzas (Conciliacion/Inteligencia/Cobranza/Repricing/Proyectos/Horas) que antes solo se alcanzaba por menu/paleta. Pie del sidebar: Buscar (paleta) + Copiloto. Se renderiza siempre; CSS .sidenav lo oculta bajo 1024. Media queries: 680-1023 mantiene el cap movil de 600px; >=1024 muestra .sidenav, descapa .shell + margin-left:212, oculta .bottomnav, baja el padding del header (.apphead) y del #main-scroll. FAB oculto en desktop (gate !isDesktop). EL MOVIL <1024 QUEDA IDENTICO (verificado a 375px). Verificado a 1280px: sidebar + contenido full-width + navegacion desde el sidebar OK; a 375px sin cambios; build verde. Las vistas aun usan su layout movil estirado (respiran pero no multi-columna) -> eso es Fase 2 (layouts desktop por vista, piloto Clientes 2-pane). Ver arquitectura-desktop-movil.
