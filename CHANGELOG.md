2026-09-01: Modales iPhone 2do paso. (1) Forms hideHeader (Factura/BillingForm, Nueva tarea/QuickTaskForm, Duplicados/ConciliarFacturasModal) ahora fullscreenOnMobile con boton 'Volver' + safe-area: el Modal marca su panel con clase .modal-fs cuando es fullscreen, y CSS sistemica da safe-area-inset-top a .qt-head, safe-area-inset-bottom a .qt-body, muestra .qt-volver y oculta .qt-close (con !important, gana al display inline). Cada qt-head suma un boton '<- Volver' a la izquierda (className qt-volver, oculto salvo en .modal-fs). (2) FIX overflow horizontal: el cuerpo fullscreen solo centra a maxWidth 720/margin auto en modales con header estandar; los hideHeader van a ancho completo (evita el shrink-to-content que cortaba columnas). + overflowX hidden en fullscreen. (3) Bottom-sheet PROHIBIDO eliminado: 'Asistente de liquidacion' de caja chica pasa de justifyContent:flex-end (panel desde abajo) a modal centrado (borderRadius completo, maxWidth 520, backdrop navy). (4) 'Clasificar pagos' (fullscreen a mano, zIndex 1000) gana safe-area-inset-top en su header + boton 'Volver' (el notch ya no corta el titulo). Verificado en demo movil: Factura full-screen con Volver, sin x, sin overflow horizontal; build verde. PENDIENTE: anticipo/fondo (hideHeader sin qt-head) y ~17 overlays a mano restantes -> migrar al componente Modal en proxima pasada.
