2026-09-12 Escala tipografica (migracion gradual, piloto): regla "colapsar medios-pasos al entero" (8.5->9, 9.5->10, 10.5->11, 12.5->13) aplicada al calendario del equipo. Cifras-hero (22-30) quedan como tier display aparte. Se sigue vista por vista.
2026-09-12 Contraste de personas: "Sin socio" en cockpit #5B7686 -> #93ABBB (2.49 -> 4.98:1, deja de fallar sobre navy) y color de Erasmo global #8A7012 -> #74600E (4.77 -> 6.14:1 sobre blanco), ambos con mismo tono y mejor legibilidad WCAG.
2026-09-12 Unificar personas (lote 2): badges de responsable y "Personal · X" pasan a nombre en color (sin pastilla de fondo); avatares de iniciales reemplazados por punto de color + nombre; cabeceras "por persona" en color. Selectores/filtros siguen como pills.
2026-09-12 Calendario del equipo rediseñado: celdas uniformes, encabezado por semana (etiqueta + conteo + puntos de urgencia), chip de cliente por urgencia (desktop) / punto + cliente (móvil), y click en día abre detalle inline + resalta la fecha.
- 2026-09-12 Unificar personas (alternativa E: nombre en color) - LOTE 1: los BADGES DE DISPLAY del responsable/asignado pasan de pastilla a solo el nombre en el color de la persona (fichas de cliente, filas de venta, asignados de tarea). Los BOTONES para seleccionar persona (asignar, triage, filtro) se mantienen como pastilla (son accion/tappable). Colores desde personChip (config por tenant). Verificado demo 1280. Resto de badges y decision sobre avatares de iniciales: pendiente.
- 2026-09-12 PASO 2 . Socios . la foto: cockpit rehecho como "foto del estudio" (deja de ser duelo C-vs-E). (1) Cada barra es el TOTAL del estudio segmentado por abogado (colores heroCol); (2) toggle Ano en curso / Historico (heroData en dos marcos); (3) chips Todos + por socio con multi-seleccion (suma) que marcan sus segmentos y ponen sus cifras + % del total; (4) Conversion a caja anclada arriba (contexto estudio o seleccion); (5) Ticket y % recurrente; (6) peek (title) por segmento; (7) Vencido en negrita como sub-linea de Por cobrar. GENERICO/vendible: dibuja los abogados que traiga cada estudio (N socios), nada cableado a C/E. Cobrado = de CONVERSION (cobradoBill) -> encadena Facturado = Cobrado + Por cobrar, misma fuente unica que Facturacion (verificado en prod: cobrado 2026 $218M banco separado; conversion Ano C 90% E 74%, Total C 94% E 86%). La tabla completa se mantiene en desktop. Movil = solo el cockpit. Verificado demo 1280 y 375.
- 2026-09-12 Tipografia (fixes de bajo riesgo): (1) el peso 800 ahora SI se carga (se agrega 9..40,800 a la URL de DM Sans) -> los ~230 usos de fontWeight:800 dejan de ser faux-bold sintetizado y se ven igual en todos los sistemas. (2) Se quita Fraunces del index.html (estaba cargada pero no se usa en ningun lado = peso muerto). Space Mono se mantiene (logo FirmDesk). Verificado demo, sin crash. La migracion de tamanos a la escala queda para hacerse gradual, vista por vista.
- 2026-09-12 LoginScreen: se limpia el acceso a demo. Se quitan los textos "Acceso con tu cuenta corporativa", el divisor "o" y el texto "Ver demo con datos de ejemplo"; queda solo la flecha verde (icono play) bajo el boton de Google, que entra a /?demo=1. Verificado.
- 2026-09-12 Demo (?demo=1) enriquecida para mostrar la app sin datos reales. (1) Boot demo ahora carga demoData.bulk_imports (antes setBulkImports([]) hardcodeado a vacio -> Carga masiva salia vacia). (2) LoginScreen: boton "Ver demo con datos de ejemplo" -> /?demo=1 sin login. (3) demoData: +bulk_imports (2 cargas de notaria, una con "no son nuestras" en el resumen), +14 OT de notaria (12 por pagar bi1 + 3 liquidadas bi2), +1 liquidacion (rendiciones rn1), +7 tareas del usuario Demo (Inicio del team con KPIs/agenda/caja), +4 clientes y +3 ventas para densidad. Notaria (Deuda/Cobros/Pagos/Carga/no-nuestras), home del team y listas ahora se ven poblados. Verificado en demo 1280.
- 2026-09-11 Inteligencia / Socios . la foto . ALTERNATIVA A (barras partidas): el hero navy pasa de barras espejo a BARRAS PARTIDAS x2d una barra por metrica dividida por socio (Cristobal, Erasmo, otros), cifras a los extremos. Metricas: Vendido, Facturado, Por cobrar, Vencido (con nota dinamica "X concentra N%" en oro, sin rojo) + Ticket y % recurrente enfrentados + linea de Martin. Colores REALES del hero (heroCol: azul #6FB0DE / oro claro #E0BC5E / verde #8FC57A), DM Sans con tabular-nums (no mono), labels en mayuscula +2px (11px). Se quitan las barras enfrentadas (duelBars) y el martinStrip por redundantes x2d la Alt A los cubre; en desktop se mantiene la tabla completa. Movil = solo el hero (marcador C vs E). Fuente unica (Ventas/Facturacion), sin cifras nuevas. Verificado demo 1280 y 375.
- 2026-09-11 Deuda notaria (pend) . REDISENO: (1) layout desktop (maxWidth 820, antes se estiraba a todo el ancho); (2) tres tarjetas en fila con separacion, ya no a ancho completo: Pendiente a notaria + "Como se paga" (con fondo / adelanta oficina, reparto del total) + Notaria; (3) toggle de agrupacion Cliente / Responsable / Estado (por responsable y estado: cabecera navy plegable con subtotal, N clientes y N OT; los clientes anidados); (4) al seleccionar para pagar, desglose de FONDOS POR ABOGADO (con fondo vs adelanto por cada abogado). Fecha en formato canonico DD-MM-AAAA (notaRow). Movil intacto (tarjetas se acomodan con flexWrap). Verificado demo 1280 y 375.
- 2026-09-11 Carga masiva . Una carga abierta ya no es un muro de OT planas: buscador + toggle de agrupacion (Cliente / Responsable / Estado / Plana). Por defecto agrupa por CLIENTE con cabecera en navy y letras blancas (contraste), subtotal y N OT; cada OT muestra fecha . OT . tramite . estado . monto. La cabecera del cliente abre su ficha. El toggle aparece cuando la carga tiene mas de 8 OT. Verificado demo 1280 y 375.
- 2026-09-11 Inicio del equipo (TasksOnlyView) . REDISEÑO DESKTOP (movil intacto): en escritorio el home del team ya no es lista angosta + riel de 330px (se veia vacio y desproporcionado). Ahora: banda de 4 KPIs a todo el ancho (vencidas / vencen esta semana / activas / terminadas este mes, cada una abre su seccion), debajo el CALENDARIO de dos semanas a la IZQUIERDA (protagonista) y MIS TAREAS a la DERECHA, y abajo el resumen financiero a 2 columnas (caja chica + ultimos gastos). Las sugeridas desde Gmail (solo admin) se conservan como tira bajo la banda. El movil (<1024px) queda identico: _hero + tareas + calendario + caja en una columna. Verificado demo 1280 y 375.
- 2026-09-11 Datos (correccion en produccion) . (1) Gasto "Movilizacion Julio Uber + Micro" ($11.600, oficina) estaba categoria Notaria por error -> movido a Movilizacion; sale de la deuda a notaria. (2) OT 475674 "Autorizacion de uso de domicilio comercial" ($10.000) estaba asignada a Pablo Demaria pero es gasto de la oficina -> reasignada a Liberona Escala (client_id oficina, entity_id null); ahora "se cubre sola" y es seleccionable para pagar. Deuda a notaria: 102 OT/$6.927.600 -> 101 OT/$6.916.000.
- 2026-09-11 Notaria . OT "no son nuestras" ahora tienen LUGAR PROPIO (antes se perdian al cerrar la carga). Las OT que marcas "no es nuestra" en la carga ya se persistian en bulk_imports.resumen.noNuestras, pero no habia donde verlas/accionarlas despues. Ahora: (1) tarjeta-alerta en el hub de Notaria "N OT que no son nuestras . X por consultar" (solo aparece si hay), respeta canon (accion=alerta, no KPI de plata, no rompe el grid 2x2); (2) sub-vista con la lista acumulada (OT, materia, requirente, monto, carga de origen), seleccion multiple, badge "consultada el DD-MM" y boton "Resolver" por fila (si era nuestra / la notaria respondio -> sale de la lista, reversible); (3) boton "Consultar a la notaria (N)" que envia el correo a sdelgado@notarialascar.cl dirigido a Silvana y Mildred, estudio en copia, mismo texto que en la carga, con confirmacion. Estado consultada/resuelta persistido en learnings kind notaria_nonuestra (value "estado|fechaISO"). Sin tabla nueva ni flag en expenses: reusa la data ya guardada. NOTA_DIRIGIDO hoisteado a scope de modulo (DRY, lo usan la carga y el hub). Verificado demo 1280 y 375.
- 2026-09-11 Notaria . Consultar a la notaria (OT no nuestras): correo destino fijado a sdelgado@notarialascar.cl (se quito el gmail personal del default) y saludo personalizado a "Estimadas Silvana y Mildred," (NOTA_DIRIGIDO). El envio sigue con confirmacion + estudio en copia.
- 2026-09-11 FUENTE UNICA (fix): las vistas nuevas mostraban un "cobrado" del plano FACTURA (cobradoBill/paid_at) que NO cuadra con los indicadores de Inicio ni con Facturacion por socio (plano BANCO/conciliacion). Regla del usuario: cero cifras nuevas; los numeros salen de la fuente de verdad (Inicio + Facturacion). PASO 1 (seguro): saco el cobrado/conversion del plano factura de las vistas nuevas para no dejar ningun numero mal. Socios . la foto: hero pasa a Vendido/Facturado (fuente unica) con nota "el cobrado a caja se ve en Inicio y en Facturacion por socio"; se quitan del cockpit las filas Comisiones/Cobrado/Cobrado neto/Conversion a caja neta/Eficiencia (bloque renombrado "Facturacion y cartera"). Ingreso por ano (Ventas): se quita el "$X cobrado". PASO 2 pendiente: reconectar el cobrado a la fuente de Inicio (banco/conciliacion) via un helper unico, verificando que de identico a Inicio/Facturacion. Verificado demo 1280.
- 2026-09-11 Inteligencia/Socios: hero rediseñado a BARRAS ESPEJO (Diseño 2 elegido por el usuario), reemplaza los anillos. Dos filas de barras enfrentadas con mismo peso: "Conversion a caja neta" (eficiencia, cobrado neto/vendido propio) y "Aporte a la caja del estudio" (contribucion = cobrado neto / cobrado neto total). Cristobal azul crece hacia el centro desde la izquierda, Erasmo dorado desde el centro a la derecha;  0.000000e+00n los extremos; pie con $ a caja / de $ vendido. Sin anillos, sin VS, sin preguntas. Responde de un vistazo quien convierte mejor Y quien aporta mas. Verificado demo 1280 y 375.
- 2026-09-11 Ajustes de feedback: (1) Por socio (Facturacion): alineacion linea con linea entre socios en movil (el label "neto de comisiones" se partia en 2 lineas y descuadraba) -> ahora "neto", nowrap, cifras alineadas. (2) Inteligencia/Socios hero: el anillo de Cristobal era navy sobre fondo navy = invisible -> colores claros por socio para los anillos sobre el navy (heroCol); titulo simplificado a solo "Conversion a caja neta"; quitado el "VS" (dos anillos lado a lado). (3) Ingreso por ano (Ventas): cuotas con vencimiento ANTERIOR al ano de la venta (dato malo, ej. typo de fecha) se marcan "revisar" (tag ambar en el ano + "revisar fecha" en la cuota) en vez de esconderse; solo 2 casos en toda la base (Vasa, Eugenia Perry). Datos NO tocados. Verificado demo 1280 y 375.
- 2026-09-11 Facturacion . Por socio: REVERTIDO al diseno original (a pedido del usuario). Se quitaron los anillos/hero/barras enfrentadas que se habian agregado; vuelve a los dos paneles (El mes + Acumulado, Cristobal | Erasmo, Facturado y Entro a caja bruto/neto). UNICO cambio respecto al original: pasa de modal chico a PAGINA full-screen (Modal fullscreen fsMaxWidth 760), adaptado en tamano para desktop y movil. Bugfix: otrosHay a booleano (antes 0 && ... renderizaba un "0" suelto). Verificado demo 1280 y 375.
- 2026-09-11 Facturacion . Por socio: el hero pasa de "eficiencia de cobro" (caja/facturado) a CONVERSION A CAJA NETA (caja neta / vendido), la MISMA senal que Inteligencia . Socios (a pedido del usuario: "el hero era el comparativo de inteligencia"). Se calcula vendido del ano por socio desde sales (Activo+Terminado, sin subarriendo, ventaCLP, atribucion s.responsible||cliente). Las barras (facturado/comisiones/neto/caja) quedan igual. En prod la caja conciliada ~= cobrado del cockpit -> conversion consistente entre ambas vistas (~53 por ciento); en demo sale baja por poca conciliacion.
- 2026-09-11 Facturacion . Por socio: de modal chico a PAGINA (Modal fullscreen, patron Cierre de mes) con diseno de anillos + barras. Hero navy con dos anillos = EFICIENCIA DE COBRO del ano (caja neta / facturado neto) Cristobal vs Erasmo. Acumulado en barras enfrentadas: Facturado bruto -> Comisiones (ambar) -> Neto factura (lo que deja, barra gruesa) -> Entro a caja neto; El mes idem. Comisiones explicitas + neto protagonista. Se conserva todo el calculo (mesAcc/ytdAcc, navegacion de mes, export CSV). Verificado demo 1280 y 375. (Los % bajos en demo son por poca conciliacion; en prod ~88/81.)
- 2026-09-11 Inteligencia . Socios la foto: rediseno visual (menos plano). Hero navy "duelo" con dos ANILLOS de conversion a caja neta (Cristobal vs Erasmo, VS al centro, % grande dentro del anillo + "$X deja de $Y"). Debajo, BARRAS ENFRENTADAS por metrica (agrupadas Lo que trae / Lo que deja / Lo comprometido): la barra crece hacia el lider; en DSO y Vencido (menos=mejor) el mejor va verde y el peor rojo. En movil las barras reemplazan el marcador (mas atractivo y sigue siendo cara a cara); en escritorio queda ademas la tabla completa debajo. Martin en tira compacta. Verificado demo 1280 y 375.
- 2026-09-11 Inteligencia . Socios la foto: la CONVERSION A CAJA NETA (cobrado neto de comisiones dividido vendido) pasa a ser el numero heroe. Nuevo hero band arriba del cockpit (una tarjeta grande por socio con el porcentaje + "$X deja de $Y vendido"), tanto en escritorio como en movil (antes del marcador). Es la senal financiera real de las ventas (no se puede maquillar: sobrevive a facturado + cobrado + neto de comisiones), vs el "vendido" que es vanidad. Se sumaron las comisiones a colaboradores (terceros) al calculo: IntelligenceView ahora recibe terceros; socios computa comCaja/comFact, cobrado neto y facturado neto. Bloque Cobro renombrado "lo que deja" con filas nuevas: Comisiones, Cobrado bruto, Cobrado neto, y la fila estrella Conversion a caja neta. Verificado demo 1280 y 375.
- 2026-09-11 Inteligencia · pulido: (1) el panel de socios se renombra "Socios . la foto" (antes "cara a cara", sonaba a enfrentamiento); intro y marcador movil suavizados ("quien lidera" / "metricas al frente" en vez de "GANA"). (2) El radar tributario del SII ahora nace REPLEGADO (estado radarOpen, default false): el hero muestra "N focos activos" + chevron y "toca para ver"; se despliega la lista al tocar. Con 151 focos ya no ocupa toda la pantalla. Verificado en demo.
- 2026-09-11 Inteligencia · panel "Socios · cara a cara" (comparativo financiero por abogado). Nueva tarjeta en el hub de Inteligencia que abre el cockpit con TODOS los KPIs por abogado, fuente única, atribucion respVenta (venta.responsible || abogado del cliente, unificada). Escritorio: tabla socios x KPIs agrupada en 4 bloques (Produccion, Clientes, Cobro y liquidez, Futuro y valor) con lider resaltado + columna Estudio. Movil: marcador Cristobal vs Erasmo (protagonista) con ganador por metrica en verde + Martin aparte. ~20 KPIs (vendido, ticket, % recurrente, conversion, ventas por cliente, concentracion, facturado, cobrado, eficiencia de cobro, DSO, conversion a caja, por cobrar, vencido, MRR, comprometido 2027+, por facturar). Margen por hora y margen % marcados "requiere horas" (1 sola hora cargada en la base). Exportable a CSV. Notaria excluida (cifras repartidas en varios planos, sin confirmar). Verificado demo 1280 y 375, build verde.
- 2026-09-10 Ventas · REGLA "toda venta tiene abogado". El formulario de venta ahora EXIGE responsable al guardar: guard en `handleSave` que bloquea con aviso ("Toda venta debe tener un abogado responsable...") si no hay `responsible` — excepto subarriendo (no es venta de cartera; ya se excluye de Vendido/metas/desglose). El campo Responsable se marca con asterisco rojo y borde rojo mientras esté vacío. Auditoría de datos: 0 ventas reales sin abogado (la única sin responsible es el subarriendo de oficina, exento). Refuerza la consistencia del desglose "Por responsable" e "Ingreso por año" (que agrupan por s.responsible). Verificado en demo: auto-relleno del abogado del cliente + bloqueo al dejarlo vacío.
- 2026-09-10 Ventas · "Ingreso por año" ahora con años CLICKEABLES + proyección multi-año. Cada año del panel se expande y muestra sus cuotas (fecha DD-MM, cliente, estado canónico Cobrado/Vencido/Programada, monto). Proyección: se muestran TODOS los años hasta que termina la cobranza (todas las cuotas de las ventas no recurrentes); EXCEPCIÓN — las recurrentes (mensuales activas) se capan a un año = sus primeras 12 cuotas por vencimiento (`ingresoPorAnio` corre por venta y aplica `esRecurrente`->slice(0,12)), para no arrastrar un contrato indefinido a años futuros. Fuente única montoFactura/cobradoBill/estadoCobro. Verificado en demo 1280 y 375 (Erasmo: 2026 con 7 cuotas, Cobrado verde / Vencido rojo / Programada gris).
- 2026-09-10 Ventas · "Ingreso por año" en el drill de Por responsable. Al elegir un abogado en el desglose, el panel muestra una tarjeta compacta que distribuye las cuotas (emitidas + programadas) de sus ventas por AÑO de vencimiento — cuándo entra la plata — con lo cobrado en verde por año y total. Fuente única montoFactura/cobradoBill (no duplica fórmula); `billing` pasado como prop a SalesView. Lente distinto a "vendido" (valor del contrato): el vendido de Cristóbal ($289M) difiere de su ingreso por cuotas ($219M) porque son ejes distintos, no un descuadre. Build verde + verificado en demo escritorio 1280 y móvil 375.
- 2026-09-10 Cuatro mejoras: (1) Dashboard hero Vendido/Cobrado PARTIDO en Bruto|Neto con % de meta (kpiTileSplit), color por tarjeta. (2) "Mi cartera" para limitados (MiCarteraView, Alt A): sus proyectos por dias sin mover + Registrar avance. (3) Gastos "Sin cliente": el asistente IA ahora SUGIERE SOLO al abrir la seccion (useEffect, una vez, !DEMO) para confirmar sin acordarse de correrlo. (4) NC anidada pulida en la ficha (fila ambar con -monto + "saldo $0") y la 1a tarea de Activar propuesta pasa a "Solicitar fondos al cliente" enlazada al modulo de hoy. Build verde + verificado en demo (dashboard desktop/movil, Mi cartera Vista Team, Gastos sin crash).
- 2026-09-10 Correos · logo INLINE (CID) para que se vea siempre. Los correos del estudio llevaban el logo como imagen REMOTA (<img src="https://gestion.leabogados.cl/le-logo-*.png">), y Gmail (y la mayoria de clientes) bloquea las imagenes externas por defecto -> el header navy y la firma llegaban sin logo hasta que el cliente pulsaba "mostrar imagenes". Fix en el unico punto de envio: `incrustarLogosCorreo(html)` (App.jsx ~20128) descarga los logos same-origin, los pasa a base64 y reemplaza los src remotos por `cid:...`, entregandolos como inlineImages a `sendGmailWithPdf` (que ya soportaba CID). Aplica a TODOS los correos (facturas, fondos, recordatorios) porque todos usan el mismo cascaron facturaCorreoShell + firmaCorreoHtml. El preview en la app y el envio por SMTP de oficina siguen con la URL (sin regresion; notify-task no soporta inline, queda como follow-up). Verificado en el navegador: fetch->base64 (18.292 y 54.800 chars) + reemplazo a cid:lea-logo-blanco/color. Build verde.
- 2026-09-10 Recordatorio de cobro · rediseño completo (tono único amable + flexibilidad + PDF adjunto). `recordatorioCobro(bs)` (App.jsx ~18269) pasa a FUENTE ÚNICA que acepta una factura o un arreglo (correo COMBINADO por cliente): tono ÚNICO amable (Opción C aprobada, SIN rojos) — "hacer seguimiento… puede tratarse de un olvido / Les agradeceremos gestionar su pago" — con el cascarón de las facturas `facturaCorreoShell` (header navy con logo + pie de firma), firmado **Administración**, enviado desde la cuenta del usuario. Nuevo `RecordatorioModal` en Cobranza: selección de facturas (vencidas marcadas por defecto, al día seleccionables), toggle un-solo-correo-con-todas vs uno-por-factura, toggle adjuntar PDF (ON), y **vista previa del correo SIEMPRE visible y desplegable**. Nuevo helper `facturaPdfAdjunto(b)` re-genera el PDF con timbre desde el DTE y se adjunta por defecto en TODOS los flujos de recordatorio (Cobranza, Facturación, tanda). Nueva REGLA DE ORO: por definición no existen facturas sin dte_xml. Build verde + verificado en demo móvil 375 y escritorio 1280 (modal monta, texto/firma/datos correctos, envío registra cadencia).
- 2026-09-10 Revisión de caja chica · Fase 2c (barrido diario server-side). Edge function `caja-chica-sweep` (supabase/functions/) + cron diario (job 15, 45 12 * * *): enlaza sola las cargas de caja chica de Martín/Martina SIN enlace con la transferencia que calza (mismo RUT vía EQUIPO_RUT, monto EXACTO, ≤3 días); solo el calce ÚNICO+EXACTO+en-ventana, lo ambiguo queda para la compuerta in-app. Patrón cobranza-auto: esCron por secret, dry-run con JWT @leabogados.cl, gate por learnings config `caja_chica_auto` (INERTE hasta prender 'Automática' en el panel). Robusto a tildes (NFD: user_name 'Martín' calza aunque el mapa esté sin tilde). Probado en dry-run contra prod: 8 enlazables (Martina, mismo día y monto exacto) + 1 ambigua, sin escribir. Reversible. Cierra la Revisión de caja chica (Fases 1+2+2c).
- 2026-09-10 Revisión de caja chica · Fase 1 (capa de datos). Nueva columna real `petty_cash.movimiento_id` (uuid) con índice único parcial = enlace 1:1 transferencia↔caja chica (garantía dura de no-duplicación); backfill del hack previo `notes: · mov:{id}` (3 enlazadas migradas). `cajaChicaMatch`/`abonoCajaChica`/`deshacerCajaChica` (App.jsx ~27050) pasan a usar `movimiento_id` (además del texto); al crear caja chica desde el banco se guarda `delivered_at`=fecha de la transferencia + el enlace real. Nueva REGLA DE ORO en CLAUDE.md (caja chica ↔ transferencia, ventana ±3 días, admin-only, sueldo excluido). Fase 2 (panel 'Revisión de caja chica' en Conciliación + barrido diario con cron + compuerta) pendiente. Build verde.
- 2026-09-09 Oficina · rediseño a hub BENTO con resultado DEVENGADO y caja viva. Tras aprobar el prototipo (artefactos), `OficinaHub` (App.jsx:7134) pasa del strip+6 tarjetas a un **bento** de 3 tamaños: protagonista navy = **Resultado del mes DEVENGADO** (Facturado SII − costos − comisiones, cifra en blanco), Caja hoy (teal) + Flujo del mes de contexto, y 3 puertas (Costos, Retiros, Gastos varios) + tira de 4 KPIs del año. **Cambio de cifra (confirmado): el ingreso protagonista es el DEVENGADO** (facturas con `dte_xml` = el facturado real del SII), no el cobrado a caja. Nuevo helper único module-level `facturadoSiiPeriodo(billing, ym)` (ym de 7 = mes, de 4 = año), reusado por el hub y por `EstadoResultadosModal` (cuya línea superior pasa de "Ingresos (cobrado)" a "Facturado (DTE del SII)"; export y notas al pie actualizados). **Caja viva:** config `learnings` kind=config key=`caja_ancla` = `2026-09-09:44148153` (insertada en prod); el hub lee el ancla y le suma Σ(abonos−cargos) de `cartola_movimientos` posteriores a la fecha del ancla (ambas cuentas), y `FlujoCajaModal` recibe `saldoInicial` (ya no arranca en 0). `presupuestoOficina` registrado en `VIEW_MODULO` ('finanzas'). Reutiliza los 4 modales existentes (Costos/EstadoResultados/FlujoCaja/Retiros), sin lógica en paralelo. Build verde + verificado en demo **desktop 1280 y móvil 375** (bento colapsa bien, Estado de resultados abre mostrando "Facturado (DTE del SII)", ancla de caja $44.148.153 visible).
- 2026-09-09 Cobranza · razón social por factura. `CobranzaView` recibe `clientEntities` y muestra la RS como protagonista de cada fila (cuando el cliente tiene más de una RS o difiere de su nombre); el folio pasa a subtítulo. Nuevo helper único module-level `rsDeFactura(b, clientEntities)` (entity_id → cruce receptor_rut → receptor_name → única RS), extraído de `ChecklistFacturacion` (que ahora delega) para no duplicar. Al tocar la factura ya abría su detalle completo (BillingForm). Build verde + verificado demo.
- 2026-09-09 Carga de cartola · dedup ROBUSTO por conteo (arregla "todo aparece como nuevo"). El banco BICE entrega la cartola MENSUAL con distinto n° de operación y con la glosa cortada por columna (inserta espacios: "Rut"→"R ut", "Chile"→"Chil e"; a veces trunca distinto) respecto de la cartola DIARIA ya cargada → el hash no calzaba y marcaba 47/48 como "nuevos" (riesgo de duplicar todo el mes). Fix en `onFiles` (~App.jsx:25872): el dedup ya no usa el hash sino la clave estable **cuenta|fecha|tipo|monto** comparada por **multiset/conteo** (montos y fechas sí parsean bien — los totales cuadran); soporta 2 movimientos idénticos el mismo día (ej. dos abonos de $5.000.000 el 19-08 de clientes distintos). Validado con el parser real (src/cartola.js) + las claves reales de prod: la cartola mensual de agosto da **42 ya cargados / 6 nuevos** (los 3 días del hueco BICE 27-28-31: TGR + 4 sueldos + el $5M de Kemeny a la Factura 377). Build verde.
- 2026-09-09 Alerta de cartolas faltantes (3 fases). Nace de que BICE se saltó las cartolas del 27, 28 y 31-ago y nadie se enteró. **Tablas nuevas** (corridas en prod): `cartola_cargas` (registro de cada cartola ingresada: rango + n° movimientos + origen; fuente de verdad "llegó cartola de este día", aunque venga vacía) y `feriados` (fecha/país configurable, sembrada con feriados CL 2025-26). **Fase 1 (app):** `CartolaGapCard` en el hub de Conciliación (móvil+escritorio) — tarjeta amarilla clara (~330px, no franja) con los días hábiles sin cartola; roja a los 3+ días; botones "Cargar cartola" y "Sin movimientos" (este marca cobertura insertando `cartola_cargas` con 0 movimientos → deja de avisar). Helper único `cartolaGapDias` reusa `_esDiaHabil` (feriados ya cargados de API). Verificado en demo (muestra "Faltan 2 cartolas · última 4 sep · sin 7, 8 sep", saltó el finde). **Fase 2 (edge):** `procesar-cartola` v8 escribe una fila en `cartola_cargas` en cada ingesta diaria (best-effort, no rompe la carga). **Fase 3 (edge):** nueva `alerta-cartola` (cron diario) — computa los días hábiles no feriados sin cartola y envía correo a los socios (cl@, ee@) desde `contacto@leabogados.cl`, firma la oficina. **Envío APAGADO por defecto** (dry-run salvo secreto `ALERTA_CARTOLA_ON=on`); falta setear secreto + programar el cron para activarlo. verify_jwt=false + secreto compartido.
- 2026-09-09 Facturación · corrimiento automático de cuotas no facturadas + tile Oficina en Inicio. (1) Nuevo helper único module-level `rolledDueMap(billing, hoy)`: una cuota Programada (sin folio) cuyo mes de devengo ya pasó sin emitirse RUEDA sola al mes de facturación en curso, preservando el día pactado; dentro de una misma venta NO puede haber dos el mismo mes (cascada: la de atrás empuja a la siguiente, salta meses ya ocupados por emitidas). Cálculo PURO, no muta la base — la fecha real se estampa recién al emitir. (2) ChecklistFacturacion: `items` filtra por vencimiento EFECTIVO (rodado) → las atrasadas aparecen en el mes en curso; nueva sección "Vienen de meses anteriores · N" (traza "vence oct (era jul)") separada de "De este mes". (3) `handleEmitirProgramada`: la fecha de emisión es SIEMPRE la real (hoy, no la planificada pre-cargada), y el vencimiento negociado pospuesto se estampa rodado (rolledDueMap). Cuota estándar sigue con dueFromIssued. Proyección de fin de año sigue usando la fecha planificada (sin tocar cifras de prod). Motivado por Survías (2 cuotas 50% que se posponen un mes). Build verde + verificado demo (sección "Vienen de meses anteriores" con traza correcta, sin crash). (4) Inicio: el tile "Presupuesto Oficina" pasa a llamarse "Oficina" (una sola puerta al módulo) y se elimina el tile duplicado "Costos de oficina" de la fila de Mis proyectos.
- 2026-09-09 Revisión de datos · detector "Facturas posiblemente duplicadas" con cruce REAL (RevisionDatosModal, App.jsx ~7360). Agrupa por identidad del receptor **RUT > razón social > cliente** + concepto + monto, EXCLUYENDO las anuladas por nota de crédito (status Anulada / billing_type nota_credito/reembolso). Así dos sociedades distintas con el mismo monto (ej. Tarragona factura la misma cuota a 3 RS con RUT distinto; Javier Bórquez folios de Producciones Maso vs Mauna Kea) YA NO se marcan como duplicado; solo la MISMA entidad con la misma cuota repetida. Valida contra prod: marca 4 grupos reales (Golf "Julio" ×2, Survías opinión tributaria ×2, Majmud "3-6" ×2, V.S.G. honorarios ×2), cero falsos positivos. Cada fila muestra folio/estado/vencimiento y abre la factura. Build verde + verificado demo (panel abre sin crash). Motivado por: las revisiones deben cruzar por RUT+RS+folio+NC, no solo por nombre de cliente.
- 2026-09-09 Oficina fase 3 · "Retiros vs utilidad distribuible" en RetirosOficinaModal. Nueva sección (3 barras: Margen acumulado / Ya retirado / Disponible) que controla que los retiros no superen la utilidad real del año. Margen YTD = Σ de cada mes (ingresosMesBill − costosOficinaMes − comisionMesTerc) → misma fuente única que Estado de resultados/hub, sin duplicar fórmula. Guard margenYTD>0; alerta roja si disponible<0 (se retiró más que el margen). RetirosOficinaModal recibe billing/terceros/costosOfiRows. Build verde + verificado demo (abre sin crash).
- 2026-09-09 Oficina · consolidación (elimina duplicados para una vista concentrada). (A) La ficha del cliente interno en Gastos ya NO muestra el `OficinaCostPanel` (presupuesto/real por categoría, que duplicaba la tarjeta Costos fijos) → banner que lleva al módulo Oficina + la lista de gastos crudos queda debajo. (B) El tile "Costos de oficina" del Inicio ahora abre el **módulo Oficina** (`onOpenCostosOfi`) en vez de la ficha estructural — una sola puerta. (C) La tarjeta "Varios y personales" del módulo abre la vista "Gastos oficina" (varios + personales por cobrar) vía nuevo gatillo `openGastosOfi` (espejo de `openOfi`, efecto en ExpensesView: cierra hub + setShowGastosOficina); se quitó el tile "Gastos oficina" del hub de Gastos (única puerta desde Oficina). Además fase 2: franja "Resultado del mes" en el hub reusando `ingresosMesBill`/`comisionMesTerc` (helpers de módulo extraídos de EstadoResultadosModal → fuente única, sin duplicar fórmula). Build verde + verificado demo (hub + franja + Varios→Gastos oficina sin crash).
- 2026-09-09 Oficina · módulo unificado con ámbitos en tarjetas (antes "Presupuesto Oficina"). El tab `presupuestoOficina` ahora renderiza `OficinaHub` (App.jsx ~6988): 6 tarjetas limpias (1 cifra + 1 contexto, iconos SVG y colores de la app, sin emojis) — **Costos fijos** (presupuesto `costosOficinaMes` → abre CostosOficinaModal), **Retiros a socios** (nuevo: `retirosOficinaData` agrupa la categoría 'Retiros' del cliente interno por socio vía RUT en glosa → `RetirosOficinaModal` con equidad entre socios + movimientos; NO es costo), **Nómina** (Remuneraciones+Leyes del presupuesto), **Subarriendo** (es_ingreso), **Varios y personales** (gastos operativos + personales por cobrar → ficha oficina), **Estado de resultados** (→ EstadoResultadosModal). Fiabilidad: cada cifra de su fuente única, presupuesto ≠ real nunca se suman, Retiros fuera del costo. Nav renombrada a "Oficina". Build verde + verificado demo (hub + modales Costos/Retiros abren sin crash). Renders aprobados. Pendiente fase 2: franja Resultado del mes (necesita fuente única de cobrado/margen) + "retiros vs utilidad distribuible".
- 2026-09-09 Ficha cliente · limpieza de código muerto. Se eliminaron 357 líneas de JSX oculto (`<div style={{display:'none'}}>` en el Resumen de ClientFicha, App.jsx ~18558-18915): las secciones antiguas (Portal/Actividad/Ventas/Cobros/Gastos/Proyectos/SII + aside de contexto) que quedaron ocultas tras el rediseño Propuesta A — su contenido ya vive en las pestañas (Contacto/Ventas/Cartola) y en las tarjetas del Resumen. Build verde + verificado demo (Resumen y Ventas renderizan sin crash, sin pantalla negra).
- 2026-09-09 Seguridad · verificado que el relay `notify-task` YA está cerrado (edge fn v35 con `autorizar()`: rechaza anon key, exige sesión @leabogados.cl o service key + allowlist de orígenes). Backlog marcado done; no requería cambios.
- 2026-09-09 Conciliación · asistente para glosas BICE enmascaradas (identificar sin ocultar plata). BICE oculta al remitente en ciertas transferencias (solo muestra RUT/nombre/cuentas propias del estudio). El parser YA marca interno solo lo confiable ("entre cuentas propias" / "Liberona→Liberona"); NO auto-marca los enmascarados (correcto: hoy resultaron ser fondos de clientes). Se agregó en `abonoInlineAcc` (App.jsx ~27601, punto único de la acción por fila en "Por resolver", móvil+escritorio) un **badge/pista** para abonos sin remitente: (B) glosa `"Transf. a terceros … a cuenta propia, Liberona Escala"` → **"Pago de cliente · BICE ocultó al remitente"** (azul) — son confiablemente de cliente, se asignan por monto/factura con el "Es X ✓" que ya existe; (C) glosa `"de CLIENTE Rut propio → a BENEFICIARIO Rut propio"` (ambigua, puede ser traspaso interno gastos↔honorarios) → si el monto **calza un pago ya conciliado en la otra cuenta**, muestra **"¿Traspaso interno? calza pago de \<cliente\>"** (ámbar); si no, "Remitente oculto (BICE) · ¿interno o cliente?". No auto-clasifica (cero riesgo de ocultar ingresos), solo guía. Build verde, verificado demo (Por resolver renderiza sin crash; badge no aplica a glosas con remitente visible).
- 2026-09-08 Cobranza escritorio · colores + controles discretos. (1) Cabecera de la tabla `#003C50` (navy) con texto blanco (antes gris `C.bgSoft` con texto muted); (2) fila de cada cliente: **blanca por defecto**, gris `C.bgSoft` al hover (invertido a pedido — antes era gris de página con hover blanco); (3) panel que se abre al expandir un cliente: fondo `#FFFFFF` (antes `C.bgSoft`). Además, a pedido: las dos tarjetas protagónicas sobre la tabla ('Cobranza automática' toggle + botón 'Enviar a todos los que toca') se **degradaron a controles discretos** en la fila 'Ordenar' (derecha): link `Enviar a todos (N)` + mini-switch `Automática` — se elimina el protagonismo sin perder la feature. Compartido móvil+escritorio. App.jsx CobranzaView ~24043/24055/24065/24072. Build verde, verificado demo 1280+375.
- 2026-09-08 Facturación · (1) BM Soluciones 'Asesoría permanente' ajustada a 20 UF/mes: venta 35→20 UF + 6 cuotas Programadas jul–dic 2026 $1.426.915→$815.380 (jul+ago quedan listas para emitir este mes, 2 facturas de 20 UF); factura 239 ene–jun (Pagada) intacta. Datos SQL, verificado. (2) Excel de emisión (Facturar_{mes}.xlsx, descargarExcel ~6294): fuera columna 'Monto guardado ($)', agregada columna 'Cuota' = N/total de la cuota en su venta (ej. 4/10). Build verde.
- 2026-09-08 Ficha cliente · rediseño coherente Propuesta A (sin repetir · ancho unificado · liviano). Análisis del código detectó repetición fuerte entre pestañas (Fondos 3×, Por cobrar 4×, RS/Ventas/Anticipos 2×, y DOS "Cobrado" con números distintos) + anchos distintos por pestaña. Cambios: (1) **Resumen liviano** = 5 tarjetas KPI **individuales e iguales** (Vendido·Facturado·Cobrado·Por cobrar·Fondos, grid repeat(5,1fr) desktop / repeat(2,1fr) móvil, cifras 1 décima, clickeables a su pestaña) + 2 tarjetas alineadas del mismo alto: **Vencidas** (facturas vencidas + "Cobrar → Ventas") y **Próximas tareas** (+ "Nueva tarea"). Se OCULTARON las secciones antiguas del Resumen (Portal/Actividad/Ventas/Cobros/Gastos/Proyectos/aside) via wrapper display:none — su contenido vive en las pestañas; sin borrar lógica (limpieza posterior). Fuera el panel lateral (Contacto ya está en su pestaña, Responsable junto al nombre). (2) **Ancho unificado a 900** centrado en las 4 pestañas (antes Resumen full / Contacto 820 / Ventas·Cartola 1040) → ya no salta. (3) **Ventas** sin la tira de KPIs (duplicaba el embudo) → Proyectos + Facturas + Anticipos. App.jsx ClientFicha ~18503 + tabs maxWidth. Build verde, verificado demo 1280+375. Render Propuesta A aprobado.
- 2026-09-08 Ficha cliente · Resumen pulido (embudo + Fondos, sin duplicar cifras). Se reemplazó la tira de 4 KPIs (Vendido/Por cobrar/Cobrado/Saldo fondos) que DUPLICABA los montos de las secciones, por un **embudo del negocio** Vendido→Facturado→Cobrado→Por cobrar + tile **Fondos** aparte (plata del cliente, no del flujo). Cifras a **una décima** (`fmtM1`/`ufM1`: $65,7M, UF 3.302,1); **sin flechas**; cada cuadro **clickeable** → su pestaña (Vendido/Por cobrar→Ventas, Facturado/Cobrado/Fondos→Cartola); rótulo de tiempo ("del año" vs "saldo vivo"/"N vencidas"). Secciones del Resumen **replegadas** por defecto (el embudo ya da el resumen; se expanden al tocar) — se revirtió el auto-expandir de openRS/openSaleGrp/rSec. "Cobros pendientes" ahora muestra **conteo** (N facturas) en vez del monto (que ya está en el embudo). Coherente con el embudo de Dashboard/Facturación y las secciones-ícono de Contacto/Cartola. Móvil: embudo en 2×2 + Fondos full-width, secciones colapsadas. App.jsx ClientFicha ~18460/18502. Build verde, verificado demo 1280+375. Aprobado por render (Propuesta 1 refinada).
- 2026-09-08 Clientes desktop · rediseño lista + ficha full-screen (aprobado). (1) La lista de Clientes en escritorio ya NO es maestro-detalle (lista 336px + ficha al lado): al elegir un cliente, su ficha ocupa **toda la pantalla** (como Colaboradores). (2) Sin selección, la lista es un **directorio alfabético multicolumna** (`ClientsView` rama isDesktop ~18968): agrupado por inicial, con `columns:'250px'` → más ancho = más columnas (así se ven muchos más clientes que la tira vertical); cada fila = nombre + badge de tareas + saldo en rojo; buscador + filtros (estado/responsable) arriba. (3) **Ficha desktop** (`ClientFicha`): `useIsDesktop`; KPIs a lo ancho (grid repeat(4,1fr) en desktop vs 2×2 móvil); el cuerpo del Resumen se envuelve en 2 columnas (secciones a la izquierda | **aside** de contexto a la derecha: Contacto, Razones sociales, Responsable, sticky) solo en desktop. **Móvil idéntico** (wrapper sin estilo → columna simple, sin aside). Verificado en demo a 1280px y 375px, build verde. Basado en renders aprobados (ficha opción A pulida + lista opción C directorio).
- 2026-09-08 Cobranza · fila de factura pulida (opción A) + clic en nombre. `detItem` (App.jsx ~23937): se quitó la línea de color a la izquierda (ya no se usa) y el gradiente; ahora la fila es blanca con el color de mora en el día grande (emisión) y en "Vencida N días". Fechas destacadas (antes gris claro 9px): dos columnas **Emitida** (fecha, oscuro) + **Vencida N días** (color por severidad) / **Vence en N días** (verde, al día) — se eliminó la fecha de vencimiento por redundante con los días. Toda la fila abre la factura (con su "Volver"); se quitó el botón "Abrir factura" (redundante) y quedó solo el link "Buscar pago ›" bajo el saldo → menos alto. Orden por defecto DENTRO del cliente: antigua (arriba) → nueva (abajo). Clic en el NOMBRE del cliente ya no salta a la ficha (era incómodo): el nombre/fila expande sus facturas y la ficha queda en un link aparte "Ver ficha ›" (móvil: en la meta; escritorio: en el encabezado del cliente expandido). Móvil + escritorio verificados en demo, build verde.
- 2026-09-08 Dashboard "Resultado del año" · drill pulido (coherencia tamaño/color, opción A). Los 4 paneles (Vendido/Facturado/Cobrado/Margen) tenían filas de tamaños dispares (las base heredaban ~14px vs 12,5px de las de año), colores de cifra sin criterio (Cobrado verde, Facturado navy) y héroe solo en 2 de 4. Ahora comparten UN ADN de fila (App.jsx ~3230): etiqueta `rowL` (12,5px·600·gris), cifra `rowV` (13px·800·tabular) con color SOLO por rol — navy=neutro/subtotal, verde=neto (`vPos`), rojo=resta (`vNeg`); héroe único `Hero({v,l,val})` arriba de cada panel (verde=neto, navy=total, rojo=pérdida); banda de meta unificada `metaBand()`; año protagonista `lbYr`. Facturado y Margen ganaron su héroe (antes no tenían); se quitaron los fondos rojos de las restas (comisiones/costos ahora son fila normal con cifra roja). Cifras de año/cliente ya no cambian de color entre Cobrado y Facturado (ambas navy). Verificado en demo (móvil 375 + escritorio): Vendido/Facturado/Cobrado montan con su héroe y filas coherentes; build verde. Render de referencia (opción A) aprobado por el usuario.
- 2026-09-08 Colaboradores · fusión de entidades duplicadas de BDA: `272d3b8d` (RUT sin puntos) era huérfano puro (0 refs) → eliminado con guardas; queda solo `ee5967b1` (RUT 78.099.865-2). Hecho por SQL directo (sin botón de un solo uso).
- 2026-09-08 Colaboradores 2ª fase · re-rolar BDA + ficha con las 2 caras. Schema: `clients.is_colaborador` + `proveedores.client_id` (migración colaborador_role_and_proveedor_client_link). BDA (68f398b0) marcado is_colaborador; proveedor Rodrigo (53a8f028) ligado al cliente BDA. **Clientes**: la lista (`base`) y los contadores activeN/endedN/prospectoN excluyen `is_colaborador` → BDA sale de Clientes pero su ficha sigue accesible por cross-link; sus facturas de honorarios (comisiones que le cobramos) SIGUEN contando en Facturado/Cobrado (son ingreso real, NO se excluyen — solo el subarriendo se excluye). **Ficha del colaborador** (ProveedoresModal ~11225): cuando el proveedor está ligado a un cliente muestra las dos caras — INGRESO (tarjeta Subarrendamiento → vista detalle + "Comisiones que nos paga" = facturas honorarios del cliente: 78/234/429/113) y EGRESO (reparto existente, ahora rotulado "Comisiones que le pagamos"). Folio 429 ($90.000) ligado a la venta "Comisiones". Build verde, ficha monta en demo sin crash. Queda para otra sesión (chip): fusionar 2 client_entities duplicadas de BDA (RUT con/sin puntos).
- 2026-09-08 Colaboradores + Subarrendamiento (etapa código + venta). (1) Módulo "Comisiones" → **"Colaboradores"** (título del modal ProveedoresModal ~11038 + botones en Clientes ~18826/18882). (2) **Marcador subarriendo**: helper de módulo `esSubarriendo(s)` = `cobro_config.subarriendo===true` (App.jsx ~2451, junto a esRecurrente; el `area` de sales es un enum acotado con CHECK, no se usa). (3) **Exclusión de Vendido/metas/áreas** (single-point por gate): se agregó `&& !esSubarriendo(s)` en los ~10 orígenes que suman ventas — VentasPorMes 2339, salesYr 2662, metricasAnio 2748, aniosDisponibles 2956, ventasReales 3905 (Inteligencia), SalesView filtered/yearSales/grupos 4370/4377/4429, clientSales 18288, IntelligenceView período 20568, tarjeta cliente past 5393. (4) **Exclusión de Cobrado/Ingresado a caja**: en ingresosPorAnioVenta se saltan abonos categoria='Subarriendo' (loop de conciliaciones + porIdentificar) → el arriendo NO cuenta como honorarios cobrados. Facturado ya lo excluía (filtra billing_type='honorarios'). (5) **Venta creada** (id 42cfeac6): BDA (68f398b0), title 'Subarrendamiento oficina', cobro_type='mensual', $1.080.000/mes CLP, status Activo, año 2026 mes 3, entity BDA, area NULL, cobro_config.subarriendo=true — excluida de Vendido por construcción (verificado: build verde + Dashboard/Ventas montan, Vendido demo intacto UF 12.358). PENDIENTE (requiere OK de cifras): importar folios SII 321 ($2.160.000 Mar+Abr) y 113 ($70.650), programar cuotas Jun–Sep + futuras, conciliar los 6 abonos, re-rolar BDA a colaborador, UI del panel subarriendo.
- 2026-09-08 Conciliación · Categoría de abono "Subarriendo" (ingreso por arriendo, NO honorarios/venta). Agregada a CATS_ABONO + RESUELTAS_ABO + TAG_STY (teal) en el modelo de Conciliación (App.jsx ~25738). Datos: 6 abonos de $1.080.000 (mar–sep 2026) de Rodrigo Díaz por el subarriendo, que estaban 'Sin identificar' (glosa enmascarada 'Transf. a terceros a cuenta 1403834'), marcados categoria='Subarriendo' + cliente_id=BDA + estado conciliado. Contexto Rodrigo: (1) honorario compartido con Rodrigo/proveedores = reparto en terceros_pagos (YA resuelto, Rodrigo tiene $732.610 en reparto); (2) subarriendo = esta categoría; NO existe factura de subarriendo en el sistema (solo abonos). 323 Pablo Demaría reclasificada a reembolso. Vittorio 94/104 conciliadas (pagado con respaldo). Facturado del año = honorarios EMITIDOS en el año (definición fijada). Build verde.
- 2026-09-08 Facturado drill · usa sale_year (huérfanas sin sale_id). facturadoByYear atribuía el año SOLO por sale_id → 88 facturas emitidas en 2026 sin venta enlazada (pero CON sale_year resuelto a mano) caían a 'sin venta asociada' ($23M). Ahora prioriza sale_id→sales.year, luego b.sale_year, luego año del vencimiento (espeja anioVentaDe de Cobrado, que ya lo hacía). Resultado: Facturado 2026 ($242,1M emitido) se reparte De ventas 2026 $185,8M · 2025 $54,9M · 2024 $1,4M; cero 'sin venta'. Solo cambia la atribución de año en el drill; el total y el criterio (honorarios emitidos en el año) no cambian. Ligar sale_id real de cada huérfana queda pendiente (opcional, no afecta la cifra; muchas son ad-hoc sin venta puntual). App.jsx:2846. Build verde.
- 2026-09-08 Dashboard "Resultado del año" v2 (4 ajustes pedidos). (1) **CRITERIO Facturado afinado**: pasó de "por año de venta" a **HONORARIOS EMITIDOS en el año** (por issued_at) → captura TODAS las facturas emitidas en el año, incluidas las de ventas de años anteriores ($54,3M de ventas 2025 que antes se perdían) y las sin venta asociada ($23,1M); EXCLUYE facturación de gastos (billing_type='reembolso', solo honorarios). `facturadoYr`/`facturadoByYear` reescritos (emisión, drill por año de venta 2026/2025/sin venta). Efecto: RESUELVE el "cobrado > facturado" (ahora Facturado emitido $242M ≥ Cobrado a caja $205M; embudo Vendido $373M ≥ Facturado ≥ Cobrado, monótono). (2) **Tarjetas escritorio arregladas**: el texto del % ya no se salía; ahora icono a la izquierda (coherente con los indicadores) + título/cifra/"% de la meta" alineados en columna, el % bajo la cifra en minúsculas sin negrita. Móvil intacto. (3) **Informe (PNG + impreso) rediseñado**: el peso va DEBAJO de la UF (no al lado) en héroes y detalle; Facturado con desglose "emitido en el año, según año de la venta". (4) **Cobranza con color por severidad de mora** (franja izquierda + tinte): verde al día · ámbar <30 d · rojo ≥30 d, mismo criterio que abonos/cargos (detItem, compartido móvil/escritorio). Auditoría Geslog OK (11 facturas, vencido $17.810.610, total $20.991.911, mora 115 d — cuadran exacto). App.jsx: facturadoYr/ByYear ~2836, tarjetas ~3182, informe ~2862/2934, detItem ~23780. Build verde, verificado en demo (escritorio sin desborde, iconos OK). PENDIENTE de revisar por el usuario: Factura 323 (Pablo Demaría "Reembolso") tipada honorarios; Itaim/Intermec quedan neteados (no se tocan).
- 2026-09-08 Conciliación · Traspaso interno vs pago de tercero: LA GLOSA MANDA. BICE no muestra el nombre de quien nos paga; cuando el pago viene de otra cuenta BICE la glosa queda como "Transferencia de CLIENTE Rut 77.700.387-9 ... a BENEFICIARIO Rut 77.700.387-9" (nuestro propio RUT a ambos lados), casi idéntica a un traspaso entre nuestras cuentas → el detector, que emparejaba solo por monto+fecha+cuenta distinta (±2 días, par único), podía marcar interno un pago de tercero. Fix (App.jsx conciliarAuto ~27064): (1) helper `_glosaInterna` = interno solo si el banco lo declara "entre cuentas propias" o el REMITENTE es el propio estudio ("de LIBERONA ESCALA ABOGADOS ... a LIBERONA ESCALA ABOGADOS" / "de QUAD ASES", RUT 77.700.387-9) — anclado en el remitente, porque "... a LIBERONA ESCALA" aparece en TODO pago entrante y no distingue; (2) helper `_glosaTercero` = nunca emparejar como interno una pata con glosa de tercero ("Abono por transferencia de <nombre>", "Transf. a terceros", el enmascarado "de CLIENTE.../BENEFICIARIO", o "Depósito"); (3) glosa propia explícita → interno directo sin necesidad de par; el par por monto queda solo como respaldo para glosas neutras. Auditoría de datos (SQL): 0 falsos negativos (los 99 internos verdaderos ya estaban marcados); 4 movimientos (2 pares Itaim/Intermec, depósito+salida de terceros nombrados, ya conciliados a su cliente) quedan marcados interno con glosa de tercero — se dejan para revisión manual (no se tocan: cambiar es_interno afecta registros conciliados). NOTA de análisis: un primer patrón mío ("...a LIBERONA ESCALA") era erróneo y habría marcado interno pagos reales de clientes (Jasna Misetic) y reembolsos del socio; se descartó tras verificar contra los datos (regla: cuestionar el propio análisis). Build verde, Conciliación monta OK en demo.
- 2026-09-08 Dashboard "Resultado del año" · REDISEÑO completo (tras varias rondas de render aprobadas). (1) **Tarjetas** (Vendido/Facturado/Cobrado/Margen) unificadas: cifra "UF X" como un solo texto de 22px + cifra y % en azul oscuro `C.accent`, textos en gris, color solo en el recuadro; layout en una fila (cifra izq, % der con rótulo de su denominador debajo: "de la meta"/"de lo vendido"/"de la meta"/"rentabilidad"); Cobrado headline = total a caja (calza con % de meta cobranza); Margen ahora es tarjeta clickeable (antes abría estado de resultados directo). (2) **Panel por tarjeta (clic 1 resumen → clic 2 año → clic 3 cliente)**: Cobrado/Vendido muestran **neto de comisiones** (hero) + chips Bruto/Comisiones; el desglose "según año de la venta" lista los cobros por **fecha de cobro** (día grande), con control de **orden Fecha/Monto** y "Ver las N"; cada fila abre la ficha; comisiones expandibles (los pagos del año); meta de cobranza anidada. (3) **Margen** = cascada **Cobrado − Comisiones pagadas − Costos de oficina** (base caja); `resultado` pasó a restar `comisPagadasAnioVenta.total` (antes solo restaba costos de oficina). (4) **Recuperado bruto/neto de comisiones** que se había perdido. (5) **Descargar imagen** (PNG) e **Imprimir informe** rediseñados a recuadros por sección (Ventas/Facturado/Cobrado/Margen/Cobranza), con **logo real** de la oficina, **fecha de corte destacada** y cifras en **UF y pesos**; el PNG carga el logo con timeout (no se cuelga). Una sola unidad en Inicio (toggle UF/$); el informe muestra ambas. App.jsx: tarjetas+panel ~3138, resultado ~3103, descargarResultado/imprimirResultado ~2855. Cifras reales al 8-sep: cobrado $203,6M (neto de comisiones $194,4M, comisiones pagadas $9,15M), margen $83,1M. Build verde, verificado en demo (móvil): tarjetas, panel, orden, cascada y ambas exportaciones OK.
- 2026-09-08 Dashboard "Resultado del año" · RECUPERADO lo que se había perdido al quitar el hero "Cómo va el año". (1) **Distinción por año de venta** (drill 2 niveles): tocar Facturado o Cobrado abre el desglose por año de venta (byYear) y cada año expande sus facturas/abonos (listByYear) con folio y monto, clickeables → ficha del cliente; cierra el descuadre visual "facturado < cobrado" (eran planos distintos: Facturado por año de venta vs Cobrado por año de depósito). El headline de Cobrado ahora es `ingresosPorAnioVenta.delAnio` (de ventas del año), con "Total a caja {año}" y "Meta de cobranza" anidados. (2) **Metas visibles como valor**, no solo %: Meta de ventas (drill Vendido) y Meta de cobranza (drill Cobrado) muestran su UF/CLP. (3) **Descargar imagen** (PNG branded, canvas propio → salida limpia, hoja de compartir en iPhone) e **Imprimir informe** (HTML → PDF con KPIs + cobrado/facturado por año) recuperados como botones en el header de la tarjeta. `facturadoByYear` useMemo (App.jsx:2838, montoFactura por año), `resDrill` usePersistedState, `descargarResultado`/`imprimirResultado` (2852/2897), header 3123, drill 3163. Sin emojis, paleta C, una línea. Verificado en demo (móvil 375): header sin topones, drill 2 niveles OK, ambas exportaciones sin error, build verde.
- 2026-09-07 SII/Cuadrar con SII · CORRECCIÓN criterio de candidatas: MÁS ANTIGUA sin pagar (FIFO), no el mes de emisión. El fix previo (match por mes) era incorrecto: las mensuales se emiten con desfase variable (mes vencido; ej. Perry "junio 26" se emitió en agosto), así que el mes de emisión NO indica la cuota. Ahora las candidatas se ordenan por período ascendente (vencimiento, o mes del concepto) y se propone por defecto la **más antigua sin pagar** (badge "Más antigua sin pagar", botón primario); el resto queda tras "Ver todas las cuotas (N)" — reduce scroll y evita cuadrar al mes equivocado. Coincide con la regla FIFO de conciliación (abono a la factura más antigua). App.jsx:7482 (orden), 7514 (render). Build verde.
- 2026-09-07 SII/Cuadrar con SII · Match por MES + Sincronizar persiste el espejo. (1) "Elige la factura correcta": antes las candidatas se rankeaban solo por cliente+monto → para mensuales de igual monto (ej. Perry $250.000) mostraba 5 cuotas (jul/sep/oct/nov/dic) todas "Monto exacto" y dejaba cuadrar al mes equivocado (pegar el folio de julio a la cuota de diciembre = descuadre). Ahora se rankea por MES reusando `mesScoreCandidata` (App.jsx:7126): usa la fecha de emisión del SII y calza contra el concepto/vencimiento−1 de cada cuota; si hay UNA que coincide el mes, se muestra solo esa (badge "Coincide el mes") y el resto queda tras "Ver otras N cuota(s) de otro mes"; si no hay calce claro, se listan todas con su mes rotulado (App.jsx:7482,7514). (2) `SiiSyncModal.sincronizar` ahora, además del cotejo en vivo, **persiste el mes en el espejo** (`sync-ventas-rcv` + `link_sii_billing` vía rpc) — antes solo cotejaba y el mes no quedaba guardado en `sii_cargas_docs`, dejando huecos (julio 2026 no estaba: el importador histórico llega a jun-2026 y el cron cubre mes anterior+actual). Read-only sobre el SII, idempotente, en try/catch (no rompe el cotejo si falla). Build verde.
- 2026-09-07 Facturación · FIX CRÍTICO pantalla negra: `billingIntent`/`setBillingIntent` se usaban (en `navTo` App.jsx:28587 y en `<BillingView intent={billingIntent}>` App.jsx:30474, más `onIntentDone`) pero **el `useState` nunca se declaró** (quedó pendiente del trabajo de navegación navTo/goBack). El build NO lo caza (variable no declarada = ReferenceError en runtime, no error de compilación); como `intent={billingIntent}` solo se lee al renderizar la pestaña `billing`, la App crasheaba **solo al abrir Facturación** ("Cannot access/`billingIntent` is not defined" → pantalla negra). Fix: agregado `const [billingIntent,setBillingIntent]=useState(null)` junto a los otros gatillos de navegación (App.jsx:28613). Verificado en demo: Facturación renderiza (Ciclo de la factura, Cierre de mes, Por socio, etc.), consola sin ReferenceError. (`carteraFocus`/`concFocus` sí estaban declarados; solo faltaba éste.)
- 2026-09-07 Facturación · Remediación + causa raíz: 16 facturas fantasma sin folio (duplicados). AUDITORÍA (contra fuentes de oro SII + banco): 19 "emitidas sin folio" ($15,4M) resultaron ser en su mayoría DUPLICADOS de facturas que ya existen CON folio (mismo cliente/mes/monto, gemelo en el SII). Origen: `insertarCuotas` (App.jsx:28831) regeneraba TODAS las cuotas de la venta al re-activarla (`_regenProg` 28861 / `_actualizarPago` 28868), y el único resguardo (28832) descontaba solo lo pagado en status `Pagado` → los meses ya EMITIDOS pero impagos (Pendiente/Vencido con folio) se recreaban como programadas sin folio, y luego pasaban a Pagado/Vencido = fantasmas. (a) DATA: soft-delete (`deleted_at`+`motivo_baja`) de los 16 duplicados verificados (0 conciliaciones/anticipos/NC/SII ligados; banco intacto), −$11.228.614 de "emitido" falso (−$5,25M cobrado sin respaldo, −$5,98M Vencido). Quedan 3 huecos reales sin folio (Daniel Abragan Mayo, Gloria Cheyre Testamento, Mobilitex) para decidir aparte. (b) FIX causa raíz: el resguardo de `insertarCuotas` ahora considera un cobro cubierto si ya existe factura de la venta **Pagada O emitida con folio** (invoice_no presente), no solo Pagada (App.jsx:28832-28841) → al re-activar/editar una venta ya no recrea meses ya facturados. `matchProgEmitidas` no servía de guard (ignora por diseño conceptos recurrentes mensual/permanente/cuota N/M). Build verde; el fix corre solo al guardar/activar venta (prueba real = próxima re-activación).
- 2026-09-07 Conciliación · BUG: la lista escondía los movimientos más antiguos. `lista` (abonos/cargos) estaba topada en `slice(0,400)` y, con orden por defecto nueva→antigua, con >400 movimientos de un tipo (hay 533 abonos / 536 cargos) los ~130 más antiguos NO se mostraban → la lista "terminaba" a mitad de agosto 2025 y no se veían feb–jul 2025. Fix: tope subido a 3000 (App.jsx:26888). Las filas de meses colapsados no se renderizan (solo con `_yOpen&&_mOpen`), así que el tope alto no pesa en móvil; los filtros de año/mes/búsqueda siguen para acotar. Build verde; Conciliación sin crash en demo.
- 2026-09-07 UI · Dingbat ✦ → ícono SVG (#9, cosmético). El glifo de texto `✦` (marcador "sugerido/aprendido/IA") se veía tosco y variaba según fuente/plataforma. Nuevo componente global `Sparkle` (App.jsx:194): SVG estrella de 4 puntas, `fill=currentColor` (hereda el color del texto/botón donde va) + `verticalAlign` para asentarse en línea. Reemplazados los ~16 usos de UI (asistente de gastos cliente/categoría aprendida, sugerencias por RUT, año de venta sugerido, venta sugerida, "usar" proyecto, "calza" factura, serie sugerida, "Redactar con IA", "Generar borrador", "aprendido", "IA", Gasto Oficina vía, "¿categoría?", "Pago en grupo"). Solo quedan `✦` en comentarios de código. Build verde; verificado en demo (SVG nítido, sin crash, sin glifo viejo).
- 2026-09-07 SII · Cuadre de ventas AUTOMÁTICO mensual (backlog #4: "el sync no persistía el RCV → cuadre a mano"). El motor de persistencia ya existía (edge `sii-sync` acción `sync-ventas-rcv` → espeja el RCV de ventas en `sii_cargas_docs` estado `rcv_historico`); faltaba automatizarlo y auto-enlazar. Nuevo: (a) función SQL `public.link_sii_billing()` = auto-enlace doc SII ↔ `billing` por **folio normalizado + RUT + monto, solo 1:1 inequívoco** (facturas t34 y NC t61); marca `sii_cargas_docs.estado='registrada'`+`billing_id` y `billing.sii_synced_at` (NO corrige receptores ni toca cifras de facturas ya conciliadas; idempotente). (b) acción edge `cron-cuadre-ventas` (cron-gated, agregada a `CRON_ACTIONS`): trae el RCV del **mes anterior + actual** (`getVentasFull`), lo persiste (ignoreDuplicates, read-only) y llama a `link_sii_billing`. (c) cron `cuadre-ventas-sii-mensual` (`0 13 5 * *`) que la invoca con `cronSecret`. Probado en prod: trajo 15 facturas de agosto que faltaban y las auto-enlazó (SII registradas → 371; quedan 3 `rcv_historico`: 113/321 BDA y 350 Dagoway, que no calzan 1:1). Reemplaza el cuadre manual que hicimos hoy. Deploy: `supabase functions deploy sii-sync`; SQL aplicado a prod.
- 2026-09-07 Conciliación/Facturación · Calce cercano por DIFERENCIA DE UF (#8, corrección de marco pedida por el usuario: las diferencias chicas NO son comisiones bancarias). CAUSA real: en una venta en UF la cuota se PROGRAMA con el `uf_value` de la venta (queda en `b.amount`) y se EMITE a la UF del día (queda en el DTE = `montoFactura`, `handleEmitirProgramada` NO toca `amount`, App.jsx:29973); el cliente paga el monto PROGRAMADO → el abono queda unos pesos menor que el saldo (el DTE) y ese faltante ES la variación de UF entre programación y emisión. Se **eliminó el umbral inventado** (2% / tope $10.000): la tolerancia se **calcula de la propia factura** → `gapUF(b)=montoFactura(DTE)−amount(programado)` (>0 solo con DTE real y DTE>programado → se auto-restringe a UF; en CLP/sin DTE da 0). Un abono del mismo RUT es calce cercano cuando el faltante observado (`saldo−abono`) **coincide con `gapUF`** (margen solo de redondeo, max $1.000/0,1%) — auto-validado, sin % global (App.jsx ~8530, `gapUF`/`esCalceCercano`/`calcesCercanos`). Wording: "DIFERENCIA DE UF · MISMO RUT · pagó lo programado · dif $X por UF"; panel "programado $A → emitido $B · Conciliar (queda saldada)"; confirm explica prog vs emitido. `conciliarCalceCercano` salda la factura (`onStatusChange…'Pagado'`, `paid_amount`=monto programado que llegó; `saldoBill`=0 con Pagado → sin saldo residual) y registra la diferencia como **diferencia de UF**. Con compuerta (appConfirm con detalle) y reversible. Móvil+escritorio. Demo: venta UF `s5` → factura `bcc` programada 300 UF×$39.000=$11.700.000, DTE a $39.500=$11.850.000, abono `mov8` $11.700.000 → dif $150.000 (1,3%), que el viejo tope $10.000 rechazaba mal. Build verde; verificado en demo (sugerencia + panel programado→emitido + confirm; cancelado sin escribir BD). Reemplaza el marco "±comisión" del commit 6a60757.
- 2026-09-07 Conciliación/Facturación · Calce cercano ±comisión (#8, render aprobado; toca dinero → SIEMPRE con compuerta). Cuando el banco retiene una comisión (Transbank/Webpay/transferencia con costo), el abono llega unos pesos MENOR que la factura → antes no calzaba exacto y no se sugería nada. Ahora, para una factura Pendiente/Vencida con un abono del **MISMO RUT** cuya diferencia es baja (**umbral = min(2% del saldo, $10.000)**, `UMBRAL_CALCE`/`esCalceCercano`/`calcesCercanos`, App.jsx ~8530), aparece una sugerencia teal **"CALCE CERCANO · MISMO RUT · dif ≈ comisión · Revisar"** (bloque colapsado, distinto del verde de calce exacto y del "mismo RUT sin conciliar"; se excluye del bloque genérico para no duplicar). Al "Revisar", la fila del abono muestra **"CALCE CERCANO · dif $X"** con "comisión ~$X" y el botón **"Conciliar (queda saldada)"** → `conciliarCalceCercano` (App.jsx ~8531) inserta la conciliación por el monto del abono (`marco_pago:true`), marca el movimiento conciliado y **salda la factura** (`onStatusChange…'Pagado'`, `paid_amount`=neto que llegó al banco; `saldoBill` devuelve 0 con status Pagado → sin saldo residual). La diferencia queda como comisión/ajuste, NO como saldo pendiente. **Nunca automático** (appConfirm despliega factura+abono+diferencia esperada), reversible (Deshacer conciliación). Móvil y escritorio (mismos bloques). Demo: factura `bcc` $250.000 (RUT 76.222.333-4) + abono `mov8` $246.811 → dif $3.189 (1,3%). Build verde; verificado en demo (sugerencia colapsada + panel "Conciliar (queda saldada)" con el detalle correcto). Queda pendiente #9 (dingbats ✦→SVG, el usuario pidió dejarlo).
- 2026-09-07 Ventas · "Por hora" en "Cambiar forma de cobro" de ventas ACTIVAS (#3, render aprobado). El modal "Cambiar forma de cobro" (modMode='cambiar') tenía 4 formatos (Mensual/Cuotas/Personalizada/Único); se agregó **Por hora** como 5.º (App.jsx:5690). Al elegirlo: editor **Tarifa (UF/h) + Tope + Por mes/Total** (reusa los estados `tarifaHoraUF/topeHoras/topePeriodo`) y **aviso ámbar** contextual "Se cancelan N cuotas programadas desde {vigencia} y la venta pasa a cobro por hora · las emitidas/pagadas no se tocan" (App.jsx:5733). `confirmAndSave` para 'hora': `nuevasCuotas=[]` → `onCambiarFormato` borra las programadas futuras (≥ vigDate) sin crear nuevas, y el `onSave` posterior fija `cobro_type:'hora'` + `cobro_config` (tarifa/tope) (App.jsx:4961). Reversible (cambiar a otro formato regenera cuotas). Build verde; verificado en demo (venta activa M&A → Cambiar formato → Por hora → tarifa/tope + aviso al fijar vigencia). Con esto cierra el #3 y los 3 puntos (Facturación electrónica→página, Nueva venta/propuesta→página, Cambiar cobro→Por hora).
- 2026-09-07 Ventas · Comisiones DESDE una fecha X (pieza 2 del rediseño; regla "editable + no retroactivo"). En el reparto a proveedores (`RepartoTerceros`, App.jsx:4437) cada comisión tiene ahora un campo **"Comisión desde"** (month; vacío = todas las cuotas, con fecha = "en adelante"). En la generación de `terceros_pagos` (`handleSaveSale`, App.jsx:28782) se **salta toda cuota con `due` anterior a `row.desde`** → la comisión solo corre desde ese mes; las cuotas anteriores (o ya emitidas/pagadas) no se tocan. El `desde` se **round-trippea sin columna nueva**: al reabrir la venta, se infiere del mes de la cuota MÁS ANTIGUA con comisión de ese proveedor (solo si no cubre todas las cuotas) — así re-guardar preserva el efecto (reconstrucción del reparto, App.jsx:4563). Permite actualizar una venta activa para agregar comisión desde un mes en adelante. Build verde; verificado en demo (Nueva venta con honorarios+costos+proveedor → aparece "Comisión desde · todas" y admite fijar el mes).
- 2026-09-07 Ventas · Activar propuesta con PREVIEW antes de reprogramar (pieza 3 del rediseño; regla "avisar antes"). El preview de cuotas del SaleForm (que existía como nota azul "se crearán N cobros") se convirtió en un **aviso** más claro: al activar una propuesta o editar propuesta/borrador (`_activandoPropuesta||propBorr`) se muestra **ámbar** "Se programará la facturación · revisa antes de guardar" con la lista de cuotas (fecha + monto, hasta 6 + "N más") y "Se reprograman N cuotas · las emitidas/pagadas no se tocan" (App.jsx:5396). La activación ya reusa el form editable → se puede **cambiar modalidad/cuotas/forma de cobro** y el inicio de cobro antes de aplicar con "Activar propuesta". Demo: propuesta `sp1` (cuotas, inicio oct-2026) para exhibirlo. Build verde; verificado en demo (Activar propuesta → forma de cobro editable + aviso ámbar con Cuota 1/4 oct … 4/4).
- 2026-09-07 Ventas · SaleForm rediseño a PÁGINA (render aprobado; sub-tramos). (a) **modal → página**: el modal de venta/propuesta ahora es `Modal fullscreen fsMaxWidth={600}` → pantalla completa en escritorio con "‹ Volver" (App.jsx:30298), ya no tarjeta centrada con backdrop. (b) **"Lectura con IA"**: el botón "Subir archivo" (que ya lee el PDF con IA y autocompleta cliente/área/forma de cobro, App.jsx:4688-4759) se renombró a "Lectura con IA" y se destacó (azulBg). (c) **Secciones desplegables (accordions)**: "Clasificación" (Área/Responsable + Estado/Año/Mes) y "Notas" pasaron a acordeones colapsados por defecto con resumen inline (estado `openSec`, App.jsx ~4537); Cliente y Honorarios/Cobro quedan abiertos. Estructura inline (no componente anidado) para no perder foco en inputs. Build verde; verificado en demo escritorio (página con ‹ Volver; Clasificación/Notas colapsan y despliegan; "Lectura con IA" visible). PENDIENTE del rediseño: activar-propuesta con preview + comisiones desde fecha X.
- 2026-09-07 Ventas · "Por hora" SIN presupuesto (corrección pedida por el usuario; parte del rediseño del form). ANTES: el selector de forma de cobro solo aparecía con honorarios > 0 → no se podía elegir "Por hora" sin ingresar un monto fijo. AHORA: (1) enlace **"¿Cobro por hora? Sin monto fijo →"** al pie del bloque Honorarios (App.jsx:5273) que setea `cobroType='hora'`; (2) al elegir Por hora se **oculta el bloque Honorarios/costos** (`formNuevo&&cobroType!=='hora'`, App.jsx:5240) — no pide presupuesto; (3) el selector de forma de cobro se muestra aunque no haya honorarios cuando es por hora (`totalCLP>0||cobroType==='hora'`, App.jsx:5278). Solo pide **Tarifa + Tope** (por mes/total); se factura por las horas (tabla `horas`), no genera cuotas. Reversible (elegir Cuotas/Mensual reabre Honorarios). `handleSave` no exige monto → guarda con amount vacío. Demo `s13` pasó a `amount_uf:null` (por hora = sin presupuesto; ya no infla "Vendido"). Build verde; verificado en demo (enlace visible sin honorarios; al elegir Por hora desaparece Honorarios y aparece Tarifa/Tope).
- 2026-09-07 Facturación · vencimiento ahora respeta FERIADOS de Chile (cierra el pendiente del "día hábil" estricto). El helper `ultimoDiaHabilMesEmision` (App.jsx ~297) ahora considera día hábil = ni fin de semana ni feriado. Los feriados se cargan al iniciar (`cargarFeriadosCL`, App.jsx:488) desde `https://api.boostr.cl/feriados/{año}.json` (fallback `apis.digital.gob.cl/fl/feriados/{año}`) para año-1/año/año+1 en el Set `FERIADOS_CL`; parser tolerante al formato (`date`/`fecha`, array directo o `.data`). Si la API no responde, cae a solo fin de semana (comportamiento anterior, seguro; no bloquea el arranque). Verificado: API responde (16 feriados 2026, CORS ok) y la lógica retrocede sobre feriados (test: mayo con 31 feriado → 28). Build verde.
- 2026-09-07 Horas · Rentabilidad: una lista con TABS (Por cliente / Por abogado) en vez de dos listas apiladas (pendiente UI del fin de semana; render aprobado). Antes: hero "Margen real" + lista "Margen por cliente" + sub-sección "Rentabilidad por abogado" apiladas (doble lista, vista larga). Ahora: el mismo hero + un **segmento Por cliente / Por abogado** (estado `margenTab`, App.jsx:23843) sobre UNA sola lista; `margenData` (cliente) y `margenAbogado` (abogado) se muestran según el tab, cada uno en grilla 2-col en escritorio. Mismas cifras (helpers `margenDeSale`/margenAbogado intactos), solo reorganización visual. Admin-only (`isAdmin && vista==='ytd'`) — los limited nunca ven Rentabilidad (confirmado). Build verde; verificado en demo (tab Por cliente: Comercial Andes/Tech Araucanía; tab Por abogado: Martín/Cristóbal/Martina). NOTA de permisos (consulta del usuario): la ficha de cliente para rol limited mantiene su comportamiento — ve "Financiero (Por cobrar/Facturado)" SOLO en los clientes de los que es responsable (App.jsx:920); el "Resumen" es saldo de caja chica (apropiado). El usuario decidió dejarlo como está (#6 sin cambios).
- 2026-09-07 Wording · unificar "Por cobrar" para las facturas del cliente (decisión del usuario: gana "Por cobrar", el término contable). Había inconsistencia: las facturas pendientes del cliente se llamaban "Por cobrar" en Inicio/Cobranza pero "Por pagar" en Facturación/Cierre (cambios de este mismo día). Se revirtieron a **"Por cobrar"** los 6 puntos firm-internos: `CIERRE_EST.sinpago` (App.jsx:7630), tarjeta de Cierre de mes (7750), tendencia Cierre/Facturación (7879), "pasan a Por cobrar" (9351), **Ciclo de la factura** del hub (9397: Por facturar → **Por cobrar** → Pagado), y el contexto de Cobranza ("Por cobrar total", 23493) + 2 comentarios. **NO se tocó** el "por pagar" que es plata que SALE (Comisiones/Proveedores/Oficina/Notaría/terceros — cuentas por pagar del estudio) ni el **portal del cliente** ("Tus facturas · Por pagar", que es la perspectiva correcta del cliente). Build verde; verificado en demo (Inicio "Por cobrar $4,7M" + "Comisiones $7,0M por pagar" coexisten; Facturación "Ciclo: Por facturar → Por cobrar → Pagado").
- 2026-09-07 Facturación · REGLA de vencimiento redefinida por el usuario (dinero — confirmada antes de tocar). ANTES: `due` al emitir = emisión + 30 días. AHORA: **vencimiento = último día hábil del MES DE EMISIÓN si la factura se emite dentro de los primeros 10 días; si se emite después del día 10, salta al último día hábil del mes SIGUIENTE.** Solo **nuevas emisiones** (no se recalcula lo ya emitido). Implementación: helper global `ultimoDiaHabilMesEmision(iso)` (App.jsx ~297) y `dueFromIssued` pasa a ser su alias (global + duplicado local 21118); maneja fin de semana (sáb/dom → viernes) — **feriados no contemplados** (no hay calendario en el código; anotado). Al emitir (`handleEmitirProgramada`, ~29858) el `due` se recalcula a la regla, EXCEPTO cuando es una fecha **negociada** (porcentaje/personalizada, que no cae en día 1) → esas se respetan; la programada sigue guardando día 1 para agrupar por mes. Verificado con casos (3-oct→30-oct vie; 15-oct→30-nov; 11-oct→30-nov; 20-dic→29-ene-2027 vie). Build verde. Memoria [[facturacion-vencimiento-cuotas]] actualizada (reemplaza la regla vieja de +30). PENDIENTE opcional: calendario de feriados para "día hábil" estricto.
- 2026-09-07 Facturación electrónica · modal → PÁGINA (render aprobado; directriz "no perder fechas/folios/NC" + "traer histórico muestre el pareo"). ANTES: la tarjeta "Emisión electrónica · SII" abría un `Modal` de 640px (App.jsx:9449). AHORA: **página a pantalla completa** in-situ (contenedor `position:fixed` con ← back, ancho `isDesktop?900:560`, sin backdrop) reorganizada en **tarjetas** (grilla 2-col en escritorio): **Estado+folios · Emitidas/Puesta en marcha · Acciones · Histórico SII · Notas de crédito**. **No se pierde nada + se agrega:** cada factura emitida ahora muestra **fecha de emisión** (`dte_emitido_at||issued_at`) y **fecha de envío** (`email_sent_at`, o "sin enviar" en rojo) — antes solo folio·cliente·monto·estado. **Traer histórico ahora muestra el pareo:** al terminar (`siiHistProg.done`), tarjeta "Resultado · qué se parea con el sistema" con mini-tarjetas **pareadas** (=`docs−guardadas`, ya en el sistema) · **sin parear** (=`guardadas`, nuevas que no calzan) · **NC**, con "Ver las N sin parear →" que cierra la página y abre el listado de facturas (donde caen las sin cliente, flujo existente). Se conservan folios (aviso CAF bajo) y NC con fecha/monto/factura anulada (misma compuerta `anularPorNC`). Cero cambios de lógica ni de datos; todas las acciones intactas (Verificar estados/Enviar resumen/Traer histórico/Set de pruebas/Libro de ventas/Historial). El scope de variables (`emitidas/enProd/Btn/siiIcon/estTxt/foliosEstado`) se mantuvo (la página se renderiza dentro del mismo IIFE de la portada). Build verde; verificado en demo móvil 375 (página con ← back, tarjetas apiladas) y escritorio 1280 (grilla 2-col aprovecha el ancho).
- 2026-09-07 Ventas · nuevo tipo de cobro "POR HORA" + consumo/alarma (Bloque 4, render aprobado con iconos+jerarquía; tope "ambos" elegido por el usuario). **SaleForm**: se agregó `['hora','Por hora']` al selector de forma de cobro (App.jsx:5271) con su editor — mini-tarjetas **Tarifa (UF/hora)** + **Tope de horas** + segmento **Por mes / Total del proyecto** (App.jsx:5292). Estado nuevo `tarifaHoraUF/topeHoras/topePeriodo` (App.jsx:4533), guardado en `cobro_config` (jsonb existente, sin columna nueva; App.jsx:4846). `generarCobros()` NO genera cuotas para 'hora' (cae a `[]`): se factura por consumo real. Resumen de la venta y `COBRO_LBL` muestran "Por hora · X UF/h · tope N h/mes|total". **HorasView** (App.jsx:23791): nueva sección **"Ventas por hora · consumo y tope"** en **tarjetas** (grilla 2-col en escritorio) — cada venta por hora con icono de reloj + barra de progreso + estado: **verde** normal, **ámbar** al ≥90% ("quedan X h"), **rojo** al pasar ("X sobre el tope"); muestra consumidas/tope, tarifa y facturable ≈ UF (`consumoVenta` suma `horas` por `sale_id`, que ya existía; tarifa = `cobro_config.tarifaHoraUF` con fallback al global 3 UF/h). Reusa la tabla `horas` (enlace `sale_id` ya existente) — nada duplicado. Demo: venta `s13` "Asesoría laboral por hora" (tope 20 h/mes) + 18 h en septiembre → alarma ámbar 90%. Build verde; verificado en demo (editor Por hora en Nueva venta con mini-tarjetas; tarjeta de consumo en Horas al 90% ámbar). PENDIENTE (anotado): agregar "Por hora" al modal "Cambiar forma de cobro" de ventas ya activas; opcional: alarma también en "Atender hoy" del Inicio (requiere pasar horas al Dashboard).
- 2026-09-07 Inicio · "Mis proyectos → Ver todos" con pausados y terminados (Bloque 4, render aprobado con iconos+jerarquía). ANTES: el strip mostraba solo activos-no-pausados (aunque el tile contaba los pausados → inconsistencia) y "Ver todos" saltaba a Cartera. AHORA (Dashboard, App.jsx:3525): cabecera **protagonista** navy "Mis proyectos · N en total" con conteos (en curso · pausa · fin); grupos con icono y conteo — **En curso** (▶, verde), **En pausa** (⏸, gris, con badge), **Terminados** (✓, con badge). Los pausados (que ya venían en `proyectosCartera`) ahora se muestran; los **terminados** (`activo:false`, que NO se cargan en el Inicio) se traen **bajo demanda** al tocar "Ver todos" (`cargarTerminadosProy`: query `proyectos_cartera` activo=false filtrada por mi inicial; en demo agrega un terminado de ejemplo), y el footer pasa a "Abrir en Cartera →". Cada fila con icono de estado + señal (punto de color) + fecha + chevron, clickeable a su proyecto. Nuevos estados `verTodosProy`/`terminadosProy` (App.jsx:2576). No duplica el panel Cartera (que ya tiene sus pestañas En curso/En pausa/Terminados) — es la vista rápida personal. Build verde; verificado en demo (protagonista + En curso; "Ver todos" carga Terminados y cambia el footer).
- 2026-09-07 Ventas · filtros del desglose más limpios (pedido del usuario: "menos pills, adaptado a desktop y móvil"). Los filtros que se despliegan al abrir Vendido dejaron de ser un MURO de pills (6 estado + 3 abogado = 9): ahora son **4 chips desplegables en una línea** — `Estado ▾` y `Abogado ▾` muestran el resumen ("Activo, Terminado") y abren un **checklist** (multi-selección, con `openF` = filtro abierto y un backdrop `position:fixed` que cierra al clic afuera; un solo desplegable a la vez), y `Año`/`Área` son selects nativos con look de chip. Además se QUITÓ del desglose de Vendido el aviso "N propuestas +14 días" (era de propuestas, ya vive en la tarjeta Propuestas como "· N tardías"; de paso corrige el "llevan"→concordancia). Envuelve en móvil, una línea en escritorio. Build verde; verificado en demo móvil 375 (chips, checklist Estado marca/desmarca y actualiza el resumen, cierre al clic afuera) y escritorio 1280 (barra en una línea sobre el desglose 2-panel).
- 2026-09-07 Ventas · HUB rediseñado al formato Banco/Facturación (render aprobado por el usuario, iteración: "más tarjetas, dos por línea, entrar limpio, el resto desplegable al clic"). `SalesView` (App.jsx:4075). ANTES: título con 2 botones (Nueva venta/Nueva propuesta), 3 `<select>` nativos siempre visibles (Estado/Año/Área), y hero de 2 tarjetas con el toggle UF/$ escondido dentro de "Vendido". AHORA: (1) **Encabezado** "Ventas" + toggle **UF/$** a la derecha (sale de la tarjeta). (2) **Grilla 2×2 de tarjetas** (ninguna full-width): fila 1 **Vendido {año}** (protagonista navy, icono trending) · **Propuestas** (pipeline UF + "N en pipeline · M tardías"); fila 2 **Nueva venta** · **Nueva propuesta** (tarjetas de acción dashed, mismo tamaño → onAdd/onAddPropuesta). (3) **Al tocar Vendido** (`hubView`) se despliegan los **filtros**: Estado **multi-selección** (`estSel` Set, por defecto Activo+Terminado — antes era `fStatus` string de un solo valor), **Abogado** multi (`abogSel` Set, chips con color de persona), Año y Área (selects compactos) + el **desglose** por Responsable/Área (2-panel en escritorio, ya existía). (4) **Al tocar Propuestas** → lista de propuestas (con Rechazar/Activar). (5) La búsqueda muestra lista plana. **Cuadratura**: "Vendido" y el desglose salen del MISMO universo (`vendSrc` = año/área ∩ estSel ∩ abogSel) → el desglose suma exacto al total (verificado 7.315+5.043=12.358). Fuentes de cifra intactas (`ventaUF`/`ventaCLP`). Info que SALE de la portada: "neto de costos"/"% del vendido" (secundario) y los 3 selects nativos (eran el muro). Build verde; verificado en demo móvil 375 (grilla 2×2, Vendido abre filtros multi + desglose, multi-select recalcula) y escritorio 1280 (grilla ancha, desglose 2-panel, drill a las ventas del responsable). PENDIENTE Bloque 4: venta "por hora" (tarifa/hora + alarma, enchufada a tabla `horas`) y "Mis proyectos → Ver todos".
- 2026-09-07 Bloque 2 (dinero) · Banco/cargos a personas: preguntar, no asumir "Sueldo" (render aprobado + ajuste del usuario "en Otro debe aparecer Comisión y poder escribir/aprender otras"). PROBLEMA: un cargo al RUT de alguien del equipo con caja chica (Martín/Martina) se sugería fijo como **Sueldos** con un solo "Confirmar" — pero muchas veces es reposición de caja chica. FIX en `useConciliacionModel`: (1) `cargoSugerencia` (App.jsx:26142) — cuando el RUT matchea `EQUIPO_RUT` y esa persona tiene caja chica (`pettyCash`), en vez de `{category:'Sueldos'}` devuelve `{persona, ambiguoCaja:true}`; **respeta lo aprendido**: si la glosa ya está en `costoOfiLearn` (p.ej. una "Comisión" enseñada antes), sugiere eso por sobre el RUT. Socios (`SOCIO_RUT`→Retiros) y personas sin caja chica mantienen la sugerencia directa de hoy. (2) `sugBar` dentro de `renderCargoClasificar` (App.jsx:26415) — nueva rama que pinta la PREGUNTA con 3 opciones: **Caja chica** (badge "probable", teal → `registrarCargoOficina(m,'Caja chica',persona)`→`abonoCajaChica` con su confirmación anti-duplicado ya existente), **Sueldo** (azul → Sueldos) y **Otro…** (ámbar → abre el clasificador de oficina completo: PERSONAS/SERVICIOS EXTERNOS con **Comisiones**/LOCAL/… + buscador para **escribir un concepto nuevo** que se aprende `glosa→categoría` vía `costoOfiLearn` y luego se sugiere solo). Nada se asigna hasta que tocas una opción; todo reversible (Deshacer). (3) `cargoInlineSug` (App.jsx:26762) — rama para la fila cerrada: chip teal "Cargo a X · ¿caja chica o sueldo?" con "Elegir" que abre la fila (sin esta rama, la sugerencia inline habría intentado confirmar una categoría `undefined`). NOTA verificada: la lista de cargos YA agrupa por año→mes (acordeón), la queja de "no separado por mes" estaba resuelta. Demo: se agregó `mov7` (cargo $300.000 a Martín, RUT 19.889.733-7) para exhibir la feature. Build verde; verificado en demo (la pregunta con 3 opciones aparece, "Otro…" despliega Comisiones + buscador, sin errores React).
- 2026-09-07 Finanzas · reorden del menú + "Banco" + Conciliación abre como PÁGINA (directrices del usuario, render aprobado). (1) **Menú Finanzas reordenado** al orden pedido: **Banco · Cobranza · Horas · Inteligencia · Repricing** (antes Conciliación·Inteligencia·Cobranza·Repricing·Horas), en el sidebar de escritorio (App.jsx:1961). (2) **"Conciliación" → "Banco"** como nombre del DESTINO en todos los menús que el usuario ve: sidebar (1962), acceso directo del inicio (2915), back-nav/breadcrumb `TAB_LABELS` (27801), paleta de comandos `VIEWS_PALETTE` (27804) y modal de roles `CAP_ROWS` (6368). La vista por dentro MANTIENE su H1 de trabajo **"Conciliación bancaria"** (26681/26843) y la acción de paleta "Conciliación bancaria" (PALETTE_ACTIONS 27812) se conserva para que la búsqueda por "conciliación" siga funcionando. Elección del usuario vía render (Banco vs Conciliación). Coherente con la regla de wording del proyecto: "Conciliar" = solo banco → el menú nombra el destino (Banco), el verbo vive dentro. (3) **Acceso directo "Banco" abre la PÁGINA, no el modal-menú.** Antes `onAcceso('conciliacion')` hacía `setModal({type:'conciliaHub'})` (un modal con 4 opciones); ahora hace `setTab('conciliacion')` → la página completa de Conciliación bancaria (App.jsx:30065). NO se pierde ningún camino: el hub de 4 opciones (Bancaria · Cotejar SII · Duplicados · Cobertura SII) sigue disponible desde el menú ☰ (29998); Gastos y Copiloto no se tocaron. Build verde; verificado en demo móvil 375 (acceso "Banco" → página con las 6 tarjetas + back ‹, ya no modal) y escritorio 1280 (sidebar Finanzas en el nuevo orden con "Banco").
- 2026-09-07 Bloque 1 (dinero/cuadratura) · ① Cobranza wording + ② facturas sin año. **① Por qué Cobranza ($48,9M) ≠ Facturación por pagar ($53,9M): NO es descuadre, es que miden cosas distintas** — Cobranza actúa solo sobre lo VENCIDO, Facturación muestra TODO lo por pagar (vencido + al día). Se aclaró en la UI sin tocar cifras: el hero de Cobranza cambió el título "Por cobrar hoy · recordatorios que tocan" → **"Vencido"** (se quitó la frase que el usuario no usa); se agregó `porPagarTotal` (fuente única `_cobr`: facturas Pendiente/Vencido con saldo>0, excluye reembolso/NC) y `nAlDia`, y una línea de contexto "Por pagar total $X · N al día" que aparece SOLO cuando el total supera lo vencido (App.jsx:23228-23231, 23296-23299) — así se ve de dónde sale la diferencia. **② Facturas sin año de venta (desorden) → 0.** Backfill en prod en 3 pasos: (a) 317 facturas con `sale_id` recibieron `sale_year` de su venta (`sales.year`); (b) 4 pagadas sin venta, por `paid_at/issued_at`; (c) 23 vivas/anuladas sin venta (11 Vencido, 7 Anulada, 5 Pendiente), por `issued_at/due`. Resultado verificado: **0 facturas sin año** (361 con venta + 23 por fecha). Nota: el modelo de lectura (`anioVentaDe`, App.jsx:8170) deriva el año en vivo de `sale_id→sales.year` a propósito (sin respaldo por `issued_at`, comentario 8169) — el backfill es para las que NO tienen venta. **Fix a futuro (que no vuelva a pasar):** `upsertBilling` (supabase.js:174) ahora, en filas NUEVAS sin `sale_id` ni `sale_year` pero con `issued_at`, copia el año de emisión como respaldo — no toca updates (traen id) ni facturas con venta (derivan en vivo); mismo respaldo inline en el import masivo (App.jsx:21084). Build verde; ① verificado en demo (Cobranza hero "Vencido", sin la frase, sin crash; la línea de contexto no dispara en demo porque vencido=total). PENDIENTE Bloque 1 cerrado; siguen Bloques 2-4.
- 2026-09-07 Conciliación · sugerir por RUT/RS (idea del usuario) + fix dato Mi Market. (1) Nuevo helper `pagosRutSugeridos(b)` en useBillingModel (junto a `pagosDe`): abonos SIN conciliar cuyo RUT/RS (o cliente) coincide con la factura pero el MONTO NO calza exacto (los exactos ya son el verde). Misma lógica de identidad que `pagosDe`, sin el requisito `resto===saldo`. En las filas de Facturación (fila/filaAll de la landing "Por cliente"/"Todas") se agregó un bloque TURQUESA "PAGO DEL MISMO RUT SIN CONCILIAR · Revisar" que aparece SOLO cuando no hay calce exacto (`!porConciliar`); "Revisar" abre el panel `pagosFor` que ahora CAE a `pagosRutSugeridos` cuando `pagosDe` viene vacío (header "Pagos del mismo RUT/RS · revisa el monto", fila "MISMO RUT · monto distinto"). Al conciliar, `conciliarPago` ya maneja el parcial con su confirmación que muestra todo — nunca a ciegas, nunca automático. Cubre los casos reales (Ariel Vaisman: $46.811 de Rendalo vs factura $250.000; Kemeny con 2 abonos). (2) DATO: factura 372 (Mi Market) estaba conciliada 100% en el banco ($1.625.782 de MU Mercadito Urbano SpA, 13-07-2026) pero quedó status "Vencido" → corregida a "Pagado" con paid_at/paid_amount/ref reales (único caso así en toda la cartera; verificado). Build verde; landing verificada en demo sin crash (turquesa no dispara en demo por falta de abono de mismo RUT). PENDIENTE (anotado): extender la sugerencia turquesa a Cierre de mes y a la ficha (hoy solo en las filas de la landing).
- 2026-09-07 Conciliación · hub a grilla de tarjetas + fin del doble conteo (directriz del usuario). Antes "Listo para conciliar" (calces exactos) y "Por resolver" contaban el MISMO abono dos veces (Listo ⊆ Por resolver). Ahora: se eliminó la tarjeta "Listo para conciliar" y "Por resolver" se renombró a **"Por confirmar"** (fusiona ambas; un solo número, sin duplicar). El hub pasó a GRILLA 2-col (ninguna tarjeta full-width) en el orden pedido: **Abonos · Cargos / Por confirmar · Conciliados / Sin respaldo · Cargar cartola** + Exportar CSV. "Por confirmar" es la protagonista (roja, badge N + $monto) y al tocarla abre el interior `concView='porresolver'` con la comparación. NADA se pierde: los batches "Conciliar N que calzan" (`setLoteConfirm`, en el interior 27176/27183) e "Identificar por su nombre" (`setRevSugOpen`, interior 26833) siguen vivos en la lista; se agregó tarjeta "Conciliados" (conteo de abonos ya cruzados). Build verde; verificado en demo escritorio 1280 (6 tarjetas en orden, sin "Listo"/"Por resolver") y móvil 375 (sin overflow).
- 2026-09-07 Facturación · HUB simplificado (render aprobado, iteración móvil). Estructura nueva del resumen: (1) "Etapas del cobro"→**CICLO DE LA FACTURA** (Por facturar → Por pagar → Pagado, ya iba). (2) Se QUITÓ "Vencido por antigüedad" de la portada (vive dentro de Por cobrar). (3) Tarjetas de mes con TITULAR + MES en mayúsculas: "CIERRE DE MES / AGOSTO" y "Facturas por emitir"→**"FACTURACIÓN DEL MES" / SEPTIEMBRE**, monto grande (23px). (4) ACCESOS a tarjetas iguales con icono (grid 2-col): Por socio · Anticipos disponibles (valor + dot) · Cobranza · **Por revisar** (badge N, se despliega en tarjeta via `porRevOpen`) — reemplaza el link ancho de Por socio + los minis + la lista Por revisar. (5) SII sigue como tarjeta (abre la página). (6) Se ELIMINÓ el menú **"↑ Importar ▾"** del top (Excel/PDF/Drive/Cuadrar-SII/Cargar-XML/Cargas-anteriores): Cuadrar-SII y Cargar-XML ya viven en "Facturación del mes"; se agregaron **Cargar PDF** y **Desde Drive** al hub "Cargar XML" para no perder ningún camino (el input XML oculto se conserva). Wording Pagado/Por pagar en todo. Build verde; verificado en demo escritorio 1280 (todas las tarjetas, Por revisar despliega, sin "Importar") y móvil 375 (sin overflow).
- 2026-09-07 Cierre de mes · FIX bug "se mueve entre formatos en escritorio" (reporte del usuario) + inicio rediseño hub Facturación. BUG: al navegar de mes el layout saltaba de escritorio (tabla ancha) a móvil (tarjetas angostas). Causa doble: (1) `shiftMes` hacía `setEstFiltro(null)` → sin tarjeta activa no había lista → el contenedor `maxWidth:1100` SIN `width` colapsaba a ~600 (shrink-to-fit) y parecía móvil; (2) sin ancho fijo el layout dependía del contenido. Arreglo: el contenedor ahora es `width:100%` + maxWidth (el ancho depende solo de isDesktop, nunca del contenido) y `shiftMes` MANTIENE la tarjeta activa al cambiar de mes. Verificado en demo 1280: tabla escritorio persiste al navegar (‹/›), ancho estable 867px, maxWidth 1100px constante. HUB Facturación (parcial, en curso): "Etapas del cobro"→"Ciclo de la factura"; labels Por cobrar→**Por pagar**, Cobrado→**Pagado** (consistente con Cierre de mes); se quitó el bloque "Vencido · por antigüedad" de la portada. Falta (próximo commit): tarjetas de mes con título/mes en mayúsculas, accesos (Por socio/Anticipos/Cobranza/Por revisar/SII) a tarjetas iguales, y quitar el menú Importar del top.
- 2026-09-07 Cierre de mes · rediseño a tarjetas-acordeón + wording Pagado/Por pagar (render aprobado por el usuario, iteración móvil). La foto pasó a: Emitido (hero) + GRILLA 2×2 de 4 tarjetas con icono — **Pagado** (verde/check), **Por pagar** (navy/file), **Sin identificar** (ámbar/exchange), **Vencidas** (rojo/alert). Cada tarjeta es un ACORDEÓN: al tocarla se despliega SÓLO su listado abajo (las demás se cierran), manejado con `estFiltro` (default 'porcobrar'): 'cobrada'→pagadas, 'porcobrar'→por pagar, 'vencido'→vencidas + botón "Recordar a N", 'sinid'→panel de abonos del banco sin identificar (con "Revisar en Conciliación"). Se eliminaron las dos FRANJAS de ancho completo (amarilla "sin identificar" + roja "vencidas"): ahora son tarjetas. WORDING (regla del usuario: al emitir ya está cobrado → sólo Pagado/Por pagar): COBRADO→**Pagado**, POR COBRAR→**Por pagar** en la foto y en "Historial y comparación"; el estado de factura `CIERRE_EST.sinpago` "Sin pago"→**"Por pagar"**. La lista de facturas sigue en tarjetas móviles (sin desborde) con folio+glosa+estado. Build verde; verificado en demo móvil 375 (4 tarjetas 2×2, acordeón Sin identificar→abonos y Pagado→"Pagada", 0 overflow) y escritorio 1280 (4 tarjetas, 0 overflow). NOTA: el "corte" que el usuario veía en su iPhone era build cacheado — el código ya daba tarjetas a 375px.
- 2026-09-07 Cierre de mes · mini-desglose por cliente (pagado/por cobrar) en la cabecera del grupo (directriz del usuario). Cada grupo suma ahora `g.cob` (cobrado) además de `g.saldo`; el header muestra el saldo grande (por cobrar) y, si el cliente tiene pago parcial (cob>0 y saldo>0), una línea chica verde "cobrado $X". Así se ve de un vistazo lo pagado y lo no pagado por cliente, sin cifras duplicadas ni tarjetas extra. Build verde; verificado en demo (Tech Araucanía: $4.000.000 por cobrar · 2 facturas · cobrado $4.000.000).
- 2026-09-07 Cobranza · detalle de facturas enriquecido (directriz del usuario: "muy pobre, poca info y acciones"). Al expandir un cliente (siempre replegado por defecto), cada factura vencida ahora muestra —igual densidad que Facturación— FECHA día-grande + folio + GLOSA (concept) + chip "Vencida N d" + "emitida … · vence …" + saldo (y "abonado X de Y" si es parcial), vía un renderer compartido `detItem`. Dos ACCIONES por factura: "Buscar pago ›" (→ Conciliación, donde está el pago sugerido/motor al revés) y "Abrir factura ›" (→ modal de la factura). Se agregó el detalle también al MÓVIL (antes no tenía; ahora la tarjeta despliega con chevron). Se MANTIENE intacto el cockpit de recordatorios (Enviar recordatorio / Liberar / Pausar / Cobranza automática) — Cobranza no replica Facturación, la ENLAZA (regla no duplicar). Props nuevas `onOpenFactura`/`onIrConciliacion` cableadas en App:30074. Build verde; verificado en demo móvil 375 y escritorio 1280 (Tech Araucanía: fecha+glosa+chip+acciones; otros clientes replegados) sin error React. PENDIENTE (anotado): no perseguir en Cobranza una factura que ya tiene abono en el banco (requiere pasar los abonos a CobranzaView).
- 2026-09-07 Notas de crédito #2 (ficha) + #4 (detector RCV) + fixes de esquema. **#2 Historial NC en ficha:** backfill de las 10 NC históricas (tipo 61) al MODELO CANÓNICO de `anularPorNC` — se insertó cada NC como fila billing (`billing_type='nota_credito'`, `sii_tipo_dte=61`, `nc_ref_billing_id`→factura) y se marcó la factura anulada con `anulada_por='Nota de crédito N°X'` (+ reemplazo 256→257 en `reemplazada_por_billing_id`). CERO UI nueva: la ficha YA renderiza el vínculo (App.jsx:16830 "Nota de crédito N°X · reemplazada por…"), las NC se muestran anidadas bajo su factura y todos los helpers ya excluyen `nota_credito` de los totales → no cambia ninguna cifra. **Fixes de esquema (prod, vía migración):** (a) `billing_billing_type_check` ampliado para permitir `'nota_credito'` — antes lo rechazaba, o sea `anularPorNC` habría fallado en prod (bug latente); (b) índice `billing_folio_unico_vivo` del blindaje ahora EXCLUYE `nota_credito`/tipo 61 (serie de folios aparte, no compite con facturas). **#4 Detector RCV→anular con compuerta:** nuevo `cargarNCResolver` (modelo) que lee `sii_cargas_docs` tipo 61, cruza `detFolioDocRef`→factura y lista las NC cuya factura SIGUE VIVA (no anulada) y sin fila NC aún; tarjeta "Notas de crédito del SII · N por resolver" en la página SII (siiPageOpen), botón "Revisar y anular ›" que abre la MISMA compuerta existente (`setNcConfirm`→`anularPorNC`) — nunca anula sola. El sync mensual ya persiste NC 61 (`sync-ventas-rcv` cuenta `notasCredito`). Build verde; #2 verificado en datos (10 NC enlazadas), #4 verificado en demo (la página SII abre y muestra el detector en estado vacío, sin crash). PENDIENTE (avisado): correr el RCV mensual lo hace el usuario (certificado SII); detectar el reemplazo (replId) automático desde el RCV queda para después.
- 2026-09-07 Conciliación · "Conciliar cobradas · cartola" de MODAL → PÁGINA (directriz del usuario: "esto no debería ser un modal"). CobradasSinRespaldoModal renombrado a `CobradasSinRespaldoPage`: se cambió el wrapper `<Modal fullscreenOnMobile title=…>` por un contenedor de página con header sticky (botón "←" + título + "cobradas sin respaldo · N") y cuerpo centrado (maxWidth 900 en escritorio), mismo patrón que el interior/hub de Conciliación. En ConciliacionView se agregó retorno temprano `if(cobradasOpen) return <CobradasSinRespaldoPage …/>` (tras `if(hubOpen) return concHub`) y se quitó el overlay `{cobradasOpen&&<…Modal…/>}`; ahora reemplaza la vista (móvil y escritorio) en vez de abrir un modal encima. Sin tocar la lógica (mismos props: reconciliar, aplicadoByFactura, etc.). Build verde; verificado en demo escritorio 1280: abre como página con la barra del app visible (no overlay), foto (4 por conciliar/1 exacto/3 por revisar) + tarjetas + "5 sin respaldo" colapsado, y "←" vuelve al hub, sin error React.
- 2026-09-07 Facturación landing "Por cliente" · consistencia de canon con "Por socio" (directriz del usuario: "esta vista también es antigua" → mismo canon en las dos, sin duplicar). La vista YA tenía lo de fondo (agrupación por cliente colapsable, glosa por factura en `fila`/`filaAll`, Conciliar inline "PAGO SUGERIDO" reusando `pagosDe`→`conciliarPago`); lo que la hacía ver antigua era la tarjeta de cliente plana. Se reestructuró el header de cada tarjeta al mismo patrón que el grupo de "Por socio": chevron + ÍCONO user en cuadrito azul (SIcon) + nombre protagonista con RS/RUT debajo + total (headColor) y "Ficha →" a la derecha. Sin tocar la lógica ni las cifras (mismos helpers). Hereda automáticamente la confirmación "muestra todo" de `conciliarPago`. Build verde; verificado en demo escritorio 1280 (grupos con ícono; Constructora Pehuén abierta muestra facturas con glosa por acordeón de estado) sin crash ni error React. Cierra el "las dos vistas, mismo canon".
- 2026-09-07 Facturación "Por socio"/Cierre de mes (CierreMesModal) · rediseño (render aprobado v3, directrices del usuario). (1) FOTO sin cifras repetidas (canon): se eliminó la fila de 3 tiles (Pagada/Por conciliar/Sin pago) que DUPLICABA el hero ("Pagada"="Cobrado", "Sin pago"="Por cobrar"). Ahora Emitido protagonista → Cobrado (icono check) + Por cobrar (icono file), con Vencido (subconjunto real, icono alert, `tot.mV/nV` nuevos, filtro `estFiltro='vencido'`) y "N listas para conciliar" (icono exchange, solo si nD>0) ANIDADOS bajo Por cobrar. Iconos del canon (SIcon). (2) LISTA agrupada por CLIENTE (protagonista + icono user + total + N facturas; multi-factura colapsa, auto-abre si hay pago detectado; `grpOpen`), cada factura con N° folio + GLOSA (concept) + responsable. Reusa filaEl(f,inGroup); grid escritorio 6-col, tarjetas en móvil. (3) CONCILIAR inline (motor al revés, `pagosDe`→`est==='detectado'`): bloque verde "PAGO DEL BANCO QUE CALZA · MISMO RUT/MONTO" con botón un-toque (varios candidatos→"Ver N" abre detalle). (4) `conciliarPago` (FUENTE ÚNICA, mejora ficha+Cobranza+esta vista): el confirm ahora MUESTRA TODO antes de aplicar — factura+cliente+glosa+saldo y el pago del banco (fecha, monto, quién, RUT, n° op, ✓ mismo RUT) — nunca a ciegas. Build verde; verificado en demo (?demo=1) móvil 375 y escritorio 1280 sin crash (solo 401 Supabase esperado). PENDIENTE: aplicar el mismo canon al landing "Por cliente" (en curso).
- 2026-09-07 Blindaje #3 · REGLAS DE ORO de la factura, a nivel BASE DE DATOS (lo más valioso; a prueba de bugs futuros). Migración `blindaje_facturas_folio` aplicada a prod (MCP), 3 protecciones: (1) trigger `billing_guard_folio_delete` (BEFORE DELETE OR UPDATE) que BLOQUEA borrar/soft-borrar cualquier factura CON folio (`invoice_no` no vacío) — "Regla de oro: no se puede borrar la factura con folio N…"; escape puntual y auditado sólo vía `set_config('app.allow_folio_delete','on')` en la misma transacción (recomposición controlada); (2) trigger `billing_limpia_conciliaciones` (AFTER DELETE OR UPDATE→soft-delete) que borra las conciliaciones `tipo_destino='factura'` que apuntaban a esa factura → NUNCA quedan conciliaciones huérfanas (era la causa raíz de "Factura N°—"); (3) índice único parcial `billing_folio_unico_vivo` sobre folio normalizado (`regexp_replace(invoice_no,'\D','','g')`) donde `deleted_at is null` → un folio = una factura viva. Verificado en prod: el UPDATE que intentaba soft-borrar el folio 362 (Javier) lanzó el error esperado; un UPDATE normal (updated_at) pasó sin problema. Frontend (defensa en profundidad, :16466): si una conciliación apuntara a una factura ausente, la fila muestra "→ por resolver · re-conciliar" en vez de "→ Factura N°—". Build verde. Cierra la remediación de integridad 2026-09-06: ahora el sistema IMPIDE que se repita el batch fantasma de 2026-06-09.
- 2026-09-06 Conciliación inversa · PAGO SUGERIDO inline en la factura impaga (idea del usuario; el motor YA existía — regla "no duplicar"). Reuso `pagosDe(b)` (:8259, la inversa de `facturasParaMov` :25298) que ya alimentaba el badge "Por conciliar" (calcesSugeridos :8297 / porConciliarIds :8317) y el panel "Conciliar pago" (:9401). Mejora de VISIBILIDAD (mejora 1, alto impacto/bajo riesgo): en cada fila Pendiente/Vencida con un abono que calza, se muestra AHORA el pago sugerido inline SIN abrir el panel — "PAGO SUGERIDO · MISMO RUT/MISMO MONTO · fecha · monto · contraparte" con botón: si es único → "Conciliar" en un toque (conciliarPago, con confirmación); si hay varios → "Ver" abre el panel completo. Insertado en las DOS filas (lista agrupada :9382 y "Todas" :9550) vía el mismo bloque. No cambia el motor ni la lógica de match. Build verde, verificado en demo: Facturación renderiza sin error de consola (el demo no tiene un abono que calce, así que la sugerencia no aplica ahí). PENDIENTE (ofrecido, no construido): tolerancia de calce cercano (±comisión, ej. Abragan −$2.000; hoy exige monto EXACTO en :8259), combos 2-abonos (reusar comboExacto), unificar pagosDe/facturasParaMov en un helper único, y extender el inline a ficha Financiero + Cobranza.
- 2026-09-06 SII histórico · FUENTE DE LA VERDAD read-only (directriz del usuario: fuente verificada, sin tocar nada más). Remediación previa de integridad ejecutada contra prod con OK por bloque: 0 pagos "Factura N°—" y 0 folios duplicados vivos (restauradas 11 facturas reales borradas por el batch 2026-06-09; quitadas fantasmas/Anuladas sin banco de Alejandro Lee 110/120/139/166/187/218/140/260/294/314/109; Karla Itaim 69 y Eugenia Perry 190 = doble depósito+devolución → movimientos a 'interno'; Javier Vergara 362 conc duplicada al mismo movimiento borrada + fusión de RS duplicada "INVERSIONES SIRKA SPA"). Regla desempate: "Pagado" solo válido con conciliación bancaria real. NUEVO en edge fn sii-sync (v31, ADITIVO, caminos existentes intactos): acción read-only `sync-ventas-rcv` + `getVentasFull` en rcv.ts que trae ventas RCV incl. TIPO 61 (notas de crédito, que getVentas omite) y las espeja en sii_cargas_docs con estado 'rcv_historico' (INVISIBLE a los flujos existentes, que filtran 'sin_registrar'), `ignoreDuplicates` (nunca pisa), SIN conciliar ni tocar billing. Frontend: botón "Traer histórico del SII (2025-01 → 2026-06)" en la página admin Facturación electrónica·SII (siiPageOpen), loop mes a mes con barra de progreso, reusa siiCall + sesión admin. Rango = un mes antes del 1er pago (mov más antiguo 2025-02-06) hasta antes del sello directo del SII (XML 2026-07+). PENDIENTE (avisado): (a) el sync mensual futuro también debería persistir el RCV incl. 61 al espejo; (b) flujo NC→anular con compuerta; (c) cuadre SII↔sistema (faltantes/fantasmas/montos/NC), resolver folio 72 (Saavedra vs SSIAL), 37 huecos de secuencia y folio jul-2025 de Perry. Build verde; el disparo al SII lo corre el usuario (sesión+certificado) — piloto de 1 mes primero.
- 2026-09-06 Proyectos/Cartera → FUNDIDO en Inteligencia (Opción 1, decisión del usuario; resuelve la duplicación con el lente de salud). El lente `cartera` de Inteligencia se renombró a "Seguimiento" (wording Set B: sub "N sin avanzar · toca empujar", ct = riesgo+dormido en color ámbar). Se sumó RAZÓN SOCIAL + RUT a las filas de cada segmento (regla de la casa; via rsLabel). Se agregó enlace "Ver seguimiento completo · novedades y rentabilidad ›" que abre el board CarteraView (setTab('cartera')) → NADA se pierde (novedades IA, rentabilidad, estado manual siguen reachable). Se quitó "Proyectos" de la NAV visible: TABS_LIMITED (:850), SideNav secciones admin (:1967) y palette del limited (:27682). La RUTA sigue viva (router tab==='cartera' :29948 + palette admin + alertas de Inicio "Proyectos críticos/sin mover" + el enlace nuevo). El limited YA NO ve Proyectos (foco en Tareas/Gastos/Horas, directriz del usuario). Build verde, verificado en demo: nav sin "Proyectos", lente "Seguimiento" abre su detalle, enlace abre el board sin crash. NOTA "crítico": sigue siendo estado MANUAL del proyecto (no hay regla automática); pendiente si el usuario quiere una (plazo vencido / sin actividad +Nd).
- 2026-09-06 Horas · REGISTRO RÁPIDO para adopción del equipo (directriz del usuario: Horas clave para limited). Tarjeta verde arriba de "Mis horas": cliente → proyecto → horas con CHIPS, en 2 toques (si el cliente tiene un solo proyecto queda auto-seleccionado). Clientes-chip = "frecuentes" aprendidos por usuario (peso: horas propias×3 + tareas asignadas + ser responsable), top 4, + "Otro cliente…" con buscador. Proyecto = ventas activas del cliente (o "Sin proyecto"). Glosa + facturable detrás de "+ glosa (opcional)" (no se pierde detalle, queda clickeable). Guarda vía addHora con nuevo campo sale_id. SQL aplicado a prod (MCP): ALTER TABLE horas ADD COLUMN IF NOT EXISTS sale_id text. Build verde, VERIFICADO en demo end-to-end (elegir cliente→aparece Proyecto→botón "Registrar 1,0 h · Comercial Andes SpA"→guarda→resetea, sin error). Iconos SVG (clock/check), cifras a la derecha, móvil intacto (misma tarjeta responsive). Aplica a limited y admin ("Mis horas"). Se conservan Revisar-mi-día (IA), timer y pegar-y-redactar.
- 2026-09-06 Canon · franjas de estado → opción C (ícono SVG, decisión del usuario; iconos no emojis). Se quitó el `borderLeft` de color de estado en 3 puntos: (1) Caja chica fila (limited) — reemplazado por ÍCONO SVG de estado tras el día-grande: check verde = listo (con cliente+categoría) / triángulo-alerta ámbar = incompleto; (2) Conciliación fila (:27144) — quitada la franja; el chip de estado ya la marca (no se duplica); (3) Tareas resumen "Saldo en tu caja" (:20728) — quitada la franja; el color ya va en la cifra. El borde de color de SALDO en "Clientes por abogado" de Gastos NO se toca (es signo de plata, semántica distinta). Build verde, Conciliación verificada sin crash; Caja chica es vista limited (verificar en prod).
- 2026-09-06 Canon · ClientsViewLimited → 2-PANEL de escritorio (limited también merece escritorio, directriz del usuario). Antes: al abrir un cliente la ficha REEMPLAZABA toda la vista (se perdía el directorio A-Z). Ahora en escritorio (isDesktop) = grid 360px|1fr: directorio A-Z a la izquierda (scroll propio) + ficha a la derecha (o "Elige un cliente para ver su ficha"); reusa el mismo `listInner` y la `Ficha` interna. MÓVIL IDÉNTICO (if(selected)→ficha; si no→lista, mismo listInner). Build verde. NO verificable en demo (vista solo de rol limited) → verificar con usuario limited en prod. Falta aún: reemplazar el resumen paralelo pobre por la grilla KPI del canon (pendiente).
- 2026-09-06 Canon de hubs (auditoría vs Inicio/Gastos/Facturación) · Inteligencia: el ÍNDICE de lentes (Oportunidades/Salud de clientes/Servicios/Tendencias/Asesor IA) pasó de lista-acordeón a GRILLA DE TARJETAS (patrón de casa: cardBase + iconSq cuadro-34 + conteo arriba-derecha tabular + título accent), grid 3-col escritorio / 2-col móvil (isDesktop:3544). El detalle de cada lente se abre igual debajo (biSec intacto), mismas cifras. Build verde, verificado en demo escritorio (grilla 3×, 5 tarjetas; al tocar Oportunidades se abre su detalle). NOTA de la auditoría de canon: la app está muy cerca del canon; brechas restantes = Cartera hub (sin hero + config en portada :24357), ClientsViewLimited (sin 2-panel :1054 + resumen sin grilla KPI), Ventas lista plana angosta en escritorio (:4244), franjas borderLeft de estado (Caja chica :1648 / Conciliación :27142 / Tareas-saldo :20726 — DECISIÓN transversal pendiente), Cobranza buscador/expand móvil. DIRECTRIZ del usuario: aplicar los cambios de formato también al rol LIMITED y priorizar que sus vistas sean fáciles, sobre todo HORAS (clave para adopción).
- 2026-09-06 Conciliación · rediseño del hub · Tanda 4 (de-dup): la "tira de triage" del interior (Por resolver / Identificar por su nombre / Facturas sin respaldo) ahora se muestra SOLO en móvil (`!isDesktop`). En escritorio el rail (brújula) ya la lleva → el interior deja de duplicarla y queda con el banner "Listos para conciliar" + la lista de movimientos (la "cola"). Móvil intacto (sin rail, la tira sigue siendo su navegación). Un solo token de cambio (`&&!isDesktop`), build verde, verificado en demo escritorio (el interior va directo de "Ver:" a "Movimientos", sin las 3 filas de acción; el rail las conserva).
- 2026-09-06 Conciliación · rediseño del hub · Tanda 2/3: tarjeta "Listo para conciliar" en hub móvil (concHub) y rail escritorio (deskRail) + FUENTE ÚNICA de calces exactos. Se extrajo el cálculo de "listos" (calces 1:1 exactos con su factura) que vivía inline en el interior (~26961) a un `lcData` en el tope de ConciliacionView; el interior ahora lo reusa (dedup, sin cambio de comportamiento) y el hub/rail muestran la tarjeta verde "Listo para conciliar · N · Conciliar N" cuando listosN>0. onClick reusa el handler existente setLoteConfirm(listosExactos) → abre el modal de confirmación (NO escribe directo; la conciliación se confirma en el modal como siempre). La línea de salud "% conciliado" del spec se OMITIÓ deliberadamente (cifra nueva, cero-tolerancia). Build verde, verificado en demo sin crash (la tarjeta no aparece en demo porque no hay calces exactos = comportamiento correcto). AVISO: la tarjeta "Listo" toca el camino de conciliación (dinero) — verificar visualmente con datos reales que muestre el N correcto y abra el modal. Móvil intacto. Render/spec: artifact d87b20b5.
- 2026-09-06 Conciliación · rediseño del hub (build por incrementos, tras varios renders/comparaciones con el usuario) · Tanda 1: WORDING de fuente única (prSub, alimenta hub móvil + rail escritorio + interior). "Abonos sin cliente asignado"→"Falta asignar el cliente"; "Cobros identificados / calzan con factura emitida · por registrar / Conciliar"→"Por confirmar / la app ya propone quién es / Confirmar"; "Abonos sin factura asociada / Clasificar"→"Falta la factura / Vincular". Solo display (verificado: prSub se usa en :26503 hub y :26616 rail, cv de routing intacto). Build verde, verificado en demo (rail muestra "Falta asignar el cliente · $5.177.309"). Spec completo del rediseño (A móvil + B escritorio, detalle completo, wording, bordes corregidos) en artifact d87b20b5. PENDIENTE tandas siguientes: hub móvil A (línea de salud + "listo para conciliar" + verbos) y escritorio B (cola ancha + brújula), con render aprobado.
- 2026-09-06 Auditoría interna · Tanda B1: limpieza canónica (franjas de color + dingbats), SIN eliminar iconos (feedback del usuario: los dingbats pasan a su ícono SVG, no a texto pelado). FRANJAS borderLeft de color quitadas (la señal la dan la fecha coloreada / el punto / el chip, que se conservan): (1) fila de tarea en Tareas (bigDate ya va coloreado); (2) kpiCard de Inteligencia (la cifra ya va en color); (3) tarjeta de Oportunidades/OPPS (cada fila ya tiene su dot de color); (4) saleRow de Ventas propuesta tardía (se mantiene el borde soon + chip 'Nd pendiente'). DINGBATS→ícono SVG manteniendo el ícono: (5) chip '✓ Al día' de Gastos → SIcon check + 'Al día'; (6) '·N✓' de Cobranza (tabla escritorio, último contacto) → '· N' + SIcon check; (7) badge '✨ IA' de Horas (agenda) → sparkle SVG (mismo del Counsel AI, no emoji) + 'IA'. Cero cambios de lógica ni cifras. Build verde. Verificado en vivo (demo, 1280px): Inteligencia con KPIs y tarjeta Oportunidades sin franja (fila 'Tech Araucanía SpA · $4.000.000' con dot). Render antes/después (escritorio+móvil, versión iconos): artifact cd291c2a. PENDIENTE B2 (con su render): redundancias estructurales — Horas·Rentabilidad (doble hero navy + doble lista por cliente) y paneles de config sobre el landing de Cartera. Micro-redundancias (subtítulo Cartera, pill 'Activas' de Tareas, badge Cotejar-SII, caja 'Notaría Lascar') quedan para incluir en B2.
- 2026-09-05 Auditoría interna (9 módulos) · Tanda A: borrado de código muerto verificado (−192 líneas, cero cambios visibles). Cada identificador confirmado sin usos reales antes de borrar (varios "usos" eran homónimos en otros componentes: revOpen/fDesde/prevM). DASHBOARD: restos del hero "Metas del año" (descargarMetas ~92 líneas nunca llamado + ventasDelAnio + revOpen + histOpen + HistIcon + tendenciaPP + prevM), helpers stepEl/connEl del embudo vertical viejo, kOpen('ventas') y bloque "Equipo·trazabilidad" kOpen('equipo') (ambos inalcanzables: no existe su kTile). FACTURACIÓN: 6 constantes muertas de ChecklistFacturacion (porFacturarCLP/emitidasCLP/totalCLP/totalUF/nEmit/nTotal, hero de totales que el rediseño quitó). TAREAS: andamiaje de un calendario anterior (semanaOffset/lunesSemana/diasSemana/fmtLabel; _cal recalcula su propia semana). GASTOS: menú notaBtnOpen inalcanzable (setNotaBtnOpen(true) no existe) + su estado. useExpensesModel: verTodos/setVerTodos sin uso. CAJA CHICA: estados de un tab "historial" y filtros ya removidos (openLiquidados/fDesde/fHasta/fCliente) + comentario stale. Build verde. Verificado en demo: Inicio (embudo intacto), Gastos hub, Buscar clientes (lista+buscador), Tareas (calendario "Próximas semanas" ok) — cero crashes. NO tocado en esta tanda (requieren build+demo propio, no son borrado ciego): isProg de Facturación (entrelazado con dateField/sort vivos), renderClientGroup (rewire), bsCard (conversión), CostosOficinaModal open/tog (agentes se contradicen, por verificar). Backlog maestro en artifact.
- 2026-09-05 Deuda UI · overlays a mano → Modal fullscreenOnMobile (tanda 2). Dos modales de contenido más convertidos (escritorio centrado igual, iPhone pantalla completa con 'Volver' + footer anclado): (1) 'Identificar por nombre' (Conciliación · RevSug): lista de abonos con cliente sugerido + footer Cancelar/Identificar N. (2) 'Agregar novedad SII' (Inteligencia): formulario (título/resumen/tipo/N°/fecha/prioridad/fuente/áreas) + footer 'Guardar novedad' anclado. VERIFICADO EN VIVO desktop 1300px (centrado) y móvil 375px ('Agregar novedad SII' fullscreen con Volver + Guardar anclado; labels en una línea). HALLAZGO: el modal 'Registrar pago' (payingId/confirmPago) es CÓDIGO MUERTO — setPayingId nunca se dispara para abrir (marcado manual desactivado, los pagos van por conciliación); NO se convirtió, se flagged para borrar (spawn_task). Cero cambios de lógica. Build verde. Regla nueva aplicada: texto de una línea (footer de la bandeja con nowrap+ellipsis).
- 2026-09-05 Deuda UI · overlays a mano → Modal fullscreenOnMobile (tanda 1). Dos overlays de contenido hechos a mano (card centrado, apretado en iPhone) convertidos al componente Modal con fullscreenOnMobile (en iPhone = pantalla completa con header 'Volver' + cuerpo scroll + footer anclado con safe-area; en escritorio = centrado igual): (1) Asistente de liquidación de Caja Chica (el 'bottom-sheet 1561' de la memoria, que en realidad era un modal a mano — el bottom-sheet real ya no existía); su header de color a mano pasó al estándar de Modal; footer 'Confirmar los listos' anclado. (2) Bandeja de envío de Facturación; footer 'N por enviar · Enviar todas' anclado. Cero cambios de lógica. Build verde. Mecanismo verificado en demo móvil (la página 'Facturación electrónica' usa el mismo Modal fullscreenOnMobile: 'Volver' + fullscreen). Los dos modales convertidos no se abren en demo (Asistente = Caja chica role-gated; bandeja = 0 facturas por enviar) → verificación visual directa pendiente en prod, pero usan el mismo mecanismo probado. NO tocado: siiPanel (grande/anidado, riesgo alto) y los dropdowns pequeños (menús categoría/⋯, correctos como están). El punto Gmail-scope de la deuda no es UI construible (re-login de cada usuario), queda anotado. Quedan overlays de contenido por convertir en tandas siguientes (siiPanel, Registrar pago, addOpen novedad SII, revSug, etc.).
- 2026-09-05 Facturación · pase de diseño al patrón de casa (tras mapeo honesto; la mayor parte ya estaba en patrón, se tocó solo lo desviado). ANTES un fix aparte: crash al abrir "Cobertura SII" (fmtM sin definir, commit 351f2b4). Tandas: (A) QUITAR LA FRANJA DE COLOR de las filas de factura (fila 'Por cliente' + filaAll 'Todas': franja redundante, ya tenían SIcon → removida; renderClientGroup por-estado: la franja era el único indicador → reemplazada por SIcon del canon estadoCobro(b).icon). borderRadius de estría → 8 normal. (B) DINGBATS ✓→SIcon check en Facturación (checklist 'Todo emitido/Todas enviadas/ya enviada', CoberturaSII 'Todas con cliente', bandeja 'Todo enviado', detalle 'enviado el'); el ✦ (marcador de sugerencia IA) se DEJA — no hay SIcon equivalente y es un glifo intencional consistente. (C) COTEJAR-SII: la fila de cotejo pasó a CLIENTE PROTAGONISTA (receptor_name/sugerido) + SIcon file + folio/fecha/RUT al subtítulo + monto a la derecha (antes protagonista=folio); se quitó el párrafo-disclaimer al pie (queda el botón 'Cuadrar con SII'). SIN AÑO: la cifra salió del subtítulo a la DERECHA + protagonista accent + SIcon clock. (D) Checklist 'Por emitir': + día-grande (bigDate) como sus hermanas 'Por enviar'/'Emitidas' (quitada la línea 'Devengo' duplicada). NO tocado: Anticipos cabecera colapsable (pocos grupos, siempre-abierto aceptable; el colapso del canon es para listas largas). Cero cifras nuevas. Verificado en demo: 'Todas' sin franja con SIcon + cifra derecha; checklist 'Por emitir' con '1 sep 26 · Transportes Volcán · $26.000.000'. Build verde, consola limpia.
- 2026-09-05 Listas planas → patrón de casa (marginales): alineadas las 2 marginales que quedaban al patrón de fila (ícono/protagonista/cifra derecha/chevron). (a) Gastos·Descuadres (14134): + SIcon alert al inicio, nombre a w700, subtítulo a C.done, monto rojo tabular a la derecha (ya tenía chevron+expand). (b) Inteligencia·Servicios→clientes (4143): la sub-lista pasó a protagonista (nombre accent w600) + UF a la derecha + chevron ›. Horas·por cliente (23808) NO se tocó: es un popover de día que ya cumple (nombre+glosa+horas derecha+clickeable). Cero cifras nuevas. Verificado por texto en demo (Servicios: 'Inversiones Ríofrío Ltda / UF 2.700 / ›'), build verde. Con esto quedan alineadas todas las listas que valía la pena; las 6 "ya en patrón" siguen intactas.
- 2026-09-05 Listas planas → patrón de casa (2 filas, tras revisión celosa). Revisión al detalle de las 12 "listas planas" que marcó el agente: la mayoría YA estaba en patrón (borde/chip de estado, RS, aging, avatar+monto+expand) — el agente sobre-marcó. Solo 2 valían polish real, llevadas al patrón ACTUAL (el de Conciliación de esta semana): ícono/dot de estado + protagonista (nombre accent) + subtítulo gris + CIFRA A LA DERECHA (negrita, color, completa) + chevron/acción; todo el row clickeable; SIN franja de color a la izquierda (ese detalle ya no se usa). (1) Inteligencia · Oportunidades (OPPS): la fila pasó de "nombre + métrica-junta" a dot(o.col) + nombre + contexto por fila (sin actividad hace Nm / solo <área> / saldo vencido…) + val a la derecha + ›. Se separó metric en val()+ctx() por cada opp (metric se conserva para el resumen IA). (2) Gastos · Rendiciones +90d: de "$monto·días·gastos" mezclado en el subtítulo a SIcon alert + nombre (→ficha) + "N gastos sin rendir · más antiguo Nd" + cifra a la derecha con "Rendir ›" debajo (fila tapeable = rendir). Cero cifras nuevas. Verificado en demo (Oportunidades: 'Tech Araucanía SpA / saldo vencido / $4.000.000 / ›'; Rendiciones: 'Comercial Andes SpA / 1 gasto sin rendir · más antiguo 179 d / $320.000 / Rendir ›'), build verde, consola limpia. Las 6 "ya en patrón" y 3 marginales NO se tocaron (trabajo sin ganancia). NO se tocó "Cobrada"→"Pagada" (muchas son correctas).
- 2026-09-05 Auditoría #5 · wording (tanda 3): completa RS en encabezados + Copyable de folio. RS: bandeja de envío (fila ahora muestra RS del receptor + folio como Copyable) y checklist "Por enviar" (header de grupo muestra la RS junto al cliente). COPYABLE de folio: en la bandeja de envío y en las filas de la lista principal de Facturación (el folio copia-al-tocar; Copyable ya hace stopPropagation, así que en la fila que expande el folio copia sin togglear). NO tocado: "Resumen de ficha" ya agrupa Cobros pendientes POR RS (openRS) → ya tenía RS. Folio Copyable en más sitios de ficha queda disponible si se pide. Solo presentación; build verde. Con esto #5 cubre los sitios nombrados por la auditoría (queda pendiente solo dingbats ✓/✦→SIcon, que el usuario no pidió).
- 2026-09-05 Auditoría #5 · wording (tanda 2): párrafos-disclaimer + RS en encabezados + Copyable en folio (revisado 1 a 1 con el usuario). DISCLAIMERS (canon 'confianza por diseño', recortados no borrados si eran útiles): (a) 'Sin año' → 1 línea ('Facturas pagadas sin enlazar a su venta — asigna la venta o el año (se aprende por cliente)'); (b) cabecera de Anticipos QUITADA (duplicaba el aviso accionable por-fila que sí se conserva); (c) footer 'Por socio' → 1 línea ('Facturado = emitido (DTE) · Entró a caja = conciliado en banco · Neto = menos comisiones'). MANTENIDOS: el texto del interruptor 'Cobranza automática' (es funcional, no disclaimer) y el pie del PDF de Estado de resultados. RS EN ENCABEZADOS: 'Registrar pago' (modal de pago) ahora muestra la RS del receptor (rsLabel) junto al cliente + el folio como Copyable (copiar-al-tocar); 'Duplicados' muestra la RS/N° de RS junto al cliente. COPYABLE: folio de factura copiable en el modal Registrar pago (RUT/N° op ya lo tenían). Solo presentación; build verde; Facturación renderiza sin crash. PENDIENTE de #5 (barridos multi-sitio, se pueden seguir): Copyable de folio en las filas de la lista/ficha; RS en bandeja de envío y checklist 'Por enviar' y Resumen de ficha; dingbats ✓/✦→SIcon (el usuario NO lo pidió en esta ronda); 'Cobrada'→'Pagada' NO se toca (muchas 'cobrada' son correctas: una factura al cliente se cobra).
- 2026-09-05 Auditoría #5 · wording (tanda 1, segura): "aplicado"→"imputado" donde el resto de Conciliación ya usa el verbo "imputar" — "Abonos aplicados"→"Abonos imputados" (evidencia de DepurarCobradas) y "Pago parcial aplicado"→"Pago parcial imputado" (aviso de conciliarPago). Sin cambio de lógica. El resto de #5 (Cobrada vs Pagada donde de verdad aplique —OJO: muchas "cobrada" son correctas porque una factura al cliente se COBRA, distinto de pagar a proveedor—, Copyable en folios, RS en encabezados, quitar párrafos-disclaimer, dingbats ✓/✦→SIcon) queda como tanda a revisar con el usuario por ser texto visible/subjetivo. Build verde.
- 2026-09-05 Auditoría #2 · alinear los 2 motores de conciliación (parte segura/aditiva). Al marcar una factura Pagada, `conciliarPago` (motor de Facturación: Cierre de mes + botones "Conciliar pago") escribía menos que `persistPagoFactura` (motor de Conciliación): faltaban paid/paid_amount/payment_method/payment_ref/reconciled_at en billing y created_by en la fila conciliacion. Ahora, al CUBRIR el saldo, `conciliarPago` pasa esa metadata por el `extra` de onStatusChange (mismos campos y valores que persistPagoFactura: paid_amount=amount, payment_method='Transferencia', payment_ref=n_operacion, reconciled_at=now) y registra created_by en la fila (update resiliente, igual que marcaQuien). CAMBIO ADITIVO: rellena columnas que quedaban nulas; el saldo ya queda 0 al cubrir (respaldo), ninguna cifra calculada cambia. El flip de comisiones ya estaba cubierto por ambos caminos (handleConciliarTerceros / onFacturaPagada, auditoría #1). AVISO (verificar en prod, camino de pago): conciliar un pago desde Facturación y confirmar que la factura queda con reconciled_at/paid_amount/payment_method como por Conciliación. PENDIENTE / PAUSA de cifra (no ejecutado): el umbral "cubierta" de persistPagoFactura/marcaPago usa factura.amount (programado) en vez de montoFactura (DTE); por la regla saldo=DTE debería ser montoFactura, pero difieren solo cuando el DTE emitido ≠ el programado (variación UF) y cambia CUÁNDO una factura pasa a Pagada → decisión de cifra a confirmar y verificar contra datos reales antes de tocar. Build verde.
- 2026-09-05 Auditoría (ancho de escritorio) · Horas — grillas de 2 columnas (versión segura, elegida por el usuario). ESCRITORIO (rama isDesktop): las listas de tarjetas de Horas fluyen en grid 1fr 1fr con columnGap 8 (row-gap 0 → el ritmo vertical lo da el marginBottom de cada tarjeta = idéntico al móvil): (1) Equipo · Carga (tarjetas de meta por persona), (2) Rentabilidad · por cliente (margenData), (3) Rentabilidad · por abogado (margenAbogado), (4) Rentabilidad · YTD asesorías (ytdData), (5) Mis horas → Asesorías permanentes (consumo del mes). Los heros (Margen real, Podrías haber facturado), las notas de holgura y el flujo de registro de Mis horas (cronómetro, Mi semana, Por registrar, Registrar tiempo) quedan a lo ancho / en columna. NO se hizo el 2-panel completo de 'Mis horas' del mock: al estudiarlo es un bloque de ~200 líneas entrelazado (cronómetro/Mi semana/Por registrar IA/Registrar/Retainer/recientes) cuyo reordenamiento arriesgaba el móvil de la vista más compleja, para una feature con adopción 0 — el usuario eligió la versión segura de grillas. Cero cambios de lógica ni cifras. Verificado en demo escritorio 1300px (Equipo 2 col, Rentabilidad clientes/abogados 2 col, Asesorías permanentes 2 col) y móvil 375px (todo apilado, idéntico), build verde, consola limpia. Con esto cierra la barrida de ancho de escritorio en las vistas standalone del patrón 1.
- 2026-09-05 Auditoría (ancho de escritorio) · Repricing en grilla de 2 columnas + rediseño de tarjeta. ESCRITORIO (rama isDesktop): las recomendaciones fluyen en grid 1fr 1fr (gap 12, alignItems:start), hero "Reajuste potencial" y header a lo ancho; contenedor 900→1000px. REDISEÑO DE TARJETA (móvil y escritorio, mejora pedida por el usuario): el honorario dejó de ir en una línea (hoy → nuevo) y pasó a una COMPARACIÓN en columnas — 'Honorario hoy' (mutado) vs 'Propuesto' (resaltado azulBg) — alineada y comparable de un vistazo entre tarjetas; el consumo/plan/tarifa pasan a MINI-STATS etiquetados y escaneables (antes un párrafo); el nombre del cliente es clickeable (accent + '›' → ficha); el chip de delta muestra '+X% · +$/UF' (porcentaje y monto absoluto, en la unidad de la venta). BOTONES proporcionados (igual alto 34px y padding): Descartar (enlace tenue, margin-right auto) · Ver carta · Enviar. WORDING pulido: 'Preparar carta'→'Ver carta', 'Enviar al cliente'→'Enviar', hero 'rinden bajo su consumo'→'cobran menos de lo que consumen'. Cero cambios de lógica ni cifras (recos/cartaInner/verCarta/enviarCarta/descartar intactos; deltaStr nuevo solo para mostrar). Verificado en demo escritorio 1300px y móvil 375px (Tech Araucanía: hoy $900.000 vs propuesto $1.226.411, +33% · +$306.603), build verde.
- 2026-09-05 Auditoría (ancho de escritorio) · Cartera (Proyectos) en grilla de 2 columnas. ESCRITORIO (rama isDesktop): el listado de proyectos (agrupados por cliente) deja de ser columna única y fluye en grid 1fr 1fr (gap 12, alignItems:start) → se ven el doble de proyectos sin scroll. Al expandir una tarjeta (openId), su detalle rico (margen honorarios/terceros/esfuerzo, tareas, bitácora, acciones) ocupa las 2 columnas (gridColumn '1 / -1') para leerse cómodo — misma interacción de expandir-en-línea de hoy, sin extraer nada. Span: tarjeta de proyecto único cuando openId===su id; grupo de cliente cuando alguno de sus proyectos está abierto. Encabezado, pestañas (En curso/En pausa/Terminados) y filtros ya iban a lo ancho. MÓVIL: idéntico (columna única, expandir en línea) — el contenedor es isDesktop? grid : flex-column original y gridColumn va gateado a isDesktop, así que en móvil todo resuelve al valor original byte-a-byte. Reusa grupos/renderProyecto/openId, cero cambio de lógica ni cifras. Verificado en demo escritorio 1300px (5 proyectos en 2 col; al expandir Comercial Andes el detalle Etapa/Plazo/Tareas ocupa el ancho), build verde, consola limpia. NOTA: Inteligencia queda pendiente del patrón 1 pero con menor prioridad — su índice-hub YA es grilla de 3 columnas, el gap de escritorio es menor.
- 2026-09-05 Auditoría (ancho de escritorio) · Ventas en 2 columnas (maestro-detalle). ESCRITORIO (rama isDesktop): el encabezado (título, buscador, filtros, tiles Vendido/Propuestas) queda a todo el ancho; debajo un grid 430px / 1fr — a la IZQUIERDA el desglose (tabla por Responsable/Área con Ventas·UF·$·%), a la DERECHA las ventas del grupo elegido (selGroup → sus saleRow). Sin grupo elegido, el panel derecho invita a elegir uno ("Elige un responsable o área…"). Al buscar o filtrar por estado ≠ Activo (flatView) no hay desglose: la lista plana ocupa el ancho, capada a 720px para que las tarjetas no se estiren. MÓVIL: idéntico a antes (tabla y, al tocar un grupo, su detalle debajo) — garantizado porque el arreglo 2-col vive solo en la rama isDesktop; cada estilo es isDesktop? nuevo : original. Implementación: el bloque desglose se envolvió en un IIFE que arma _tbl (banner tardías + header + tabla) y _drill (detalle del grupo) como consts y ramifica grid (escritorio) vs apilado (móvil), reusando TODA la lógica y cifras (grupos, selGroup, saleRow, ventaUF/ventaCLP fuente única) — cero duplicación. Contenedor 1040→1180px. Verificado en demo: escritorio 1300px (2 col, invite → al tocar Cristóbal aparecen sus 5 ventas a la derecha), móvil 375px (tabla apilada + drill debajo intacto), build verde, consola sin errores propios.
- 2026-09-05 Auditoría #1 (ancho de escritorio) · Tareas en 2 columnas (la vista más usada). ESCRITORIO (rama isDesktop): grid 1fr / 330px — a la izquierda la LISTA de tareas (Mis tareas · buscador · + Nueva tarea, con Activas / Que asigné / Terminadas / Archivadas), a la derecha un RAIL de contexto siempre a la vista: nudge de caja chica + tablero de KPIs (foco vencidas/semana/al día + chips por persona + Activas/Que asigné/Terminadas + sugeridas de Gmail, apilado en vertical dentro del rail) + calendario "Próximas semanas" (2 semanas) + "Resumen financiero" (saldo de caja + por liquidar + últimos gastos). MÓVIL: idéntico a antes (mismo orden nudge→hero→lista→calendario→resumen, hero horizontal), garantizado byte-a-byte porque cada estilo ramificado es isDesktop? nuevo : original. Implementación: los 5 bloques del render se extrajeron a consts JSX (_nudge/_hero/_list/_cal/_fin) + _overlays (tooltip hover + modal preview), y el return ramifica grid (escritorio) vs apilado (móvil) reusando TODA la lógica (mias/asignadas/terminadas, KPIs, semanaOffset, sug Gmail, nudges) — cero duplicación de datos. Verificado en demo: escritorio 1300px (2 columnas: lista izq, rail der con KPIs→calendario→resumen), móvil 375px (apilado intacto, hero horizontal), build verde, consola sin errores propios. Molde reusable para el resto del patrón 1 (Cartera, Inteligencia/Ventas, Facturación).
- 2026-09-05 Auditoría #5 · Cobranza escritorio (tabla) + compuerta de envío + interruptor global automático. (1) ESCRITORIO: nueva rama isDesktop = tabla ordenable (Cliente/Nivel/Facturas/Mora/Último contacto/Monto/Acción; sort por monto·mora·cliente·nivel con setSortBy), fila expandible a las facturas del cliente (folio·vence·vencida·saldoBill), acciones a la derecha (Enviar / Liberar → / En automático·Pausar). Reusa grupos/cobranzaAccion/saldoBill; móvil = las tarjetas de siempre (intacto, verificado 375px). Contenedor a 1040px. (2) COMPUERTA: enviarCliente ahora pide appConfirm ('se envía como tú', con cliente·N facturas·monto·correo) salvo desde 'Enviar a todos' (que ya confirma el lote) — antes el envío individual mandaba correo sin confirmar. (3) AUTOMÁTICO: interruptor global 'Cobranza automática' (config cobranza_auto vía learnings, el que lee el edge fn cobranza-auto) con toggle + explicación; antes 'Liberar automático' por cliente quedaba inerte porque el switch global no tenía UI. Verificado en demo (escritorio: tabla, sort, expandir, toggle On/Off; móvil: tarjetas + toggle), build verde, consola limpia. Cifras completas, monto al final.
- 2026-09-05 Auditoría #4 · ancho de escritorio (patrón 1), tanda 1 — las 2 vistas de uso diario de limitados que se estiraban full-width. CajaChicaView (feed/form → molde columna centrada) y ClientsViewLimited (lista + ficha) ahora van en columna centrada en escritorio (maxWidth 820/900 + margin:auto en el root); móvil intacto (ancho < maxWidth = sin efecto). Cambio CSS puro (wrapper), sin tocar lógica ni cifras; build verde. OJO: ambas son role-gated (admin no las ve en demo — ClientsView admin ya es 2-panel; Caja chica no está en el nav admin) → verificación VISUAL pendiente con login de usuario limitado. Premium a futuro: 2-panel maestro-detalle para Clientes-limitados (espejo del admin). Pendientes del patrón 1 (admin-visibles, se pueden verificar en demo): Cobranza (tabla ordenable), Tareas (maestro-detalle), Cartera (rail+detalle), Inteligencia/Ventas (multicolumna), y en Facturación checklist/anticipos/cotejar-SII.
- 2026-09-05 Auditoría #3 · cifras inline que divergían → helper único (solo display, sin tocar datos). Antes varias vistas sumaban b.amount (monto programado) en vez de saldoBill/montoFactura → subtotales que no cuadraban con su total. Corregido: (a) renderClientGroup (Proveedores/Rechazadas): total de grupo + subtotal por entidad + monto de fila + "Monto" del detalle → montoFactura (autoridad DTE; subtotal = Σ filas); (b) EstadoCuentaTab (ficha·Cartola): saldo por-RS (pend) → saldoBill, "Monto" → montoFactura, "Saldo" → saldoBill; (c) FinancieroTab "Cobros pendientes": subtotal por-RS + monto de fila → saldoBill (ahora suman al header, que ya usaba porCobrarBills); (d) Copiloto "Por cobrar" → porCobrarBills (antes b.amount + !paid_at ignoraba parciales/NC); (e) CobradasSinRespaldoModal: la "dif" usaba fac.amount mientras "exacto" usa montoFactura → alineada a montoFactura. Inteligencia y el KPI 'pending' ya usaban saldoBill (no se tocaron). Verificado: build verde, demo sin crash (Clientes 2-panel, Ficha Resumen/Cartola, Ventas, Facturación), consola limpia. Pendiente del patrón 3: agrupación por receptor_name crudo sin cruce de RUT (va en la tanda de RS/wording), y la posible duplicación 'Cobros pendientes' vs FinancieroTab (decisión de diseño).
- 2026-09-05 Auditoría #2 · barrido de código muerto (~265 líneas, cero cambio funcional). Cada borrado verificado (grep 1/1 o rama inalcanzable) antes de tocar; build verde tras cada tanda; demo sin crash (Inicio/Facturación/Gastos/Clientes render, consola limpia). Removido: (a) rama `filter==='emitidas'` de Facturación (~83 líneas, diseño viejo "Pendiente pago/Por facturar" que ningún setFilter alcanza — ternario empalmado clientes→all); (b) panel "Salud de cobranza" (~82 líneas, nunca abría porque onIrCobranza siempre gana; duplicaba CobranzaView) + tile Cobranza simplificado a onIrCobranza; (c) bloque `{false&&…}` de la lista vieja de Gastos (~40 líneas); (d) `ClientRow` de ClientsViewLimited (35 líneas, nunca renderizado); (e) `pagar` de FinancieroTab (nunca llamado) + su comentario; (f) modal huérfano `type==='costosOficion'` (sin trigger; el tab presupuestoOficina sigue); (g) 10 useState muertos del Dashboard (gaugeMode/dgl/hoyOpen/convOpen/cobLens/openCobranza/cajaExp/openOficina/verTareasEquipo/mesOficina) + vistaCalendario/semanaLabel de Tareas. OJO metodología: el agente había marcado revOpen y varias vars de calendario de Tareas como muertas, pero grep mostró que se usan (4-5×) → NO se tocaron (verificar antes de borrar). Dejados como deuda menor (inocuos, no rompen): miembros del destructure gigante que quedaron sin uso (emitidasTotal/verTodos/saludCobranza state…), el condicional `isProg&&` del toolbar (isProg nunca true), y `estadoCuentaEnviar` (feature a migrar a CobranzaView).
- 2026-09-05 Auditoría #1 (CRÍTICO $) · el flip de comisiones ahora SÍ se dispara al cobrar por banco. Antes, al marcar una factura Pagada por conciliación (la vía real), las cuentas a proveedor de esa factura quedaban en 'pendiente' en vez de 'por_pagar' → Cuentas por Pagar subvaloradas (el único disparador, confirmPago, es un camino muerto). Fix en los DOS motores, vía el helper idempotente handleConciliarTerceros (solo toca 'pendiente'→'por_pagar'): (1) handleStatusChange dispara el flip cuando la factura queda 'Pagado' (cubre Facturación/'Buscar pago'→conciliarPago→onStatusChange); (2) persistPagoFactura (Conciliación) lo dispara al transicionar a Pagado (cubre las 4 variantes reconciliar/combo/grupo/reembolso, un solo punto), vía nuevo prop onFacturaPagada={handleConciliarTerceros} threado por ConciliacionView/useConciliacionModel; (3) handleDepurarCobradas también flipa (facturas ya saldadas). handleRevertirPago ya revertía por_pagar→pendiente (simetría). Verificado en demo: build verde, Facturación/Conciliación/Repricing renderizan sin crash, consola limpia. AVISO CIFRAS (probar en prod antes de confiar): conciliar una factura con un tercero 'pendiente' y confirmar que pasa a 'por_pagar' y que Cuentas por Pagar sube correcto; el back-fill de facturas ya pagadas antes de este cambio se puede correr con Depurar. NO se tocó el umbral DTE-vs-amount de marcaPago/cubierta (queda para la tanda de cifras, auditoría paso 3).
- 2026-09-04 Facturación · Facturación electrónica → página (paso 3): el bloque SII que crecía inline en la landing ahora es UNA tarjeta clickeable (icono + 'Emisión electrónica · SII' + pill de estado + ›); al tocar abre su propia página (Modal fullscreenOnMobile) con todo el contenido que antes estaba abierto (folios/CAF, últimas emitidas o checklist de certificación, y los botones Verificar estados / Enviar resumen / Probar conexión / Set de pruebas / Libro de ventas / Historial). Estado nuevo siiPageOpen. Sin cambios de lógica SII. Verificado en demo: 1 sola tarjeta en la landing, abre el modal con el detalle, recarga limpia; build verde. Con esto cierran los 3 pasos del rediseño de Facturación.
- 2026-09-04 Facturación · landing rediseñada (canon de la foto) + vista "Por socio" (pasos 1 y 2): (LANDING) la higiene ("Ya emitidas·vincular", "Pagadas sin marcar", "Sin año", "Por enviar", "DTE rechazadas") dejó de ser KPIs falsos / alertas sueltas y se juntó en una tira "Por revisar" (cada fila lleva a su lugar: vincular→Cotejo SII setSiiOpen, enviar→bandeja, etc.). "Cobranza" ahora abre su vista propia (onIrCobranza→setTab('cobranza')) en vez del panel Salud paralelo. Tiles "a mano" = Anticipos + Cobranza. (POR SOCIO) puerta discreta sin cifras a la vista ("Por socio · Ver →", onOpenPorSocio→modal) — no scoreboard socio-vs-socio en la landing; la vista es admin-only. (PÁGINA PorSocioModal) mes con flechas ◀▶ (▶ apagado en mes en curso) + acumulado YTD (enero→mes) + Exportar CSV; por socio Facturado (emitidas por issued_at × montoFactura) y Entró a caja (misma base que Inicio: conciliacion.monto_aplicado por fecha del depósito, vía fetch propio de conciliacion+cartola). Atribución socio con respDe (venta→responsible, si no cliente→abogado_responsable); no-socios→'Otros'. Neto = bruto − comisiones terceros (facturado: devengadas de esas facturas por billing_id; caja: pagadas ese mes por pagado_at). Cifras COMPLETAS en CLP. Verificado en demo: landing (puerta, Por revisar, Cobranza), y la página (Julio: Erasmo caja $40.000.000 atribuido, flechas, YTD, sin crash). PENDIENTE (paso 3): mover Facturación electrónica a su propia página con tarjeta de entrada. AVISO CIFRAS: fórmulas nuevas (facturado/caja/neto por socio) — revisar contra datos reales en prod antes de confiar (el demo no tiene atribución completa).
- 2026-09-04 Conciliación · hub/rail money-first + cierra asimetrías: (1) 'Identificar por su nombre' (la app ya sabe de quién es → modal RevSug) ahora se ofrece en el hub móvil Y en el rail de escritorio (antes solo en el overview de Abonos, invisible en escritorio por el foco forzado 'porresolver'); aparece cuando sugeridosId>0, con su $monto (sugMonto). (2) Cargos por clasificar visible desde el hub/rail: el tile 'Cargos' muestra 'N por clasificar · $monto' (ámbar) cuando ccN>0 y el tap entra a Cargos con el filtro 'clasificar' puesto (antes la foto solo vivía dentro de Cargos). (3) Money-first (regla del usuario 'mejor $$$ que recuentos'): las filas de 'Por resolver' (prSub) en hub y rail ahora muestran el $monto como figura (conteo movido a texto 'N mov'), no el número de movimientos. Fuente ÚNICA nueva en el cuerpo del componente: sugMonto, cargoClasifMovs/ccN/ccMonto y prSub[].m (montos por sub-tipo) — reusados por hub, rail e interior (la foto 'Por clasificar' ahora lee ccN/ccMonto). Verificado en demo móvil (hub) y escritorio (rail 1300px): Cargos '1 por clasificar · $450.000', Identificar '$4.000.000', prSub '$5.177.309'; consola sin errores propios.
- 2026-09-04 Conciliación · pulido menor: (1) píldora de ANTIGÜEDAD por fila — cada abono por-resolver cerrado (sin cruzar) con +30 días muestra 'N días' / '+90 días' junto al chip de estado, color por tramo (soon <90, overdue >90); reusa el mismo predicado de abonoInlineAcc, gateado a por-resolver (no aparece en conciliados/internos). (2) factResto ('cubre + sobra') enriquecido: cada opción muestra RS (receptor_name) + 'emitida DD/MM/AAAA · vence DD/MM/AAAA' (antes solo folio · saldo · mes), al nivel de las demás filas ricas. NO se tocaron 'Sin respaldo' (CobradasSinRespaldoModal ya cruza abono↔factura con candsFor/Deposito + botón Conciliar) ni las filas del lote (ya ricas: Transferencia/Factura con RUT/saldo/emitida-vence) — ya estaban al día, no se duplicó. Verificado en demo (píldora '61 días' en Comercial Andes; conciliadas sin píldora), consola sin errores de mi código.
- 2026-09-04 Conciliación · interiores pulidos (lista + foto) + regla de cifras COMPLETAS: (1) es la fuente de la verdad → se cambió fmtShort (abreviado '$8,3M') por fmtM (completo '$5.177.309') en el hero 'Por resolver' del hub y del rail, el '+90 días', el header de foco ('N mov · $sum') y el resumen de Cartolas ('+$68.577.309 · −$450.000'). (2) Overview de Abonos = tira DELGADA de triage (Por resolver / Identificar por su nombre / Facturas sin respaldo, cada una count + bajada + verbo → su lista), reemplaza los tiles+cards de colores que duplicaban el hub/rail; 'Todo conciliado' cuando no hay nada. (3) Cargos = foto 'Por clasificar' arriba (simétrica al Por resolver: count + $monto completo al final; toca para filtrar concView='clasificar' → cargos sin categoría; cada fila ya trae su clasificación inline) + 'Valores del mes (contadora)' REPLEGADO (colapsable, herramienta secundaria). Regla de fila: el monto siempre al final, el mensaje/estado antes. Verificado en demo escritorio (2-panel, cifras completas, foto Por clasificar 1·$450.000, toggle) y móvil 375 (hub + overview triage + cartolas completas); consola sin errores de mi código.
- 2026-09-04 Conciliación · ESCRITORIO maestro-detalle (2-panel), cierra la deuda de doble formato: en escritorio ya no hay pantalla-hub separada — un efecto colapsa hubOpen y muestra rail (296px) a la izquierda + la lista/detalle llenando el ancho a la derecha (mata el aire). Rail = Abonos/Cargos + 'Por resolver' con subsección (Abonos sin cliente asignado / Cobros identificados / Abonos sin factura asociada, la activa resaltada) + Sin respaldo/Cargar + Exportar; cada línea setSub/setConcView → cambia el panel derecho. Se levantaron a fuente ÚNICA (cuerpo del componente) las cifras de Por resolver (porResolverMovs/prN/prMonto/prSinId/prConf/prDesc/pr90/prSub) y exportConc — usadas por el hub móvil Y el rail (se quitaron los duplicados del concHub). MÓVIL intacto: if(hubOpen && !isDesktop) return concHub (hub de una columna). Verificado en demo: escritorio 2-panel (rail + Por resolver accionable), móvil 375 hub de columna; consola limpia.
- 2026-09-04 Conciliación · hub 'Por resolver' → SUBSECCIÓN C1 (monocromo rojo): se quitó el rótulo 'TRABAJO' y las chips telegráficas; en su lugar una mini-lista con frase contable completa por estado + su verbo: '11 · Abonos sin cliente asignado — no sabemos de quién es el ingreso — Asignar'; '1 · Cobros identificados — calzan con una factura emitida · por registrar — Conciliar'; '4 · Abonos sin factura asociada — anticipo, fondo o pago parcial — Clasificar'. Filas blancas sobre el rojo (se levantan), número en overdueText, cada fila filtra a su sub-tipo (sinid/porconciliar/descalces) con stopPropagation; tocar el cuerpo de la tarjeta va a 'porresolver'. Wording en plural y paralelo (Abonos con/sin…). Verificado en demo (fila 'Abonos sin cliente asignado').
- 2026-09-04 Conciliación · 'Por resolver' ACCIONABLE + wording: (wording A) chips del hub y switcher → 'sin cliente / por confirmar / sin factura' (antes 'falta cliente/confirmar'). (accionable) cada abono por-resolver en su fila CERRADA muestra su acción inline según etapa (abonoInlineAcc): sin cliente con sugerencia → 'Es X ✓' (identificar) + 'Otro ›'; sin cliente sin sugerencia → 'Asignar cliente' + 'o fondo: equipo ▾' (crearFondoPersonal); por confirmar (tiene candidato) → 'Conciliar N°X' (reconciliar) + 'Ver ›'; sin factura/descalce → 'Imputar a N°X' parcial FIFO (reconciliar) + 'Otra opción ›'; el buscador queda detrás de 'Asignar'/'Otro' (abre la fila, no desaparece). Reusa sugMov/identificar/crearFondoPersonal/reconciliar/mejorCandidato/facturasParaMov; gateado !abierto + por-resolver; el estado chip pasivo queda como tag. Verificado en demo (sin cliente: Asignar / Es X ✓), consola limpia. Por confirmar/descalce sólo se ven con datos reales (prod).
- 2026-09-04 Conciliación · HUB rediseñado (taxonomía nueva, estudio de fondo): se reemplazó la foto '5A' (Por conciliar + antigüedad + KPIs vanidad Cobertura/Resueltos/RUT/Conciliado) y las 6 tarjetas paralelas por estructura A: (1) Abonos / Cargos con ícono ('N · lo que entró' / 'lo que salió'), SIN cifras de vanidad; (2) UNA tarjeta 'Por resolver' = unión sin identificar+por conciliar+descalces (concView='porresolver' nuevo, filtro union en lista), con $monto, '+90 días' si aplica, y chips clickeables (falta cliente/confirmar/sin factura) que llevan al sub-tipo; (3) 'Sin respaldo' (Facturas sin banco) y 'Cargar cartola' chicas aparte; Exportar CSV. Se acabó la duplicación (Sin identificar/Descalces ya no son tarjetas hermanas de Abonos). El switcher del interior ahora: 'Por resolver · Falta cliente · Confirmar · Sin factura · Conciliados'. FOCOS + focoKey soportan 'porresolver'. Verificado en demo (desktop + móvil): hub, Por resolver interior, switcher, sin errores. Motivo: 'Por conciliar/Sin identificar/Descalces/Sin respaldo' eran conceptos paralelos que confundían; son etapas de un abono (Sin identificar→Por conciliar/Descalce→Conciliado), Sin respaldo es plano factura.
- 2026-09-04 Conciliación · Auditoría FIFO (item 5): factResto ('cubre + sobra') ordena ahora por issued_at ASC (más antigua primero), dif como desempate (antes solo por dif → podía pagar una nueva antes que una vieja). Resto de listas ya FIFO (candidatos/mejorCandidato/facsShow ✓; combos usa suma exacta + cercanía de fecha, aceptable). Verificación visual pendiente.
- 2026-09-04 Conciliación · Resolver de a uno + fila (item 2, cruce): sugMov ahora cruza PRIMERO por folioGlosa — si la glosa del banco nombra un folio ('PAGO FACTURA 328'), toma esa factura (única, con saldo) y su cliente como sugerencia (via:'glosa', la señal más fuerte). Se refleja en la bandeja 'Resolver de a uno' y en la fila del detalle: badge 'la glosa: N°X' / 'La glosa nombra el folio' + detalle de la factura. Reusa folioGlosa/billing/saldoFactura; sin cifras nuevas. Verificación visual pendiente (preview niega localhost).
- 2026-09-04 Conciliación · Lote (item 1 del roadmap): (wording) 'Conciliar de una vez' → 'Listos para conciliar'; columnas 'El depósito'/'La factura' → 'Transferencia'/'Factura'. (fila rica) la transferencia suma el RUT del pagador; la factura muestra razón social (o concepto) y el pie agrega 'vence' además de 'Emitida'. (clickeable) cada fila de calce ahora es TAPEABLE (cursor+onClick → setLoteOpen(false)+setModalMov(m.id)) para revisar/ajustar ese calce, mismo patrón que las filas 'por revisar'. El botón batch 'Conciliar los N' se mantiene. Cero cambios de lógica/cifras. Verificación visual pendiente (preview niega localhost).
- 2026-09-04 Conciliación · Descalces (tanda 2): en un descalce (abono con cliente, sin calce exacto, sin conciliar → esDesc) se agrega: (A) PROTAGONISTA — si la glosa nombra un folio (folioGlosa) esa factura sube arriba ('La glosa apunta a la N°X'); si no, el PARCIAL FIFO (la más antigua con saldo > pago, no la de dif más chica); botón 'Imputar a N°X' = reconciliar(m,f) parcial. (B) CONTEXTO cruzado (opción A, filas clickeables, tinte suave, sin pills, solo lo que existe): Deuda del cliente · N facturas (suma saldos → abre estado de cuenta), Gastos por reembolsar (gastosReembolsables → abre checklist devolución con preselección), Buscar en el SII (buscarMovSII). Todo reusa helpers existentes; cero cifras nuevas; gateado por esDesc (no afecta Por conciliar/Sin identificar/conciliados). Verificación visual pendiente: preview caído + demo sin descalces (solo se ve en prod).
- 2026-09-04 Conciliación · Descalces (tanda 1): (1) FIFO — el estado de cuenta del cliente ('Otra factura') ordena la MÁS ANTIGUA primero (facsShow issued_at asc, antes descendente); rótulo 'más antigua primero'. Regla: no imputar por monto igual a la factura nueva y dejar vacíos. (2) Fila de factura RICA y clickeable: 2 líneas — N° + glosa completa (sin truncar a 26) / razón social · RUT (Copyable, con stopPropagation) · emitida · vence; a la derecha monto + estado · saldo. El clic sigue abriendo el detalle. El auto (candidatos/mejorCandidato) ya era FIFO; esto alinea la vista manual. Falta tanda 2 (contexto cruzado + bloque parcial 'Imputar a N°X'). Verificación visual pendiente (preview caído).
- 2026-09-04 Wording · los 'Aplicar' no monetarios (aplicar-en-lote) ahora dicen su verbo propio: Caja Chica 'Aplicar y seleccionar listos'→'Confirmar los listos'; Cartera 'Aplicar al proyecto'/'Aplicando…'→'Agendar al proyecto'/'Agendando…' (agenda plazos); Drive '¿Aplicar? Es reversible.'→'¿Confirmar los cambios? Es reversible.'
- 2026-09-04 Wording · 'aplicar' → 'imputar' en contexto MONETARIO (pago/factura/cuota/abono): 'saldo por aplicar'→'por imputar' (alert), '¿Aplicar X como pago PARCIAL?'→'¿Imputar…', 'Aplicar a cuotas'→'Imputar a cuotas' (anticipo), 'Aplicar:'→'Imputar:' y 'Aplicar abono'→'Imputar abono' (modal cubrir factura). NO se tocaron los 'Aplicar' de aplicar-en-lote no monetario (propuesta Drive, plazos al proyecto, sugerencias listas de Caja Chica) ni los nombres de función (aplicarCliente/Orf/Categoria/Corregir): esos ya asignan entidad con texto 'Asignar…' o significan otra cosa.
- 2026-09-04 Conciliación · Cargos 8b: la sugerencia del cargo sube a la FILA CERRADA (antes solo dentro de renderCargoClasificar, al abrir). Helper cargoInlineSug(m): (a) pago agrupado (matchGrupoPresupuesto) → 'Repartir en N' (costoOficinaSplit); (b) Gasto Oficina › categoría[›sub] con '✦ por el RUT/presupuesto/glosa' → Confirmar (registrarCargoOficina) o 'Elegir' (abre drill si falta subcategoría); (c) gasto de cliente → Confirmar (marcarCargoCliente). Solo en cargos sin clasificar (!conc && !categoria) y con la fila cerrada; al abrir queda el renderCargoClasificar completo. Reusa cargoSugerencia/matchGrupoPresupuesto y los confirm que ya aprenden por RUT — cero lógica nueva. Build verde. PENDIENTE verificación visual (el browser del preview negó navegación este turno).
- 2026-09-03 Conciliación · "La foto" (opción 5A, hub): las 2 tarjetas planas (Por conciliar / Conciliado) se reemplazan por un protagonista "Por conciliar" $montoPend con la ANTIGÜEDAD anidada (0–30 verde / 30–90 ámbar / +90 rojo, por m.fecha vs hoy; se oculta si no hay pendiente) + fila de REFERENCIA de 4 tiles: Cobertura % (done/total abonos), Resueltos por la app (conc con origen='auto', distintos movimiento_id), RUT vinculados (aliases.length), Conciliado total ($ de conciliables cruzados). Todo clickeable (→ porconciliar / conciliados). Botón "Exportar estado (CSV)": baja un CSV de abonos con Fecha/Cuenta/Nombre/RUT/Cliente/Monto/Estado/Factura (export inline, csvDownload de Gastos está fuera de scope). Cifras de fuente única (resumenConc/conc/aliases/concByMov), cero recálculo ni cifra nueva; no toca lógica de cruce. Doble formato (D=isDesktop). Verificado en demo (montoPend=0 → sin tramos, Cobertura 100% 2/2, Conciliado $63M, export sin error).
- 2026-09-03 Conciliación · "Resolver de a uno" (bandeja foco, opción 6): en el foco "Sin identificar" un botón abre una tarjeta a la vez con la mejor sugerencia adelante — botón "Es X ✓" (identificar, aprende RUT + back-fill), "Buscar otro cliente" (AsignarClienteInline), "o fondo del equipo" (crearFondoPersonal), y estado "Sin factura que calce → Buscar en el SII" (onCotejarSII) cuando no hay sugerencia. Barra de progreso "N de M" + "Saltar →" (saltados van al final) + "Salir a la lista". La lógica de sugerencia se EXTRAJO a un helper único sugMov (por nombre → por monto factura calzada → por monto única emitida) usado por la fila del detalle Y la bandeja (cero duplicación). La lista se oculta en modo bandeja (gate !unoMode). Verificado en demo: estados calza (via monto: Constructora Pehuén N°1305) y no-calza→SII (Comercial Andes), Saltar avanza, Salir vuelve; sin crash. Móvil y escritorio misma estructura.
- 2026-09-03 Conciliación · interior enfocado (opción A): al entrar a una tarjeta (Sin identificar/Descalces/Por conciliar/Cargos) el header ES el contexto (icono + nombre + 'N movimientos · $monto' de la lista visible), y NO se repiten los tiles 'Por resolver' ni la barra de cartolas (Cargar cartola es su propia tarjeta). Cambiar de foco = una línea de texto 'Ver: Por conciliar N · Sin identificar N · Descalces N · Conciliados N' (el activo en navy; tocar el activo vuelve al overview). El overview (tarjeta 'Abonos' → concView='todos') queda IDÉNTICO: header 'Conciliación bancaria' + cartolas + tiles + 3-resumen + lote (móvil piso intacto). Lote 'Conciliar de una vez' solo en overview o foco 'Por conciliar'. Nada se pierde: búsqueda/filtros/orden/candidatos/conciliar iguales; todo clickeable. Gateado solo por estado 'focused' (sin ramas isDesktop → móvil y escritorio misma estructura). Verificado a 1280px en demo (Sin identificar, Cargos, overview) sin crash.
- 2026-09-03 Fix (scope): CobradasSinRespaldoModal ('Sin respaldo') referenciaba onOpenClientFicha sin recibirlo en props → crash al abrir la tarjeta. Se agregó al destructuring (el caller ya lo pasaba). Bug preexistente, no del cambio anterior.
- 2026-09-03 Conciliación hub: tarjeta 'Abonos' (simétrica a Cargos) — N° y suma de abonos (G.nAbo/sumAbo), verde, → sub abonos·todos. Reordenado: Abonos + Cargos primero, luego Sin identificar/Descalces/Cargar cartola/Sin respaldo. Se quitó 'Todos los movimientos' (redundante: Abonos+Cargos=todos, y mostraba '1000 en total' pero solo abonos).
- 2026-09-03 Correo de facturas · cierre: después de 'Quedamos atentos a sus comentarios.' se agrega línea en blanco + 'Saludos,' y debajo el nombre de quien envía. En HTML 'Saludos,' va justo sobre la firma (que trae el nombre como primera línea) → el nombre queda debajo; en texto plano cierra con 'Saludos,\n<nombre>' (firma.nombre o user.name). Idioma respetado (Best regards, en inglés).
- 2026-09-03 Conciliación Fase 1 (tarjeta de conciliar, solo presentación — no toca la lógica): (1) comparación en la MISMA FILA — grilla alineada Transferencia | ✓ | Factura por atributo (Monto/Fecha/Nombre/RUT), con ✓ al medio cuando calza (monto exacto, mismo RUT); reemplaza las 2 columnas apretadas 'El depósito | La factura'. (2) encabezados Transferencia → Factura. (3) 'Conciliar ahora' se mantiene como acción principal. (4) '¿A qué corresponde?' se REPLIEGA tras un botón 'No, es otro pago ▾' cuando hay factura sugerida (reusa otrasSet/toggleOtras); si no hay calce, se muestra abierto. Detalle secundario (N° op, descripción, estado, reembolso, concepto, abonos previos, +N pagos, Dif) conservado bajo la grilla — nada se pierde. Nombre→ficha y RUT copiable intactos. FALTA: verificar visualmente en prod (demo no tiene abono con factura candidata). F2 (lista en tarjetas) y F3 (maestro-detalle) pendientes.
- 2026-09-03 Enviar facturas (compose): (1) ahora es PÁGINA (Modal fullscreen, no popup); (2) textarea del mensaje amplio (rows 11, min 210px); (3) vista previa REPLEGADA (details, se abre a demanda); (4) varias facturas ordenadas de la más antigua a la más nueva; (5) el texto calza con la cuota de cada DTE; (6) mensaje = UNA frase corta que engloba (folios + pagos + proyecto + total), reemplaza la lista larga por concepto. APRENDE tu edición: al enviar, si editaste, guarda la frase como PLANTILLA con tokens {folios}/{total} en learnings kind=factura_msg key=cliente:(n|1):idioma; al reabrir la reexpande con los folios/total actuales (una o varias, es/en). Botón '↺ texto sugerido' para regenerar la frase A. FALTA probar el envío real en prod (necesita factura con XML).
- 2026-09-03 Fondo por rendir del equipo (Alt A, dinero de un tercero para gastos personal_de una persona): (1) Conciliación — abono SIN identificar ofrece 'o fondo del equipo · [persona ▾]' (desplegable, sin pills) → crearFondoPersonal crea type='fondo' con personal_de + guarda de quién vino (nombre del banco) + fila conciliacion (sin descuadre), con confirm; reversible. (2) Gastos oficina → Personales: ahora saldo por persona = Fondos − Gastos (fondos verdes + gastos rojos en el detalle; verde 'disponible' / rojo 'le debe'). (3) 'Gasto interno' del gasto individual pasó de pills a desplegable agrupado (Oficina / Del equipo). Sin schema nuevo (expenses ya tiene personal_de; client_id null → no toca saldo de clientes). FALTA: probar la creación real del fondo en prod (dinero).
- 2026-09-03 Facturación · fix XML de facturas ya foliadas (caso factura de anticipo 439 que no aparecía en 'Por enviar'): (B1) al cotejar, si la factura YA tiene folio (ej. viene de un adelanto) ahora se le ADJUNTA el dte_xml del staging si le falta —antes solo la enlazaba y el XML quedaba huérfano→ invisible en 'Por enviar'—; (B2) el match por folio normaliza ambos lados (folioN) como la rama de nota de crédito; (B3) 'Por enviar' ahora avisa las emitidas del mes SIN XML con su folio ('N emitidas sin XML — cargar su XML'), en vez de esconderlas. Además se re-pegó a mano el XML de la 439 (A).
- 2026-09-03 Cross-linking de respaldos: el panel de adjuntos (attachExpense) ahora abre con un encabezado 'Respaldo de' que muestra el origen (glosa · monto · cliente/personal · fecha) y es clickeable → ficha del cliente (onOpenClientFicha, salvo oficina). El adjunto deja de ser callejón sin salida. Título del modal → 'Respaldos'. No se tocó el componente Attachments (subir/ver/borrar intactos).
- 2026-09-03 Razón social accionable: la RS del header de la ficha de Gastos (texto plano) ahora es clickeable → abre la ficha completa del cliente (onOpenClientFicha), con stopPropagation y color accent. Decisión del usuario: RS → ficha del cliente. (Spot principal; en listas de facturación el nombre del cliente ya abre la ficha, RS ahí sería redundante.)
- 2026-09-03 Gasto interno en el gasto individual: el ExpenseEditForm ya tenía chips 'Personal de:' pero SIN entrada y sin 'Oficina'. Agregado control unificado 'Gasto interno ▾' (Oficina o miembro) — mismo patrón/wording que la carga masiva: 'Oficina' asigna al cliente interno (Liberona Escala), un miembro setea personal_de (el save ya limpia client_id/entity_id/paid_by_client). Guardado: se oculta si el gasto es fondo, ya es personal, ya es de oficina, o está en una rendición/liquidación. Lo marcado se ve en Gastos oficina (varios + personales).
- 2026-09-03 Adopción P0 · Recordatorio a limitados sin cargar gastos +7d: nuevo edge fn gastos-limitado-recordatorio (desplegado ACTIVE) — por cada limitado (MC/MP/RD) toma max(created_at) de expenses matcheando created_by por NOMBRE **o** EMAIL (el dato viene mixto: nombre en flujo normal, email en conciliación) y, si >= umbral (config dias_gastos_limitado, default 7) o nunca cargó, le manda un nudge suave con su última fecha + CTA. Interruptor learnings aviso_gastos_limitado (off/on/inis, default off). Cron lunes 14:00 agendado (jobid 12, reusa CRON_SECRET). FALTA: encender + probar (dryRun/testTo). Dato real: Rodrigo nunca ha cargado gastos; Martín/Martina sí cargan.
- 2026-09-03 Comisiones · colaboradores: sección colapsable 'Pagadas este año' bajo el protagonista 'por pagar' → muestra a TODOS los colaboradores incl. los ya pagados (ej. Andrés Mery, 3 comisiones = $7,5M), agrupados por colaborador con total del año, pill 'Pagado' verde y drill a cada comisión (cliente·factura·fecha de pago). Reusa el patrón del bloque por-pagar (provById/cIni/titleCase/fmtDMY); total = pagadoYr (fuente única). Solo aparece si hay pagadas en el año.
- 2026-09-03 Fix bug de escritura en nombres (titleCase): el \b (word boundary) ASCII de JS trataba las vocales acentuadas como borde de palabra y capitalizaba la letra equivocada (MartÍN, áLvaro, DÍaz). Ahora capitaliza solo la 1ª letra tras separador real (^|espacio|.|-|'|/|() → Martín Campero Mantelli, Álvaro Awad, Rodrigo Díaz, Juan de la Cruz. Afecta todo nombre/proveedor/RS de la app. (Nota verificada en datos: Andrés Mery no sale en Comisiones·colaboradores porque sus 3 comisiones están pagadas; el panel muestra solo lo pendiente y cuadra $4,0M = Martín+Álvaro+Rodrigo.)
- 2026-09-03 Conciliación: hub de entrada (patrón del hub de Gastos) — landing de tarjetas que rutea a las secciones YA existentes sin quitar info. Hero 'Por conciliar' (montoPend · N mov) | 'Conciliado' (done/total); 6 tarjetas: Sin identificar, Descalces, Cargos, Cargar cartola, Sin respaldo, Todos los movimientos. Cada una setea el estado existente (sub/concView/verCarga/cobradasOpen) y apaga hubOpen; ← del interior vuelve al hub, ← del hub cierra la vista. hubOpen se apaga solo si llega focusMovId/openProp (deep-link). Cero cifra nueva: reusa resumenConc/G/cartolas y predicados del modelo. Verificado desktop (render+navegación+volver); móvil = mismo hubInner (rama D=false) del hub de Gastos.
- 2026-09-03 Pedir fondos a los abogados: botón en el hub de Gastos (bajo el hero 'Por cobrar', solo si hay clientes con saldo<0 con abogado) → modal preview agrupado por abogado a cargo (personChip), con SUS clientes sin fondos, deuda por cliente y N gastos; envía correo por abogado (detalle de gastos que la oficina adelantó + total) desde el correo del usuario (enviarComoUsuario), con 'Enviar a todos' (appConfirm). Saldo desde la fuente única (balances), abogado derivado como la ficha (respByClienteFull). Verificado desktop+móvil, cuadre exacto vs hero. FALTA: probar el envío real en prod.
- 2026-09-03 Ventas por hora · Fase A (aviso, sin tocar cifras): nuevo edge function horas-por-facturar (desplegado ACTIVE) — junta horas billable SIN facturar (report_id null) por cliente, aplica umbral (config umbral_horas_facturar default 10 + override umbral_horas_cli_<id>), agrupa por abogado a cargo (abogado_responsable → responsable de la venta más reciente) y le manda correo semanal con detalle por cliente (horas · UF a tarifa). Interruptor learnings config aviso_horas_facturar (off/on/inis, default off). Espejo de horas-recordatorio (SMTP + cron secreto HORAS_FACTURAR_SECRET). FALTA: cron viernes + secreto + probar envío (dryRun/testTo) + interruptor ON. Parte B (venta 'por hora' en el SaleForm, toca cifras) queda para después.
- 2026-09-03 Cliente en Gastos · asignar abogado desde la ficha: se cableó el control que estaba a medias (respPickG/asignarRespG sin UI). En el encabezado del cliente: chip '<abogado> ▾' clickeable, o '+ Asignar abogado' (rojo) si no tiene; abre selector Cristóbal/Erasmo/Martín/Martina/Rodrigo + Quitar (asignarRespG → abogado_responsable). El chip usa respByClienteFull (deriva de venta), así el que 'no tiene' es de verdad sin abogado. Aplica a limited y admin. Verificado en demo.
- 2026-09-03 Gastos · (1) quitado el '+ Fondo' manual (hero del cliente y '+ Registrar fondo' en el desplegable): los fondos entran SOLO por conciliación bancaria; queda '+ Gasto'. (2) FIX 'Sin abogado' en Buscar clientes: agrupaba solo por abogado_responsable, pero el abogado del cliente = abogado_responsable O el responsable de su venta más reciente (misma fuente que la ficha de Clientes, App.jsx:17834). Nuevo respByClienteFull (clients+sales) usado en la agrupación → Familia Del Sante Vives y otros con venta pero sin abogado_responsable ya caen bajo su abogado.
- 2026-09-03 Buscar clientes (Gastos) · 4 mejoras UI (limited+admin): (1) 'Última actividad' por cliente (máx fecha de gasto/fondo) con señal de inactividad en rojo si +45d ('sin mov. N d'). (2) chip verde 'N por rendir' en la fila (gastosPorRendir). (3) control de orden Nombre/Saldo/Actividad (cliOrd; con Saldo/Actividad pasa a lista plana ordenada, Nombre mantiene agrupado por abogado). (4) el buscador ya buscaba por RUT/razón social (matchC) — confirmado. Verificado en demo.
- 2026-09-03 Oficina navegable para limited (simple): ficha de oficina acotada a lo que ÉL cargó — filtered scopea office+limited por lote/created_by (bulkImports pasado al modelo); el bloque de KPI/costos se reemplaza por una nota simple 'gastos de oficina que cargaste tú · no incluye sueldos/arriendo/otros'. Acceso 'Ver como ficha de oficina →' desde la vista Gastos oficina. Admin sin cambios.
- 2026-09-03 Gastos oficina · limited ve los gastos de oficina que ÉL cargó: en la vista 'Gastos oficina', 'Gastos varios oficina' para limited se filtra a e.bulk_import_id de un lote con bulk_imports.created_by===su nombre (o e.created_by===él); admin ve todos. Encabezado aclara '· los que cargaste tú'. Antes veían todos; ahora acotado y claro (privacidad + adopción).
- 2026-09-03 Carga masiva notaría · 5 mejoras de adopción/seguridad: (1) 'Cargar' pide CONFIRMACIÓN con resumen (appConfirm 'Vas a cargar N gastos — M clientes, K por revisar. ¿Confirmas?'). (2) Aviso al salir con cambios sin cargar (cargaDirtyRef en App + guardia en onClose del modal) + beforeunload al recargar + línea fija 'se guardan al Cargar; lo aprendido queda para siempre'. (3) Toast breve por acción (flash/accionMsg + touched) al asignar/marcar/Drive/glosa/nuevo. (4) Línea visible 'puedes deshacer toda la carga con un clic' en el recibo. (5) Guía de una línea al tope de la revisión (Revisa→asigna→Cargar). Todo UX, no toca cifras ni motor. Build verde + demo sin crash.
- 2026-09-03 Carga masiva notaría · 'Gasto interno' unificado: un botón 'Gasto interno ▾' en cada fila abre selector Oficina | Cristóbal/Erasmo/Martín/Martina/Rodrigo. Oficina → cliente interno (categoría 'De la oficina'); miembro → personal_de (categoría 'Personal · de un miembro', por cobrar al abogado). marcarOficinaRow/marcarPersonalRow/quitarInternoRow. Cada fila de esas categorías: Cambiar (interno) + Quitar; oficina además 'Asignar a un cliente'. Tarjeta del hub de Gastos se mantiene una sola (adentro ya separa Varios oficina + Personales). Reusa esOficina + personal_de, cero motor nuevo. Nada en Inicio.
- 2026-09-03 Carga masiva notaría · categoría PERSONAL (gasto de un miembro): reusa el motor existente (campo personal_de + marcarPersonal + handleBulkImport que ya persiste personal_de y NO lo cuenta como 'sin cliente'). Nuevo en la revisión: botón 'Es personal ▾' en cada fila de Falta (selector de miembro con personChip) → marcarPersonalRow setea personal_de y saca el cliente; categoría propia 'Personal · de un miembro' (separada de 'De la oficina') agrupada por persona, con 'Cambiar persona' / 'Ya no es personal'. Suma a 'Interno' del hero y a 'se cargan' del recibo. Detector _pm del import casi nunca aplicaba a notaría → ahora se marca a mano. Build verde + demo sin crash.
- 2026-09-03 Carga masiva notaría · realizado lo sugerido en el audit: (#3) Drive ahora PROPONE (compuerta), no auto-asigna: driveSuggest setea suggestion+driveFolder+suggestFrom='drive' → la fila pasa a 'Confirma el cliente' para que el humano confirme (auto-Drive al abrir y botón por fila). (#5) asignar acepta method; Drive-confirmado queda matchMethod='drive' → cuenta en 'reconocidas solas', no como manual; la fila de sugerencia muestra 'Encontrado en Drive · carpeta X'. (#4) hero: 'Sin asignar aún' = solo lo verdaderamente sin cliente (tot-rend-interno) + tarjeta 'Interno' si hay oficina/personal. Build verde + demo sin crash.
- 2026-09-03 Carga masiva notaría · audit propio: (a) el buscador de categoría ahora busca también por RUT (se muestra como identificador). (b) se ocultan las categorías vacías (antes 'Falta 0'/'Confirma 0' aparecían igual). Verificado: learnRutNota deduplica RUT; notaRowCard/notaItems siguen usándose en modo conciliar (no son código muerto); puentes de navegación con todos los setters en scope.
- 2026-09-03 Carga masiva notaría · fix 'anchos distintos': alineados los bordes izquierdos dentro de la tarjeta abierta (cuerpo a 14px; filas notaCatRow, subencabezados de Falta/Listas y sin-datos a 0 horizontal → todo calza al mismo margen; antes buscador 12 / subencabezado 14 / fila 26).
- 2026-09-03 Carga masiva notaría · detalle consistente para confirmar mejor: (1) meta de cada fila muestra ahora OT · fecha · RUT · abogado (RUT era el identificador que faltaba). (2) 'Ver detalle ▾' en TODA fila (misma en todas las categorías) → tabla con Trámite/Detalle/Compareciente+RUT/Fecha/Abogado/Cliente/Observación + (si accionable) glosa editable + buscador de cliente. (3) 'Listas' ya no es doble-colapsable: subencabezado de cliente (etiqueta) + filas visibles, igual que los subgrupos de 'Falta'. (4) 'Ya cargadas'/'Sin efecto' pasan a filas ricas (misma fila, solo lectura) con nota de estado. Todo se despliega igual.
- 2026-09-03 Carga masiva notaría · FIX 'Error: Invalid Value' en Buscar en Drive: driveFindClient usaba 'title contains' (campo de Drive API v2) → Drive v3 exige 'name contains', devolvía 400. Cambiado a name. + Drive MÁS EVIDENTE en 'Falta el cliente': 'Buscar en Drive' pasa a botón PRIMARIO (relleno azul + ícono Drive), 'Asignar a mano' secundario.
- 2026-09-03 Carga masiva notaría · 4 mejoras visibles: (1) chip en hero 'N de M reconocidas solas · K aprendidas hoy' (compounding visible; matchMethod+dupInfo+aprendidasSet). (2) Confirma el cliente CON CHECKBOX (confDesel, todas marcadas por defecto) + 'Confirmar seleccionadas (N)' + 'Confirmar sola'/'Otro' por fila. (3) Resultado cierra el ciclo: tarjetas 'Por pagar a notaría $X' + 'Por rendir N clientes' (resultado.nota desde guardar) + botón Exportar CSV (exportarCargaCSV desde rows). (4) Casos borde: subgrupo 'Sin datos para identificar' (comparecientes N/A, sin botón Drive) + chip 'OT de <año>' en OT de años anteriores. Build verde; falta probar en prod con upload real.
- 2026-09-03 Carga masiva notaría · 3 ajustes: (1) PUENTES clickeables en la pantalla de resultado (Resolver las N sin cliente → Sin cliente por asignar · Pagar a la notaría → Deuda · Rendir a los clientes), cableados con patrón openOfi (App expNav → ExpensesView useEffect(navTo) cierra modal y abre la sub-vista); onNavigate en CargaMasivaModal. (2) Recibo 'A clientes' ahora dice 'N clientes'. (3) Auto-Drive: al abrir 'Falta el cliente' corre buscarTodasDrive UNA vez (2-40 sin cliente) para achicar la lista. Build verde; falta probar en prod (upload real).
- 2026-09-03 Carga masiva notaría · categorías TODO replegado por defecto (catOpen arranca vacío). Verificado en prod con archivo real: 90 OT/3 anuladas/$6.546.000; Falta 15/$512k, Confirma 2/$440k, Listas 47/$4,17M (15 clientes), Ya cargadas 26, Sin efecto 3; recibo 'se cargan 64·$5.122.000'. Cuadre exacto.
- 2026-09-03 Carga masiva notaría · revisión POR CATEGORÍAS (tarjetas, patrón app): hero blanco (Total + A clientes/Sin asignar, sin azul pleno) + 6 tarjetas-categoría plegables y accionables (Falta el cliente · Confirma el cliente · Listas para cargar · De la oficina · Ya cargadas · Sin efecto). Cada fila muestra compareciente+trámite+fecha (identifica de quién es); botones parejos Asignar/Drive/Nuevo; buscador en categorías largas; subgrupo por compareciente; grupos de cliente colapsables con aviso de fondos (cubre/adelanto). Recibo de resultado clickeable con la acción 'Cargar' colgada del dato (A clientes desplegable · quedan por revisar). Al cargar entran TODAS: con cliente a su cliente, sin cliente a la cola 'Sin cliente · por asignar' (reusa flujo+IA existente). Micro-aviso 'lo aprendí' al asignar. Pantalla de resultado en idioma de categorías + próximos pasos (Deuda/rendir). Build verde; falta probar en prod con el archivo real.
- 2026-09-02 Carga masiva notaría · revisión más ordenada (feedback prod): (1) las OT 'ya en la app' (dupInfo otState pagada/rendida/cargada) salen de 'Sin cliente · por revisar' a un grupo APARTE COLAPSADO 'Ya en la app · se omiten' (enAppOpen) → limpia el desorden. (2) 'Por revisar' se SUBAGRUPA por compareciente (subheader para los repetidos). (3) Grupos de cliente COLAPSABLES por defecto (grpOpen, chevron; ves los encabezados con N seleccionadas y expandes lo que quieras). notaItems reescrito. Verificado build.
- 2026-09-02 Carga masiva notaría · asignar→similares + scroll página: al asignar una OT a un cliente, si hay OTRAS OT sin cliente con compareciente PARECIDO (fuzzy 80-99, los idénticos ya se propagan solos), aparece un banner 'Asignaste a X · hay N OT parecidas → Asignar las N a X' (similarPrompt). En fullscreen la lista usa el scroll de la página (maxHeight none, sin scroll anidado). Con esto queda completo el rediseño de la revisión (página + filas clickeables + checkbox + importar seleccionadas + confirmar sugerido + asignar→similares). Falta probar todo en prod con el archivo real.
- 2026-09-02 Carga masiva notaría · revisión rediseñada (página + selección): (1) el modal de carga se abre a PANTALLA COMPLETA (fullscreen, se siente página). (2) Filas de notaría COMPACTAS y CLICKEABLES (notaRowCard): línea OT · trámite · cliente · monto + checkbox; se expanden al detalle (Trámite/Compareciente/Fecha, glosa a rendir editable, y CLIENTE SIEMPRE CAMBIABLE con buscador libre AsignarClienteInline). (3) SELECCIÓN con checkbox (deselNota, por defecto marcadas las que tienen cliente) + 'marcar/desmarcar grupo' en el encabezado ('N de M'). (4) Import = 'Importar seleccionadas (N)' (solo lo marcado; se quitó importar-todo y confirmar-todas para notaría → nada a ciegas). (5) Sugeridos por IA muestran 'Sugerido: X · Confirmar' en la fila. Buscar en Drive + Cliente nuevo en el detalle. Falta: asignar→similares. Build verde; probar en prod.
- 2026-09-02 Carga masiva notaría · pulido glosas + Buscar todas en Drive: (1) glosaNotaria ahora acentúa los términos frecuentes (modificación, constitución, reducción, autorización, fusión, protocolización…) y capitaliza (el archivo viene en MAYÚSCULAS sin tildes). (2) NUEVO botón 'Buscar todas en Drive' en la barra del preview de notaría: corre driveFindClient en secuencia para TODAS las OT sin cliente, con contador (driveAll {done,total}). (3) driveFindClient ahora busca por CONTENIDO y por NOMBRE de archivo (fullText or title contains). Build verde; probar en prod.
- 2026-09-02 Carga masiva notaría · E (Buscar en Drive) ACTIVADO: edge function 'drive' extendida (search devuelve parents + acción 'meta' files.get id,name,parents) y desplegada. Front: driveFindClient(nombre) busca el compareciente (fullText), sube por las carpetas (metaOf) hasta una cuyo nombre calce un cliente de la app (nombre/RS/entidades, normName) y devuelve {client,folder}. En el preview de notaría, cada OT sin cliente (bucket man/rev) tiene botón 'Buscar en Drive' → asigna el cliente encontrado (y aprende el RUT vía asignar→learnRutNota). Validado a mano contra Drive real (Fortelio/Reiner→Alejandro Lee, Gesmaq/Gesgroup→Geslog, Santa Gracia→Eduardo Barra); esas 7 RS ya se cargaron a client_entities (auto-match RUT subió 24→50 OT). Falta probar el botón en prod con sesión.
- 2026-09-02 Carga masiva notaría · B (agrupado por cliente): el preview de notaría ahora agrupa las filas por CLIENTE (notaItems), con encabezado por grupo (nombre + 'N razones sociales' si tiene varias + estado de fondos real cubre/adelanto desde saldoCliente + N OT + total) y grupo 'Sin cliente · por revisar' PRIMERO (decisión del usuario). El cuerpo de cada fila se reusa igual (glosa editable, alarmas OT, asignar, RS por OT, fecha) — no se duplicó. Un cliente puede tener varias RS y cada OT conserva la suya. Orden: por revisar arriba. Fecha del gasto = Fecha del trámite (ya). Build verde; el render agrupado NO se pudo probar en demo (requiere subir el archivo real con sesión) — probar en prod.
- 2026-09-02 Carga masiva notaría · B (parcial, foto de la carga): en el preview de notaría se agregó la FOTO financiera — 'Total de la carga' (monto + N OT + N anuladas) y 'Por rendir a clientes' (vs oficina/personal, excluye cliente interno vía esOficinaCli). El preview ya tenía glosa editable, buckets (Auto/Sugeridos/Revisar/Manual), barra 'X de N confirmadas', alarmas de OT ya en la app y asignar cliente. PENDIENTE de B (riesgoso, post-demo): agrupar la lista por cliente + aviso de fondos. E (Drive) diferido.
- 2026-09-02 Carga masiva notaría · D (glosa en lote + aprende patrón): al editar la glosa de una OT en el preview, la app aprende materia→glosa (learnings 'notaria_glosa', singleton) y la aplica EN LOTE a las demás OT del mismo trámite no editadas a mano (glosaEdited). glosaNotaria usa la glosa aprendida si existe → próximas cargas ya traen la glosa correcta por tipo de trámite.
- 2026-09-02 Carga masiva notaría · C (Abogado→responsable): el parser lee la columna Abogado (alias abogado/responsable) y la mapea a miembro (ESCALA MARCET→Erasmo, CAMPERO→Martín, Liberona/Cristóbal→Cristóbal). Al asignar/confirmar un cliente que NO tenga abogado_responsable, se lo fija con ese valor (setRespNota, no pisa uno existente, off en demo) → la Deuda agrupa por responsable correcto.
- 2026-09-02 Carga masiva notaría · capa extra RUT→cliente desde facturas: rutBillingMap (billing.receptor_rut→client_id) como fuente adicional en el match por RUT del compareciente (method 'rut_factura'), después de client_entities. Red de seguridad: recupera clientes cuyo RUT no está en client_entities pero sí facturaste. (Para el archivo real de prueba suma 0 porque los RUT faltantes tampoco están facturados aún; el lever real es aprender-al-asignar.)
- 2026-09-02 Carga masiva notaría · aprende el RUT al asignar: cuando asignas (o confirmas la sugerencia de) un compareciente a un cliente, la app guarda su razón social + RUT como client_entity de ese cliente (learnRutNota) si trae RUT y no existe ya. Así la próxima carga matchea ese RUT solo, sin trabajo manual — cada asignación deja cargado el RUT (compounding). Enganchado en asignar() y confirmarSugeridos(). Guardado con guarda (solo con RUT, no la firma/abogados, anti-duplicado, off en demo).
- 2026-09-02 Carga masiva Etapa 2 (parser export real de notaría): el importador ahora LEE el archivo real de la notaría (columnas OT·Fecha·Materia·Compareciente·Observación·Derecho·Abogado, no el modelo antiguo). Alias nuevos Derecho→monto, Compareciente→requirente. El cliente NO viene dado (col 'Cliente'=la firma): se deduce del COMPARECIENTE — parsea partes {nombre,RUT}, matchea por RUT (exactMatch), luego por nombre, luego alias aprendido, ignorando la firma y nuestros abogados (parseComparecientes/esFirmaOLawyer). Glosa a rendir armada desde Materia + detalle de Observación (glosaNotaria, editable, la IA la pule). Detecta anuladas (Observación 'SIN EFECTO'→error) e ignora la fila 'Total' (sin OT). Verificado build; falta probar carga real con el archivo.
- 2026-09-02 Carga masiva Etapa 1.1: modal de subida queda SOLO con la carga del Excel (se quitó 'Descargar modelo de notaría' y la sección 'Importaciones recientes' con Deshacer). El Deshacer se movió a la PÁGINA: dentro de cada carga (acordeón) hay 'Deshacer carga' con confirm + GUARDA DE SEGURIDAD (se bloquea si alguna OT ya está liquidada/rendida → evita descuadres). onUndoImport pasado a ExpensesView. El deshacer borra los gastos del lote y los saldos se recalculan solos (sin descuadre). Verificado demo 1280.
- 2026-09-02 Notaría/Gastos · Carga masiva Etapa 1: de modal a PÁGINA navegable (showCargaPag) dentro de Gastos. Landing con 'Subir Excel de notaría' / 'Otros gastos' (abren la carga+revisión con IA existente, onBulk) + 'Cargas recientes' como acordeones con FECHA CANON (día grande), N OT, total, estado por OT (Pagada/Rendida/Cargada), expandible. Enruta la tarjeta 'Carga masiva' del hub de Notaría y la opción de 'Cargar' a la página (ya no abren pop-up). Back → hub. Etapa 2 pendiente (revisión rediseñada en la página: foto, agrupado por cliente, glosa al cliente editable, buckets Por confirmar/Sin cliente/Por corregir/En la app, cruce de fondos) — se calibra con archivo real. Verificado demo 1280.
- 2026-09-02 Notaría · pulido interiores Deuda y Pagos realizados: (1) DEUDA — hero con más contraste (monto grande, 'N OT por pagar'), clientes ahora ACORDEONES PLEGADOS (canon, estado notaCliOpen, todos cerrados por defecto; encabezado nombre+disponible+estado+total+N OT, se expande a toggle 'Oficina cubre' + OT); Personales y Sin cliente también plegados; contraste subido (fechas C.done→C.muted, montos C.text). (2) PAGOS REALIZADOS — agrupado por MES con subtotal, cada liquidación con FECHA día-grande (canon fecha), badge estado, monto; plegada→detalle (OT+Excel/Enviar/Reenviar/comprobante/deshacer); orden nuevo↔antiguo (pagosOrd) + CSV. Verificado demo 1280.
- 2026-09-02 Notaría · rediseño hub y herramientas: (1) renombrado 4 tarjetas → Deuda (antes Pendientes de pago) · Cobros (antes Cobros de Notaría) · Pagos realizados (antes Pagados) · Carga masiva. (2) NUEVA vista Cobros = ledger de lo que la notaría nos cobra, fuente única notaLedger (gasto notaría → estado pagada/en liquidación/por pagar + error sin-cliente/duplicado, agrupado por carga masiva); toggle Por carga (lote expandible a OT) / Por OT (buscable) + Exportar CSV. (3) Pagos realizados = liquidaciones clickeables con detalle (OT, Excel, Enviar/Reenviar, comprobante, deshacer) + CSV; se mudó aquí el historial que estaba al pie de Deuda. (4) Deuda = área de liquidar sin el historial. bulkImports pasado a ExpensesView. Verificado demo 1280.
- 2026-09-02 Gastos · navegación y correcciones pedidas: (1) FIX flechas atrás — cada ← vuelve a la vista INMEDIATAMENTE anterior (Cargar→hub, antes caía en Clientes; Rendiciones/Gastos oficina/Historial→hub; sub-vistas→su padre; Sin cliente→Gastos por revisar, ya no al hub). (2) Cargar: swap 'Registrar gasto' (ahora ingresar a mano, onAdd) ↔ 'Fondos' (ahora desde conciliación bancaria, onIrConciliacion). (3) 'Gastos por revisar' pasa de acordeón a VISTA DE 3 TARJETAS (Sin cliente · Clientes archivados · Clientes ocasionales) con sub-vistas y pie 'Mover a…' (revSub). (4) FIX título vista 'Sin cliente · por asignar' (showOrphans antes que showRevision). Verificado en demo 1280.
- 2026-09-02 Gastos · pestañas Cargar y Rendiciones dedicadas (antes caían en Clientes). CARGAR (notaMenuOpen): header 'Cargar' + tarjetas Registrar gasto (→ abre Conciliación bancaria, onIrConciliacion) · Fondos · Carga masiva · Clasificar pagos (condicional). RENDICIONES (showRendiciones): header 'Rendiciones' + 3 tarjetas en orden Rendir a cliente · Pendientes · Historial. 'Rendir a cliente' = buscador de clientes (activos, no oficina) → abre RendicionModal; 'Pendientes' = worklist de clientes con gastos sin rendir +90 días (rendirPend.dias>=90), ordenado por antigüedad, con botón Rendir por fila; 'Historial' navega al Historial del hub (histTab clientes). Iconos suaves estilo hub, contraste C.text/C.muted. Gate de Clientes excluye notaMenuOpen/showRendiciones. Verificado demo 1280.
- 2026-09-02 Gastos · ajustes de contraste + limpieza: (1) 'Historial de reasignaciones' SALE de 'Gastos por revisar' (queda solo en 'Reasignar gastos'). (2) Contraste del historial de reasignaciones: from→to pasa de C.done a C.muted, destino y montos a C.text/600. (3) 'Clientes archivados/ocasionales' y sus gastos: panel expandido pasa de fondo gris (C.bgSoft) a BLANCO, montos y total de C.muted → C.text/600, texto de gasto más grande. Verificado en demo.
- 2026-09-02 Gastos · Clientes interior (ajustes pedidos): (1) 1a tarjeta 'Buscar clientes' — la lista NO se muestra por defecto; al tocarla aparece buscador + grupos por abogado (showBuscarClientes). (2) Se QUITARON los botones 'Notaría' y 'Cargar' del top de Clientes (siguen en el hub; agregar gasto/fondo desde la ficha). (3) Iconos SUAVES estilo hub (cuadro tinte claro C.azulBg/soonBg + ícono de color, no relleno). (4) Se ELIMINÓ la columna izquierda (2-panel) en escritorio → Clientes queda columna única centrada (return body); lista móvil legacy 14180 desactivada. Verificado demo 1280 y 375.
- 2026-09-02 Gastos · HUB: tarjeta 'Sin asignar' → 'Gastos oficina' (icono building, 'Varios y personales'; los huérfanos Sin cliente siguen accesibles en Clientes → Gastos por revisar). NUEVA vista 'Gastos oficina' (showGastosOficina, full-width desktop / stack móvil): sección 'Gastos varios oficina' (gastos del cliente interno, no fondo/estructural/personal; total del mes) + sección 'Personales' (gastos personal_de del equipo, por persona colapsable, total). Iconos + color por sección (azul varios, ámbar personales), contraste corregido. Header/back/gates cableados. Verificado demo 1280 y 375.
- 2026-09-02 Gastos · Clientes INTERIOR rediseñado (pulido, aprobado por render): el cockpit-muro (foto Por reembolsar/A favor, Por resolver, Conviene rendir, mini-Historial, Personales, Costos, descuadres) se REEMPLAZA por: (1) 2 tarjetas con color+icono — 'Reasignar gastos' (azul, herramienta: elegir cliente origen → marcar gastos → Mover a otro cliente, reusa doMove/AsignarClienteInline + Historial de reasignaciones) y 'Gastos por revisar' (ámbar si hay, abre showRevision con las 3 categorías Sin cliente/Cliente archivado/Cliente ocasional); (2) LISTA REPLEGADA POR ABOGADO (acordeones <details> colapsados por abogado, 'Sin abogado' para los sin responsable, con su saldo; buscador plano cuando se busca). Header/back nuevos (Reasignar gastos / Gastos por revisar). Contraste corregido (títulos oscuros) + color por sección + iconos en móvil. Se quitó 162 líneas de cockpit muerto. Verificado demo 1280 y 375. Falta: hub (Sin asignar→Gastos oficina) y Gastos oficina interior.
- 2026-09-02 Cierre de mes (CierreMesModal) ADAPTADO A ESCRITORIO (auditoría #1): antes columna de 660px topada por el Modal fullscreen a 720. Ahora en desktop (isDesktop) usa ~1100px: banda de KPIs (foto Emitido→Cobrado/Por cobrar + 3 tiles de estado) arriba, la LISTA DE FACTURAS como TABLA ancha (Cliente·N°·Resp·Emitido·Cobrado·Saldo·Estado, barra de color por estado, fila expandible con el mismo detalle/acciones), y la tendencia de 6 meses al pie. Móvil idéntico (tarjetas). Se agregó prop fsMaxWidth al Modal (default 720, no afecta otros modales); Cierre lo usa en 1160. Cross-link del nombre del cliente a su ficha OK. Verificado en demo 1280 (tabla+expand+cross-link) y 375 (tarjetas).
- 2026-09-02 Renombrado 'Copiloto' → 'Counsel AI' en la UI: botón del sidebar (SideNav), título del modal (CopilotoModal) y botón/aria del header móvil. Nombre universal (legal + IA, legible para anglo/latino). Identificadores de código (CopilotoModal/onCopiloto/copilotoOpen) y prompts internos de la IA se mantienen.
- 2026-09-02 Sidebar escritorio (SideNav): (1) 'con tecnología FirmDesk' se movió de bajo el logo AL PIE del sidebar (centrado, footer). (2) El COPILOTO pasa de un iconito de 36px al lado de Buscar a ser el botón PROTAGONISTA al pie: full-width, con label + estrella, destacado (tinte teal #9FE1CB); Buscar queda secundario debajo. Todo dentro de .sidenav (solo desktop, móvil intacto).
- 2026-09-02 Inicio: (1) rótulo 'El negocio' → 'Resultado del año' (embudo Vendido→Facturado→Cobrado→Utilidad). (2) MÁS CONTRASTE en los rótulos 'Resultado del año' e 'Indicadores' (C.done #99ABB4 → C.muted #537281). (3) FIX pifia masonry: 'Indicadores' quedaba huérfano abajo-izq en desktop (.dash-cols column-count:2 lo separaba de sus tiles) → ahora el rótulo viaja envuelto con su 1a fila de tiles (un solo hijo break-inside:avoid) y queda arriba de la columna derecha. Verificado desktop 1280 y móvil 375.
- 2026-09-02 Presupuesto Oficina (CostosOficinaModal): rediseñado de acordeones a HUB DE TARJETAS estilo Gastos — hero del mes navegable + una tarjeta por categoria (icono en cuadro, monto, N items, % del total) ORDENADAS de mayor a menor; tocar abre el panel de detalle abajo (ítems editables, aumentos a mitad de año 'subió en', agregar item, comparar vs real). Reusa costosOficinaMes/totalCat/realPorCat (cero recalculo). Adaptado desktop (grid auto-fill) y movil (2 col). Verificado en demo ambos formatos.
- 2026-09-02 Sidebar escritorio: se restaura el LOGO real del estudio (BRAND.logo.blanco, wordmark blanco) en vez del monograma verde 'L'; fallback al monograma si el estudio no tiene logo (regla vendible). 'con tecnologia FirmDesk' se mantiene debajo.
- 2026-09-02 Inicio El negocio: subtitulos 'de lo vendido' y 'margen' pasan de C.done (#99ABB4, muy lavado) a C.muted (#537281) para MAS CONTRASTE, igualando a 'neto'/'a caja'/'por cobrar'.
- 2026-09-02 Inicio "El negocio": (1) labels del embudo (Vendido/Facturado/Cobrado/Utilidad-Pérdida) ahora en MAYÚSCULAS + peso 700 + tracking, para que destaquen (móvil y desktop, mismo bloque compartido). (2) Tarjeta "Comisiones" (kTile) INVERTIDA: protagonista = lo PAGADO (comisPagadasAnioVenta.total, verde) y en la línea chica "pagado · por pagar $X" (cxpTotDash); antes el número grande era el por-pagar. Cero recálculo (reusa ambos helpers ya existentes). Verificado en demo desktop+móvil.
- 2026-09-02 Gastos "Historial" (showHistorial) → historial + inteligencia (compounding): (1) FOTO tab-aware arriba, canon de la foto — protagonista "Rendido en <año>" (Σ rendiciones cliente !anulada del año) + alertas de accion anidadas: "Por rendir · +30d" (rendirPend, helper unico) y "Sin enviar" (rendiciones sin sent_at); en tab Notaria: "Liquidado a notaria en <año>" + "Pendiente de pago" (notaPendTotal) + "Por enviar". (2) AGRUPACION POR MES con subtotal (antes lista plana que mezclaba meses), reusando la fila rica existente renderRendRow (clientes) y la fila de notaria intacta; anuladas siguen en <details> al final. (3) EXPORTAR el historial filtrado (tab+Mes/Año/buscador) a CSV (';'+BOM, abre en Excel Chile) via nuevo helper exportHist. Cero recalculo de cifras; reusa rendiciones/rendirPend/notaPendTotal/notaEstado. Verificado en demo (foto+meses+subtotales, ambos tabs, 1280 y 375, sin errores consola).
- 2026-09-02 Gastos "Sin cliente · por asignar" (showOrphans): Asistente IA que resuelve solo el cliente de cada gasto huérfano, reusando el molde de Caja Chica (runAsistente/aplicarCliente). Cascada: 1) RUT exacto (resolverClienteSII) · 2) glosa aprendida (learnings gasto_cliente/cargo_cliente, misma llave glosaKey) · 3) IA (claudeCall) para el resto, con regla dura "NUNCA inventes un client_id fuera de la lista" + re-check de existencia. Auto-asigna lo de ALTA confianza (RUT/glosa aprendida) — reversible (banda "N asignadas por la app · Ver/Deshacer" + toast del handler) y aprende al aplicar (learnPut gasto_cliente → compounde con Caja Chica). Nuevo hero "N gastos sin cliente + Resolver con IA", buscador (glosa/monto/RUT), tarjetas con sugerencia (nombre + porqué + %) o asignación manual. Full-width columna centrada en desktop, full-width móvil. Verificado en demo (RUT match auto-asigna, banda+Deshacer, ambos formatos). La escritura y la IA/learnings solo se ejercitan en prod (demo 401). FIX de paso: el card "Sin asignar" del hub no navegaba (setShowRevision no cerraba showHub) → ahora setHubOpen(false)+setShowRevision(true), como los demás cards.
- 2026-09-01 Gastos interior Cargar: el menú "Cargar" (dropdown de botones + Gastos/+Fondo/Carga masiva/Clasificar) pasa a GRILLA DE TARJETAS con formato Notaría (ícono en cuadro + título + subtítulo). Gasto(file)/Fondo(wallet)/Carga masiva(grid)/Clasificar(exchange, condicional). Se quitó "Gastos por revisar" del menú (ya es la tarjeta Sin asignar del hub). Sin tocar gates (bajo riesgo).
- 2026-09-01 Inicio: unificado el rotulo de tiles -> un solo "Indicadores" (antes "Cobros y pagos" + "Otros indicadores"). Cierra el rediseno del landing.
- 2026-09-01 Inicio rediseno etapa 2 (dedup + tiles): se ELIMINARON los bloques "Meta anual/Como va el año" (hero navy + lista ventas) e "Ingresos del año" (Ingresado a caja + Meta cobranza) del landing — duplicaban Vendido/Cobrado del embudo. El selector de AÑO + "Ver detalle ›" (->Ventas) se reubicaron en el header de "El negocio". Tiles: "Cobranza"->"Por cobrar", Comisiones ahora muestra "por pagar · $X pagado" (comisPagadasAnioVenta, se ve lo de Andres Mery), tile "Equipo · trazabilidad" quitado del landing. Todo reusa helpers (sin recalcular). Verificado en demo desktop: embudo+tiles OK, sin duplicados, año funciona.
- 2026-09-01 Inicio rediseno etapa 1 (embudo): "Estado del negocio" (stepEl vertical) -> "El negocio" = 4 tarjetas con TONO SUAVE por etapa (Vendido azul bruto/neto · Facturado ambar · Cobrado verde con a-caja/por-cobrar · Utilidad navy), meta como % chip, cada cifra de su helper (vMon/fmtMon, m.brutoUF/neto, facturadoYr, ingYTD, comisPagadasAnioVenta, resultado/margenPct) SIN recalcular. Todo clickeable. Se quito la linea de Proyeccion del embudo (ya esta como tile). OJO: queda duplicado temporal con "Como va el año" e "Ingresado a caja" -> etapa 2 los elimina + arma la fila Indicadores.
- 2026-09-01 Cargar del SII (hub Facturacion): el .xls del SII salia gris en "Cargar archivo" (accept=.xml, solo XML) → el usuario no podia cargar su descarga Excel. El importador de Excel (ImportFacturasExcel, acepta .xls/.xlsx, detecta columnas Cliente/RUT, N° factura, Monto, Fecha) estaba escondido en el menu "Importar". Fix: agregada la opcion "Importar Excel" DENTRO del hub (abre onImportExcel), y "Cargar archivo" renombrado "Cargar XML". Modal retitulado "Cargar del SII". El input del importador Excel usa <label> (Safari-safe). No cambia cifras. OJO: si las columnas del DTE_DOWN.xls del SII no calzan (Folio vs N° factura, Monto Total vs Monto) puede requerir mapeo de columnas SII — pendiente si el usuario lo reporta.
- 2026-09-01 FIX Cargar XML del SII (hub Facturacion): "Cargar archivo" no abria el dialogo de archivos. Causa: el input file (8858) era display:none y Safari BLOQUEA el .click() programatico sobre inputs display:none; ademas el onClick cerraba el modal ANTES de clickear. Arreglos: (1) input oculto sin display:none (position:absolute,1px,opacity:0,pointerEvents:none) → Safari lo permite; (2) el onClick ahora clickea el input PRIMERO (dentro del gesto) y cierra el modal despues (setTimeout 0). Solo este hub usaba .click() programatico; el resto de inputs file usan <label>. No verificable en demo (dialogo nativo); probar en el navegador real.
- 2026-09-01 Inicio (3 ajustes, con render aprobado): (1) "Prioridades financieras" deja de ser barra aparte y pasa a ser la 9a TARJETA de Accesos directos (completa la grilla 3x3, al costado de Repricing) con su badge de conteo; al tocarla se despliega la lista debajo (mismo alertExp). Se extrajo el calculo a const `prioridades` (fuente unica, reusada por la tarjeta y la lista). (2) Switch UF/CLP como pill segmentado en la fila del titulo "Accesos directos" (a la vista, controla todo el Inicio via dashMoneda; el de "Como va el año" comparte estado). (3) Saludo "¡Hola X!" sin coma (dashboard 28786 + saludo 19947). Verificado 1280px y 375px, presentacion pura sin cifras.
- 2026-09-01 Facturacion #3 REDISEÑADO (con render aprobado): "Facturar adelantos" pasa de lista plana de 33 a TARJETA del checklist (primera, protagonista) + panel agrupado por cliente colapsado con CHECKBOXES (uno/varios/todos, check por cliente marca todos) + footer "N seleccionados · $total · Confirmar". Wording/orden nuevos de las 5 tarjetas: Facturar adelantos·para emitir este mes / Facturas programadas·para este mes (ex Por emitir) / Cargar XML·de facturas del mes / Cotejar con el SII·emitidas vs SII (ex Cuadrar) / Enviar facturas·al cliente (ex Por enviar), subtitulos en minuscula. NUEVO handleFacturarAdelantos: por cada anticipo seleccionado crea 1 factura PROGRAMADA (por emitir) del mes por su monto, ligada al anticipo (billing_id + estado consumido) → entra a Facturas programadas del mes y al Excel; se emite al SII. NO toca cuotas. Reversible. Verificado en demo (tarjeta+panel+checkboxes+footer); la ESCRITURA (Confirmar) no se prueba en demo (Supabase 401) → probar en prod. handleFacturarBloqueAnticipo (Pagada) queda intacto para el flujo de ficha.
- 2026-09-01 Gastos hub + Notaria FIEL al render autorizado (movil Y escritorio). Correcciones estructurales: (1) HERO = UNA tarjeta blanca con 2 columnas separadas por linea (Por cobrar | A favor), montos ABREVIADOS (fmtShort), en vez de dos tarjetas de color. (2) TARJETAS del hub y de Notaria = blancas planas con icono en cuadro, SIN barra de color a la izquierda. (3) Sin subtitulo bajo "Gastos y Fondos". (4) INTERIOR NOTARIA = titulo + "$X pendiente" a la derecha + 4 tarjetas planas (Pendientes de pago clock, Cobros de Notaria exchange, Pagados check, Carga masiva file), sin la caja teal grande. (5) DESKTOP: Notaria/Historial/Sin-cliente ahora van a TODO EL ANCHO (early return sin el 2-panel de clientes) — antes salia la lista de clientes al costado. Verificado en demo 1280px y 375px. Presentacion pura, sin cifras.
- 2026-09-01 Gastos hub alineado al render AUTORIZADO + interior de Notaria (Set B). HUB: cada tarjeta ahora con ICONO en cuadro (SIcon users/wallet/receipt/building/alert/clock), SIN badges numericos; subtitulos del render (Clientes "Saldos y detalle · N", Cargar "Gasto · fondo · masiva", Rendiciones "Rendir a cliente", Notaria "$Xk pendiente" con fmtShort(notaPendTotal), Sin asignar "N gastos", Historial "Rendiciones y pagos"); hero "N clientes te deben" / "N en custodia". INTERIOR NOTARIA: la tarjeta Notaria abre un SUB-HUB (notaTab hub) = hero Pendiente a notaria + 4 tarjetas Set B: Pendientes de pago (->liquidacion existente, notaTab pend), Cobros de Notaria (liquidaciones no pagadas, notaEstado!=pagado), Pagados (notaEstado==pagado), Carga masiva (onBulk true). Back respeta notaTab (sub-tab->hub->cierra). Presentacion pura, sin cifras nuevas.
- 2026-09-01 Facturacion #3 — bloque "Adelantos por facturar" en el checklist del mes (BillingView, arriba de ChecklistFacturacion). Sugiere emitir la factura del anticipo: fila por anticipo con 2 casos reusando la logica EXACTA de los botones de la ficha (SaleForm 5793/5794): "listo para emitir" (consumido + cubre cuotas + sin billing_id) -> [Emitir factura] abre FacturarBloqueModal via setFacturarAnt; "disponible" -> [Aplicar y facturar] abre CubrirCuotasModal via setCubrirAnt (modales ya montados en 9183/9184). Nombre del cliente clickeable -> ficha. No cambia cifras (surfacing de acciones existentes). Verificado en demo con anticipos inyectados (bloque + modal abren OK), andamiaje revertido.
- 2026-09-01 Facturacion cuotas — Mover inicio + Pausar/Reactivar (ficha de venta, acordeon Cuotas programadas). (1) "Mover inicio a [mes] · Correr todas": corre TODAS las Programadas no emitidas por el mismo delta de meses (preserva montos y espaciado) sin borrar/recrear folios — reemplaza el mover una a una. (2) "Pausar desde [cuota]": la cuota elegida y las siguientes pasan a status Pausada -> salen SOLAS de proyeccion (2096), checklist (6094) y export (todas filtran ==Programada), sin tocar esos filtros. (3) "Reactivar": reanuda desde la cuota elegida re-anclando el devengo al mes elegido, vuelven a Programada. handleUpdateCuotas (28443) generalizado: patch condicional due/amount/status (compatible hacia atras). NO cambia montos, solo fechas y status. OJO: la escritura no se pudo verificar en demo (Supabase 401); probar en prod.
- 2026-09-01 Export facturas a emitir: descargarExcel (6148) ahora agrega una 2a hoja "No facturadas (meses ant.)" = Programadas sin folio con due ANTERIOR al mes seleccionado (arrastre real por emitir de meses previos, no solo el mes). Reusa la MISMA fila (rowOf) que la hoja Facturar para no divergir. No cambia ninguna cifra (export nuevo). Si no hay ni del mes ni arrastre, avisa.
- 2026-09-01 Rediseno Gastos etapa 2 (HUB): la vista Gastos abre en un HUB (cara de entrada) en vez del muro de paneles. Hero de 2 protagonistas: POR COBRAR (rojo, saldos negativos = la oficina puso fondos) y A FAVOR DE CLIENTES (navy, saldos positivos = fondos en custodia) - presentacion pura, MISMAS cifras auditadas (fondos-gastos, sin recalcular). + 6 tarjetas de navegacion: Clientes, Cargar, Rendiciones (badge por rendir), Notaria (teal, badge por liquidar), Sin asignar (ambar, badge por revisar), Historial. Cada tarjeta navega a la vista existente; back vuelve al hub. Estado hubOpen local a ExpensesView. Movil (hero apilado + 2col) y desktop (hero 2col + 3col) verificados en demo, sin crash. Paleta C, sin emojis. Interiores se rediseñan en etapas siguientes.
- 2026-09-01 REVERT fix cifra Gastos (auditoria de fondo): el modelo correcto (confirmado por el usuario) es saldo = fondos transferidos - gastos realizados, donde TODOS los gastos cuentan (notaria incluida, la pague quien la pague), solo se excluye no_descuenta_saldo (historico). El fix 936358e (excluir paid_by_client) estaba MAL: sacaba la notaria (que es gasto real). Vuelto a: fondos - gastos(!no_descuenta_saldo) en balances(12611) y rsBalances(593). a favor ~29.25M, por reembolsar ~2.05M (reales, en pesos, fondos matcheados a banco). OJO: regla de oro CLAUDE.md 'excluir paid_by_client' quedo obsoleta (el flag se auto-aplica a toda Notaria) - revisar.
- 2026-09-01 FIX CIFRA (auditoria Gastos): balances (12611) y rsBalances (593) descontaban del saldo los gastos paid_by_client (104 gastos, $7,43M, casi todo Notaria = pagados directo por el cliente, NO salieron del fondo), reintroduciendo el bug que la REGLA DE ORO prohibe. Ahora excluye no_descuenta_saldo Y paid_by_client. Impacto real prod: Por reembolsar $2.048.276->$846.066 (estaba inflado); A favor $29.253.207->$35.124.422 (estaba subestimado). Landing y detalle por cliente ahora cuadran.
- 2026-09-01 Rediseno Gastos etapa 1 (lenguaje hibrido elegido): la bandeja de avisos dispersa pasa a una seccion titulada 'Por resolver · N' con items en VERBO (Asignar N gastos, Mover N a Oficina, Liquidar notaria, Rendir a N clientes). 'Rendir a N clientes' baja al panel Conviene rendir (id + scrollIntoView). Aplica a movil y desktop. Base/canon 'Gastos y Fondos / Por reembolsar / A favor / Clientes' se mantiene.
- 2026-09-01 Presupuesto Oficina: 'Costos de oficina' sale de Gastos y vive SOLO en Inicio, renombrado 'Presupuesto Oficina'. Ya NO es modal: es una vista (tab presupuestoOficina, en VIEWS_PALETTE) con back + navegacion por mes 'menor mes mayor' (usa costosOficinaMes(rows, ym) con desde/monto_prev por mes). Verificado: Sep 2026 $3.98M, Feb 2026 $1.33M (antes de marzo usa monto_prev del arriendo); movil full-width. Card quitada de Gastos.
- 2026-09-01 Parte B drag&drop (cierra Parte B): en el 2-panel desktop de Gastos, arrastrar un gasto del detalle (derecha) y soltarlo en otro cliente de la lista (izquierda) lo reasigna (onAssignClientToExpense) y salta al destino. draggable/onDragStart gated isDesktop (mvil intacto, gastos no arrastrables); drop targets solo en el aside desktop; feedback inset ring al dragover. Reversible via expense_audit. Verificado: mover 'Inscripcion Conservador' de Comercial Andes a Constructora Pehuen, saldos recalculan.
- 2026-09-01 Parte B persistencia: helper usePersisted(key,initial) (localStorage, tolera modo privado). Aplicado a tblSort (orden de la tabla de Facturacion) y saldoFilter (filtro de Gastos) -> se recuerdan entre sesiones. fYear ya persistia. Verificado: cambiar orden a Monto y recargar restaura MONTO activo.
- 2026-09-01 Parte B tablas profundas: columna 'Responsable' en la tabla desktop de Facturacion (chip de persona = abogado del cliente), oculta en movil (isDesktop), ordenable (header + rama 'resp' en colValT). Entrega 'columnas ocultas en movil' + 'orden en mas vistas'. Movil intacto.
- 2026-09-01 Parte B flechas: en el 2-panel desktop de Gastos, ↑/↓ recorren la lista de clientes (misma que el panel izq: filteredClients+saldoFilter+orden), la fila resalta y hace scroll-into-view, Esc vuelve al dashboard. Gated isDesktop + no dispara si escribes o hay modal abierto. Movil intacto (handler no se suscribe <1024).
- 2026-09-01 Parte B teclado/navegacion: (1) CommandPalette (⌘K) ahora navega con ↑/↓ + Enter (elige el primero si no usas flecha) + resalta el activo (chips 'IR A' y filas); sel inicia en -1 = look identico hasta usar flecha -> movil sin cambios. (2) '/' abre la paleta desde cualquier lado (solo escritorio, si no estas escribiendo). (3) anillo de foco por teclado (:focus-visible) global (no aparece al tap). Verificado 1280 (flujo / -> flechas -> Enter navega) + 375 (paleta identica, / no dispara).
- 2026-09-01 Legibilidad desktop (fino): las fuentes mas chicas (<11px: labels/captions como 'POR FACTURAR', 'Ingresos UF...') suben a un piso de 11px SOLO en >=1024, via selector por el estilo inline que React serializa ('font-size: 9px') + !important. Con el zoom 1.18 quedan ~13px renderizados. NO cambia layout global (11px+ intacto) ni el movil (9px sigue 9px, zoom=1).
- 2026-09-01 Legibilidad desktop: zoom subido 1.08 -> 1.18 (quedaba corto). Aplica a TODA la app en >=1024 (body zoom, sin desborde ni romper vh). Movil intacto (zoom=1).
- 2026-09-01 Doble formato: (1) REGLA DE ORO en CLAUDE.md - todo desarrollo nace en movil Y escritorio (isDesktop, movil intacto, checklist 1280/375). (2) Legibilidad desktop: body{zoom:1.08} solo >=1024 (como zoom de navegador: sin desborde ni romper alturas vh; sube todo ~8% sin tocar movil ni retocar px inline). (3) Parte B inicio: hover en desktop (wash .lf-row + lift .lf-kpi + sidenav) — canal inexistente en tactil. Movil <1024 identico (zoom=1, sin hover).
- 2026-09-01 Gastos desktop: unificado el buscador (habia 2 en el 2-panel: izq + dashboard). En desktop se oculta el buscador del dashboard (!isDesktop); queda solo el del panel izquierdo. Los filtros Todos/Archivados se mantienen (alineados a la derecha). Movil intacto (buscador general).
- 2026-09-01 Conciliacion: extraida a hook useConciliacionModel (modelo=estado+datos derivados+acciones, 287 bindings) separada de ConciliacionView (presentacion). Traslado verbatim: NO cambia ninguna formula/cifra. Fix del script de extraccion: capturaba solo la 1a declaracion por linea -> se perdio valoresInfo/setValoresInfo (2 const en una linea); detectado en runtime (tab Cargos), agregado. Billing/Gastos re-auditados: sin faltantes. Verificado: build+babel OK, demo Conciliacion Abonos+Cargos+expandir movimiento sin errores, desktop 1280 + movil 375 identicos.
- 2026-09-01 Gastos desktop: presentacion 2-panel (patron Clientes) sobre useExpensesModel. Izq = lista de clientes navegable (buscador + saldo por cliente, resalta el seleccionado); der = el render actual (detalle del cliente con KPIs+gastos, o el dashboard de paneles cuando no hay cliente). La lista general se oculta en desktop (!isDesktop) para no duplicar. Reusa todo el render/logica; movil = columna unica identica.
- 2026-09-01 Gastos: extraida a hook useExpensesModel (modelo=estado+datos derivados+acciones, 235 bindings) separada de ExpensesView (presentacion). Traslado verbatim: NO cambia ninguna formula/cifra. Habilita futura presentacion desktop (2-panel lista+detalle) sobre la misma logica. Verificado: build verde, babel OK, runtime demo sin errores (paneles, lista clientes, detalle cliente con KPIs+gastos), desktop 1280 + movil 375 identicos.
- 2026-09-01 Facturacion desktop: columna Vence en la tabla 'Todas'. filaAll desktop pasa a 5 columnas (Fecha/estado/Cliente/Vence/Monto); Vence muestra fecha de vencimiento + dias en color de estado, y es ordenable (header). El diasMini se movio a Vence en desktop (en movil sigue en Monto). Solo filaAll (vista Todas); movil identico via isDesktop.
- 2026-09-01 Fix ingreso (2 bugs): (1) spinner de login pegado en 1a carga -> bootstrap de auth con timeout de seguridad (6s) + catch/finally: loadingAuth SIEMPRE se libera aunque getSession() cuelgue (lock supabase-js). (2) pantalla en blanco/'negra' -> loadUserRole ya no lanza (try/catch, devuelve null en error de red/RLS); efecto de reintento cada 1.2s; fallback de render 'Preparando tu cuenta...' si hay sesion pero el rol no resolvio (nunca el switch de vistas en blanco).
- 2026-09-01 Facturacion desktop: tabla ordenable en vista 'Todas'. Encabezado de columnas clickeable (Fecha/Cliente/Monto) alineado al grid de filaAll, ordena cada grupo por la columna elegida en escritorio; estado de orden (tblSort) en BillingView (presentacion, no toca useBillingModel). Reusa filaAll y su panel de acciones (Recordar/Editar/Enviar/Conciliar). Movil identico (header solo isDesktop; orden original por vencimiento). Primer pago visible de la extraccion a useBillingModel.
- 2026-09-01 Facturacion: extraida a hook useBillingModel (capa de MODELO: estado+datos derivados+acciones, 277 bindings) separada de BillingView (presentacion). Traslado verbatim: NO cambia ninguna formula/cifra. Habilita futuras presentaciones desktop/movil sobre la misma logica. Verificado: build verde, babel OK, runtime demo sin errores (foto Etapas del cobro, lista Por cliente, fila/renderClientGroup, aging), desktop 1280 + movil 375 identicos.
- 2026-09-01 Desktop Fase 3 livianas: 7 vistas con presentacion desktop. Ventas/Inteligencia/Tareas centradas (max-width 1040). Repricing/Cobranza ensanchadas (560->900), Horas (560->940), Cartera (720->1040) — antes columna de telefono en el monitor. Todas via useIsDesktop; movil <1024 identico (isDesktop?...:undefined/560).
- 2026-09-01 Desktop Fase 3 Gastos: en escritorio la vista (paneles + lista + sub-vistas Notaría/Revisión/Historial) se centra en columna enfocada (max-width 1040px). La lista de clientes navega al tocar (no expande inline). Móvil idéntico. 2-panel/2-columnas requiere extraer los 80 useState a useXModel (pendiente).
- 2026-09-01 Desktop Fase 3 Conciliación: en escritorio la vista se centra en columna enfocada (max-width 980px) — patrón correcto para el flujo transaccional banco↔calces (los calces son inline por movimiento, no un panel global). Móvil idéntico. El side-by-side profundo requiere extraer los 75 useState a useXModel (pendiente).
2026-09-01: Desktop/movil Fase 3 — INICIO (Dashboard) multi-columna. La raiz del Dashboard lleva className 'dash-cols'; CSS gateada @media(min-width:1024px) en el <style> del shell: .dash-cols{column-count:2;column-gap:18px;max-width:1280px;margin:0 auto} .dash-cols>*{break-inside:avoid} .dash-cols>:first-child{column-span:all}. Resultado desktop: Accesos directos a todo el ancho arriba, y el resto de las 'fotos' (Prioridades, Estado del negocio, Como va el ano, Ingresado a caja con neto, Cobros y pagos 2x2, Otros indicadores) fluyen en 2 columnas balanceadas, cada bloque intacto. CERO cambio de logica/JSX (solo 1 clase + 3 reglas CSS); reusa todos los bloques y calculos del Dashboard tal cual. Movil <1024: columnCount=auto (una columna) IDENTICO. Verificado 1280px (2 columnas) y 375px (una columna, bottomnav). Build verde.
- 2026-09-07 Cobranza · Rediseño a vista de deuda completa (pedido del usuario). (1) Muestra TODAS las facturas pendientes por cliente, vencidas o no (antes solo vencidas-con-acción) — así se ven los clientes que pagan rápido y la deuda total real. (2) Hero y tabla separan Deuda VENCIDA y Deuda TOTAL en columnas propias. (3) Facturas ordenadas nueva→antigua dentro de cada cliente. (4) Se quitó la columna/pill Nivel. (5) Consistencia: al mostrar todas las pendientes, septiembre aparece parejo para todos (junto con el fix de paginación de la carga). (6) Control de orden para móvil y escritorio: Nombre/Facturas/Monto/Vencido/Mora asc-desc (gsort compartido). El envío de recordatorio sigue solo sobre las vencidas; a los clientes al día se muestra "Al día" sin acción. detItem maneja al-día (badge verde) además de vencida. Build verde; verificado en demo (desktop table + hero).
- 2026-09-07 Cobranza · Tabla más limpia: se quitan de la vista siempre-visible las columnas "Último contacto" y "Acción"; ahora aparecen al expandir el cliente (click en la fila), junto al detalle de facturas. La tabla queda en 5 columnas (Cliente/Facturas/Mora/Vencido/Total deuda) + chevron ▸.
- 2026-09-07 Anticipos · El detalle de anticipos por cliente ahora viene PLEGADO por defecto (la lista era muy larga); se muestra nombre + total disponible + N anticipos, y se despliega al tocar la cabecera (AnticiposPanel, estado expCli).
- 2026-09-07 Anticipos · Imputar VARIOS anticipos a una factura en UN paso. Al desplegar un cliente, cada anticipo disponible tiene checkbox; una barra muestra "N seleccionados · $X" → "Imputar a una factura →" abre un selector de facturas pendientes del cliente (con saldo, nueva→antigua) → confirma e imputa todos con handleConsumeAnticipos([ids],facId) (mismo motor del "Asignar" individual; maneja excedente → sobrante vuelve disponible). La factura queda PAGADA por Transferencia (payment_method + reconciled_at), con paid_at = fecha del ingreso original: la plata YA entró a caja con la transferencia (anticipo = conciliación bancaria), imputar solo asocia la factura, NO re-suma. Prop onConsumeAnticipos: App→BillingView→AnticiposPanel. Demo: 3 anticipos c4 para probar. Build verde; verificado en demo (checkbox+barra+selector).
- 2026-09-07 Conciliación/Banco · Color de la lista menos plano (pedido del usuario, opción A "bandas suaves"): año en banda azul (C.azulBg, 15px/800), mes en banda gris (C.bgSoft, 13.5px/700 accent), movimientos en blanco → 3 niveles de jerarquía. Se quitaron los CONTEOS de movimientos (total del título, "N mov." por año y "· N" por mes); se mantiene el monto ($) por mes.
- 2026-09-07 Navegación · Unificada bajo navStack (auditoría): nuevos helpers navTo(destino) — apila SIEMPRE el estado actual (pestaña+ficha+scroll) y salta al lugar exacto con foco opcional (conc/cartera/billingIntent) — y goBack() — vuelve a la página inmediatamente anterior restaurando pestaña+scroll, fallback al Inicio si la pila está vacía. Antes solo "abrir ficha de cliente" apilaba y los 5 "Volver/Cerrar/←" (Conciliación, Cartera, Cobranza, Repricing, Presupuesto Oficina) estaban hardcodeados al dashboard → te devolvían a otra parte. Ahora esos 5 usan goBack. Saltos convertidos a navTo (para que la vuelta caiga exacta): onIrCobranza/onIrConciliacion (Facturación y Cobranza), onCotejarSII, handleOpenConciliacion (ficha→movimiento), onOpenProyecto y onAcceso (Dashboard). Pendiente follow-up: KPIs internos del Dashboard (setTab suelto, sin regresión por el fallback) y etiqueta "← {origen}" en las 5 páginas. Build verde; app monta sin crash.
- 2026-09-07 Conciliación · 5 ajustes de lista (Abonos y Cargos, render compartido): (1) el buscador ahora también busca por CIFRA — dígitos de la query contra el monto del movimiento (`qn`/`mn`, App.jsx ~26970); "11.700.000" o "11700000" encuentra el abono. (2) Header CARTOLAS: se quitaron los totales +abonos/−cargos (no aportaban); queda "N mov" (App.jsx ~27251). (3) Fondo de las filas de movimiento a BLANCO (`#fff`) — antes heredaban el gris del fondo y no se distinguían (App.jsx ~27715). (4) Cabecera de MES: celeste (C.azulBg) → gris (C.toggleOff), jerarquía año navy → mes gris → movimiento blanco (App.jsx ~27709). (5) Separación entre AÑOS: `marginTop:12` en cada cabecera de año salvo la primera (App.jsx ~27703). Verificado en demo. Build verde.
- 2026-09-08 Conciliación · FIX falsos positivos "sin depósito que calce" (Facturas sin respaldo): el panel/contador de Conciliación calculaba el respaldo con `aplicadoByFactura` (App.jsx:25762) contando SOLO `conciliacion` a nivel factura, e IGNORABA los anticipos consumidos contra la factura. Por eso las facturas pagadas con anticipos (que sí tienen su plata en el banco vía conciliación tipo 'anticipo') aparecían falsamente "sin respaldo" (ej. Cavor 408, Clínica Vizcaya 398 = pago de Gabriela del Fierro, Comercial Aroha 404 = Happy Mom, Familia Del Sante 439 — los anticipos suman EXACTO cada factura). Fix: `aplicadoByFactura` ahora suma también el respaldo INDIRECTO de anticipos consumidos con conciliación bancaria (mismo criterio que `respaldoMap` de Facturación, App.jsx:28775). Así esas facturas dejan de salir en "sin depósito que calce". Las que quedan (sin anticipo ni conciliación) son casos reales a conciliar. Build verde; Conciliación sin crash en demo.
- 2026-09-08 Facturación · REGLA UNIVERSAL de vencimiento = emisión + 30 días (blindada en código). Síntoma: facturas recién emitidas aparecían "Vencidas" porque las cuotas nacían con `due` = día 1 del período (en el pasado, por mes vencido) y/o con `status='Vencido'` guardado. (1) DATOS: `due = issued_at + 30` para las 377 emitidas (con folio), y recomputado Vencido/Pendiente según ese vencimiento → 0 "recién emitidas y vencidas" (antes 7); vencidas reales 33. (2) CÓDIGO (blindaje): `venceBill` (App.jsx:162) ahora SIEMPRE calcula emisión+30 e ignora el `due` guardado (con issued_at); `esVencidaB` deriva puro de ese cálculo (no del status guardado) → ningún `due` mal puesto puede volver a marcar vencida una factura nueva. Build verde. Los abonos mal marcados internos (glosa "Transf. a terceros" a cuentas 1383922/1403834) se desmarcaron (20) para entrar a Conciliación; el $1.080.000 recurrente es el subarriendo de Rodrigo (categorizar, no factura); Digitalrad 255/344 conciliadas a los pagos de Daniela Figueroa; Mobilitex (error) fusionado a Luz Austral. Todo cambio de datos en prod, reversible.
- 2026-09-08 Navegación · Inteligencia y Dashboard al patrón navStack. Inteligencia: el "← Inicio" hacía setTab('dashboard') crudo (volvía al Inicio, no al origen) → ahora usa `onBack`=goBack con rótulo "← {origen}"; sus KPIs (Vendido/Por cobrar/Cobrado) y los saltos del Dashboard (atender hoy: Cobranza/Pagar/Repricing/Cargar horas; Ver detalle; Proyectos críticos/sin mover; Abrir en Cartera) pasaron de setTab crudo a `navTo` (apila origen+scroll) vía helper `go`. Así "Volver" regresa al lugar exacto. Build verde. (Auditoría de navegabilidad completa en curso para el resto de las vistas.)
- 2026-09-08 Facturación · Subtotal "Pagadas" y filas ahora usan el DTE (montoFactura), no el monto programado (b.amount). `montoDe` en las listas (App.jsx:9755 vista agrupada y 9876 vista "todas") devuelve `montoFactura(b)` para emitidas (Pagado/Pendiente/Vencido) y deja la estimación UF-de-hoy solo para Programadas; la fila (helper `fila` + vista "todas") muestra el mismo DTE. Cuadra "Pagadas" con SII/banco. Build verde.
- 2026-09-08 Navegación · Auditoría completa + fixes (el usuario reportaba "nada me devuelve a donde venía"). RAÍZ: la barra admin solo tiene 6 tabs; el resto (conciliacion/cartera/cobranza/repricing/inteligencia/presupuestoOficina/tasks/cajachica) se alcanza por cross-link, y muchos saltos usaban `setTab` crudo → la pila quedaba vacía y "Volver" caía en Inicio. Fixes: (ALTA) Horas/Gastos→Presupuesto Oficina, hub Conciliación, modal anticipos "Ir a Conciliación" pasan a `navTo`; Tareas y Caja chica (vistas admin sin retorno) ahora muestran "← {origen}" (solo admin con pila). (MEDIA) paleta de comandos, "atender hoy" del Dashboard, "Cotejar con SII" → `navTo` (con billingIntent). (BAJA) `TAB_LABELS` completado con cartera:'Proyectos' y presupuestoOficina para el rótulo del back. Inteligencia+Dashboard ya migrados en commit previo. Build verde; Inteligencia sin crash en demo. Pendiente menor: foco exacto por-factura en "buscar pago" (M6/M7) y navTo en el nudge de Caja chica desde Tareas (M8).
